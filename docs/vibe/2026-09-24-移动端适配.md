# 移动端适配（手机端不破版）

日期：2026-09-24
范围：`web/` 前端；不改后端、不改接口

## 目标

让平台在手机上（≤430px CSS 宽度）可用。当前已有半套移动端机制（`AppLayout` 的抽屉侧栏、部分页面的 768px 断点），但主链路仍有硬破版点，且**全项目没有一条 `@media` 以 430px/400px/375px 为断点**。

## 范围

| 范围 | 处理方式 |
|---|---|
| 主链路 4 页：登录 / 问答 / 知识库（列表+详情）/ 数据总览 | 完整适配，硬破版点全部修掉 |
| 其余 17 个管理页 | 只保证不横向溢出、不破版；不重排布局 |
| App 外壳（Capacitor） | 响应式为主，用安全区变量兜住状态栏/刘海/软键盘 |

**深度：最小可用。** 不做底部标签导航、不做列表卡片化——那是另一个量级的工作。

## 验收标准

直接采用 `docs/develop-guides/design.md` §8「响应式行为」，不另起一套：

1. 小屏下表单字段纵向排列，按钮组可换行
2. 侧栏、抽屉和弹窗要保证最小宽度内内容可读
3. 表格在小屏下允许横向滚动；不要把关键字段压缩到不可读
4. 图标按钮和主要操作需要保持可点击区域，移动端目标尺寸尽量不小于 40px

补充两条可验证的：

5. **在 430px 宽度下，任何页面都不出现横向滚动条**（表格自身的横向滚动除外）
6. **在 430px 宽度下，所有主操作按钮可见可点**（不允许被 `overflow: hidden` 裁掉）

## 已确认的硬破版点（修复清单）

### T0 — 硬溢出，主链路必现

- [x] `FileBrowserTable.vue` 表格窄屏压缩列宽 → 给内层 `<table>` 加 `min-width: max(100%, 520px)`，交由 `.ant-table` 已有的 `overflow: auto` 横滚
- [x] `PageShoulder.vue` 的 `.page-shoulder` / `.page-shoulder-left` / `.page-shoulder-right` 加 `flex-wrap` + `min-width: 0`，`.search-input` 加 `max-width: 100%`
- [x] `DashboardView.vue` 的 `.dashboard-toolbar` / `.toolbar-actions` 加 `flex-wrap`
- [x] `AgentStatsComponent.vue` 表格加 `:scroll="{ x: 420 }"`

**关于 `FileBrowserTable` 的一处方案修正**：初版计划照抄 antd 的 `:scroll="{ x: 'max-content' }"`，实际不可行。rc-table 在未设 `scroll.y` 时会把 `overflow-x: auto; overflow-y: hidden` 加到 `.ant-table-content` 上，使其成为新的滚动容器；而本组件的粘性表头（`position: sticky; top: 0`）依赖 `.ant-table` 作为滚动容器，改完会**表头固定失效**。`'max-content'` 还会把文件名列按完整内容宽度计算，桌面端也冒出横滚。改用 CSS `min-width` 达到同一目的：`max(100%, 520px)` 在桌面端恒等于 `100%`，行为不变。

### T1 — 已确认后判定为「紧但不破」，未改

- [~] `ToolStatsComponent.vue` `<a-col :span="12">` — 两列各 50%，单元格内容为短标签，430px 下每列约 175px，可读，不改
- [~] `AgentStatsComponent.vue` 顶部 `<a-col :span="8">` — `a-statistic` 标题最长 5 个汉字约 70px，430px 下每列约 127px，不溢出，不改
- [x] `CallStatsComponent.vue` 的 `.simple-controls` — 7 个筛选 chip 约需 530px 塞在卡片 `#extra` 里，必然撑破卡片。改为 ≤768px 时卡片头换行、`extra` 独占一行、chip 加大到约 34px 触控高度

### T2 — 外壳与全局

- [x] `index.html` 加 `viewport-fit=cover`
- [x] `base.css` 加 `--safe-top` / `--safe-bottom`
- [x] `AppLayout` 用 `100dvh`；抽屉侧栏与汉堡按钮让开安全区；`.app-topbar` 加 `--safe-top`；`#app-router-view` 加 `--safe-bottom`
- [x] `LoginView` 不在 `AppLayout` 内，`.login-navbar` 与 `.login-main` 各自让开 `--safe-top`

### 主链路复查中额外发现并修掉

从数据总览的三张卡分别跳到「反馈列表 / 知识缺口 / 问答明细」，这三页算主链路延伸，一并过了一遍。

- [x] `HumanApprovalModal.vue`（在聊天页内）`min-width: 360px` → `min(360px, 100%)`，375px 视口下聊天区仅约 343px 会被顶破
- [x] `RAGEvaluationTab.vue` 的 `.progress-info`：`min-width: 300px` 统计列 + 24px 间距 + 固定宽按钮约 424px，加 ≤767px 纵向排列
- [x] **`a-drawer` 的 `width="560"` 不会被 antd 收窄**（与 `a-modal` 不同，antd 不给抽屉加 `max-width`，只把 560px 内联到 `.ant-drawer-content-wrapper`），430px 视口下会有约 130px 内容留在屏幕外。四处固定宽抽屉全部改为 `min(NNNpx, 100vw)`：`QaRecordsView` 560 / `KnowledgeGapsView` 520 / `FeedbackManagementView` 560 / `UdeskConversationView` 560
- [x] `QaRecordsView.vue` 表格 7 列（固定列合计 610px）加 `:scroll="{ x: 960 }"`
- [x] `KnowledgeGapsView.vue` 表格 7 列（固定列合计 1140px）+ `fixed: 'right'` 操作列，加 `:scroll="{ x: 1140 }"`；`.filters` 无 `flex-wrap`（320px 搜索框 + 3 个 180px 下拉约需 980px）补上
- [x] 弹窗（`FeedbackTuningModal` 760 / `DataBaseInfoView` 700 / `DataBaseView` 800 / `FileTable` 600）**无需处理**：antd v4 的 `.ant-modal` 自带 `max-width: calc(100vw - 32px)`，≤575px 时收紧到 `calc(100vw - 16px)`
- [x] `DataBaseInfoView` / `DataBaseView` / `AgentChatComponent` / `ModelProviderManagePanel` 已有 1024/768/767 档覆盖，未动
- [x] `FeedbackManagementView`（`:scroll="{ x: 1200 }"` + `flex-wrap`）与 `KnowledgePermissionPanel`（`:scroll="{ x: 'max-content' }"`）已自带横滚，未动；仓库里另一处 `fixed: 'right'` 且无 `:scroll` 的表格不存在了

## 已确认安全（不要重复处理）

- `FeedbackSummaryComponent.vue` — 有 600px 档，已覆盖 430px
- `AgentInputArea.vue` — 768px 档已消掉唯一的 220px 固定卡宽，全批最干净
- `AgentView.vue` — 旧侧边栏已移除，body 内无并排子项
- `DataBaseInfoView.vue` 的 `.query-config-pane`（360px 固定宽）— 1024px 档已转列
- `AgentChatComponent.vue` 的 `.side-panel--state` / `--file` — 768px 档 min-width 归零 + `calc(100% - npx)` 写法正确
- 所有 `position: absolute` 场景 — 全部用 0 / 百分比 / transform / CSS 变量定位，无 px 偏移导致的视口逃逸
- `.app-layout { min-width: var(--min-width) }` — 768px 档已归零，430px 下不构成全局破版

## 已知缺口

**其余 17 个管理页未做逐页盘点。** 本次只保证「不横向溢出」，方式是：在 430px 下逐页过一遍，发现硬溢出再针对性修，不做系统性重排。

## 不做什么

- 不做底部标签导航、不做列表卡片化（超出「最小可用」）
- 不动 `--min-width` 的 450px/400px 双定义（`main.css:10` vs `base.css:142`）——768px 档已归零，非破版点，单独记一笔
- 不引入新的样式体系或断点变量层，沿用 `base.css` 现有 1440/1199/767 三档

## 验证方式

1. Docker 内前端热重载，浏览器按 430×932（iPhone 15 Pro Max 逻辑分辨率）逐页过
2. 登录 → 问答 → 知识库列表 → 知识库详情 → 数据总览，五个页面确认无横向滚动条、主操作可点
3. 跑 `pnpm run lint` 与既有前端测试

## 验证状态

| 项目 | 结果 |
|---|---|
| `eslint`（本次改动文件） | 通过 |
| `pnpm run build` | 通过 |
| `pnpm run check:i18n` | 通过（exit 0，残留中文行为既有基线） |
| `pnpm run test:frontend` | 16/16 通过 |
| **横向溢出实测** | **通过** —— 6 档宽度 × 11 页 = 66 个组合，越界元素 0 个（见下） |
| **按钮未被遮挡实测** | **通过** —— 六个页面中心点命中均为按钮自身（见下） |

### 横向溢出实测（补充执行）

Docker 起来后补做的。用 headless Chromium（playwright-core + 本机已缓存的 chromium，注入本地签发的测试 JWT 到 `localStorage.user_token` 绕过登录），在 **430 / 414 / 390 / 375 / 360 / 320** 六档宽度 × **工作台、数据总览、知识浏览、扩展、知识库详情、问答、问答明细、反馈列表、知识缺口、模型管理、登录** 11 个页面共 **66 个组合**上逐页量：

- `document.scrollingElement.scrollWidth > clientWidth`（文档级横向滚动）
- `#app-router-view.scrollWidth > clientWidth`（内容区横向滚动）
- 遍历 `#app-router-view *`，找右边界越出视口、且不在任何 `overflow-x: auto/scroll/hidden` 祖先内的元素

**结果：66 个组合全部为 0。** 验收项「430px 下无横向滚动条（表格自身横滚除外）」由按 CSS 推导变为实测结论。

### 一次被推翻的猜测（记录以免重蹈）

曾怀疑汉堡按钮（`.mobile-sidebar-trigger`，`position: fixed; z-index: 1000`）会被 `PageHeader`（`position: sticky; top: 0; z-index: 1000`，DOM 顺序在后故同层时绘制在上）盖住，进而怀疑 `.app-layout { height: 100dvh }` 与 `body { min-height: 100vh }` 不一致会让文档也滚动、把粘性头部拖进按钮区域。

实测均不成立：按钮矩形恒为 `[10, 10, 48, 48]`，`.app-topbar` 底边 48，`PageHeader` 从 48 起，三者严丝合缝不重叠；文档在六档宽度下均不可滚。`document.elementFromPoint` 打在按钮中心返回的就是按钮本身，六页皆然；`z-index ≥ 1000` 且与按钮矩形相交的元素，除 antd message 容器外一个都没有。

**故未做任何 z-index 改动。** 甲方反馈的「被盖住」后来确认是在 Chrome 手机模拟下看到的旧产物（热重载尚未吃到 `AppLayout` 那次改动），刷新后即正常。
