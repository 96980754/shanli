# 第二批缺陷修复（2026-09-12）

## 目标

在不改变普通 RAG 版本口径、历史 API、统计规则和 Office 编辑协议的前提下，修复第二批已确认缺陷。

## 范围

- 修复 HTML/DOCX 嵌套表格在知识入库和语义表格转换中的内容丢失、重复及外层列破坏。
- preview 显式 `search_mode` 按知识库类型校验，非法输入返回 422。
- 补齐中英文固定知识拒答、范围拒答和系统错误文案，并统一拒答分类。

## Checklist

- [x] 嵌套 HTML/DOCX 内容压平后完整保留且不重复。
- [x] 语义表格转换只把直接子行、直接子单元格纳入外层网格。
- [x] preview 非法模式在调用检索前失败。
- [x] 英文固定拒答不再被记录为正常回答。
- [x] 项目虚拟环境定向测试通过（131 passed，1 skipped；另有 4 条相关 prompt 测试通过）。
- [x] Docker 容器定向测试通过（137 passed，1 skipped）。
- [x] Ruff 与 diff 检查通过。
- [x] API 热重载/启动日志无本批导入或迁移错误。

## 非目标

- 不增加 DOCX 嵌套表格在线编辑协议。
- 不启用或重构通用多语言翻译。
- 不使用模糊文本匹配识别任意模型或供应商拒答。
- 不修改 Dashboard、history API、judge、handoff 规则或普通 RAG 当前版本过滤。
