# 版本变更记录

本页用于记录各版本发布说明（新增、修复与破坏性变更）。

同一版本的多次功能更新时，应以功能为单位进行更新，比如之前添加了 A 功能的更新，在后续的更新中修复了因 A 功能引入的 bug，那么这个修复说明应该和 A 功能描述放在一起，而不是新增一条修复记录，功能更新同理。

## v0.7.1 (current)

### 破坏性变更

- API Key 收紧到具体用户：`api_keys.user_id` 收紧为非空，启动 schema 演进会先清理 `cli_auth_sessions` 中对未绑定 API Key 的引用，再 `DELETE FROM api_keys WHERE user_id IS NULL`，最后 `ALTER COLUMN user_id SET NOT NULL`。**升级前请在 0.7.0 库执行 `SELECT id, name, department_id FROM api_keys WHERE user_id IS NULL;`**，决定每个未绑定 Key 的归属用户并手动 `UPDATE`，未绑定的 Key 升级后会被静默删除且无法恢复；清理前后端日志会输出 `Schema migration will delete N unbound API key(s)` 告警以便回溯。
- Dashboard 收紧到 superadmin：所有 `/api/dashboard/*` 端点从 `get_admin_user` 收紧为 `get_superadmin_user`，前端路由同步收紧。0.7.0 中创建过 `role='admin'`（非 superadmin）的运维用户升级后将失去 Dashboard 访问权限，且应用内无自助提权路径；升级前请在数据库中将需要继续访问 Dashboard 的 admin 用户 `UPDATE users SET role='superadmin' WHERE uid=...`。首装场景的首个管理员始终是 superadmin，新部署不受影响。
- CORS 生产环境默认拒绝跨域：CORS 改为通过 `YUXI_CORS_ORIGINS` 显式配置允许来源；`YUXI_ENV=production` 且未设置该变量时返回空列表（拒绝所有跨域），显式设为 `*` 时会自动关闭 credentials。**前后端跨域部署的运维请在升级前设置 `YUXI_CORS_ORIGINS=https://your-frontend.example.com`**，否则浏览器跨域请求将被拒绝；同源部署（前端与 API 同源）不需要额外配置。
- 系统配置接口权限下放：`GET /api/system/config` 由 admin 收紧到任意登录用户可读，便于普通用户读取 `default_ocr_engine` 等运行时配置；接口会暴露 `sandbox_provisioner_url`、`sandbox_virtual_path_prefix`、默认模型 ID 等基础设施信息（不包含任何密钥/Token），如有更高保密要求请通过反向代理限制该路径。

### 开发记录

- 接入 Capacitor 生成 Android 原生工程（移动端方案：WebView 封装，一个工程同时出 Android/iOS、业务代码共享；分工为「Android 我们出包、iOS 交给甲方自行配置」）。`web/` 新增 `@capacitor/core`/`@capacitor/android`（^8.5.0，dependencies）、`@capacitor/cli`（devDependencies），`web/capacitor.config.json`（`appId=com.yuxi.knowledge`、`appName=AI 知识库`、`webDir=dist`；上线前 appId 需换成甲方包名），`npx cap add android` 生成 `web/android/` 原生 Gradle 工程（`app/src/main/assets/public` 等构建产物已被 android/.gitignore 忽略）。出包：`pnpm install && pnpm build && npx cap sync android` → Android Studio 打开 `web/android`（或 `./gradlew assembleRelease`）。加载模式：默认「本地打包」把 dist 打进 APK——注意前端 API 全是相对路径 `/api/*`，本地打包下会打到 WebView 自身 localhost 而不可用，须用「在线加载」或在构建时把 API base 指到公网地址；生产推荐「在线加载」：config 配 `server.url` 指向 HTTPS 域名后 WebView 直接加载线上站点、前端发版免重打 APK，前提是 nginx 上线 HTTPS（当前仅监听 80 无 TLS）。iOS 交接给甲方：甲方在 Mac 上 `git clone && cd web && pnpm install && npx cap sync ios && npx cap open ios`，选 Apple 开发者账号 Team + Bundle ID 即可出包（iOS 不能侧载，须经 TestFlight/App Store/企业签名）。交接文档见 `docs/vibe/2026-08-26-mobile-app-capacitor.md`。
- 语音转写兼容 iOS 录音（移动端前置条件：iOS WKWebView/Safari 的 MediaRecorder 只支持 MP4 容器（AAC）、不支持 WebM，此前前端 `selectRecordingMimeType` 只认 webm 会直接「设备不支持」无法录音，后端也 415 拒绝 mp4）。前端 `voiceRecorder.js` 候选类型增加 `audio/mp4;codecs=mp4a.40.2`/`audio/mp4`（webm 优先、mp4 兜底），`transcriptionApi.transcribe` 按 blob.type 派生文件名（MP4 家族 → `recording.m4a`，否则 `recording.webm`）；后端 `transcription_service` 的 `SUPPORTED_TRANSCRIPTION_MIME_TYPES` 增加 `audio/mp4`/`audio/x-m4a`/`audio/m4a`/`audio/mp4a-latm`，转发 Provider 的文件名后缀按容器派生（MP4 家族统一 `.m4a`、webm 保持 `.webm`，不再信任上传文件名）。实测 SiliconFlow ASR（`Qwen/Qwen3-ASR-1.7B`）按**文件名后缀**判定音频格式：同一份 AAC 音频用 `.mp4` 后缀返回 500、`.m4a` 后缀正常，故统一派生 `.m4a` 即可、无需本地转码。补充单测：MP4 家族各 MIME 接受并转发 `.m4a`、webm 仍转发 `.webm`、`audio/mpeg` 仍 415；前端 voiceRecorder 新增「仅支持 mp4 的 iOS」场景测试；真实链路用 ffmpeg 合成 AAC/MP4 录音 + 真 Provider 验证三种 mp4 MIME 均转写成功。
- 产品参照图管理入口（图搜参数信号②的运维闭环，甲方此前只能手工传 MinIO + CLI 建索引）：平台级「产品图库」独立页（顶栏菜单 admin 可见、路径 `/product-images`，原知识库详情页「产品参照图」页签已移除），跨库聚合管理——知识库筛选器 + 选中具体库后多图拖拽上传（扩展名 `.jpg/.jpeg/.png/.webp/.bmp`、单张 ≤ 20MB、文件名即产品名并保留中文），列表跨库展示缩略图（MinIO `public/{kb_id}/product-images/{产品名}.{ext}`，经 `rewriteMinioImageUrls` 走站内代理）/产品名/知识库/索引状态（已索引绿标/未索引灰标）/删除（Popconfirm），「重建索引」手动按钮按选中库触发（遵循手动触发、控制 VL 嵌入 API 成本）与「刷新」。后端 `knowledge_router` 新增 `GET/POST /knowledge/databases/{kb_id}/product-images`、`DELETE .../product-images/{product_name}`、`POST .../product-images/rebuild` 四端点，并新增跨库聚合 `GET /knowledge/product-images`（遍历 Milvus 库合并 MinIO 参照图与 Milvus 索引状态、返回带 kb_id/kb_name 的扁平列表），统一 `get_admin_user` + `_require_kb_permission(can_manage)` + `_ensure_database_supports_documents` 鉴权；上传只落 MinIO（上传前 `ensure_bucket_exists("public")` 幂等重设匿名读策略，防直读 403）、不触发重建；删除为 MinIO + Milvus 双删；`ProductImageIndex` 新增 `list_indexed`（查已索引产品名集合）与 `delete_image`（单产品向量删除）两个异步方法，沿用 milvus.py 的 `collection.query/delete` + `asyncio.to_thread` 模式。补充单测 20 条：router 17（非 admin 403、权限/文档校验接线、文件名规范化参数化、列表合并索引状态、上传落桶+ensure_calls、扩展名/大小校验、删除双删/404、rebuild 返回、聚合端点非 admin 403、聚合端点跨库合并+过滤非 Milvus 库）+ index 3（list_indexed 断言 expr/output_fields/load、空集合、delete_image expr）。真实链路验证（测试图 + 手工签名 admin JWT）：上传→列表「未索引」→重建（1 张真实 VL 嵌入，indexed=1 errors=0）→列表「已索引」→`ProductImageIndex.search` 同图命中 0.999→删除→列表清零、Milvus 最终无残留（删除对已加载集合有短暂最终一致性窗口，与既有 delete 模式一致、不 flush，list 以 MinIO 为准不受影响）。已知限制（范围外）：`search_product_image` 全局搜暂无 kb_id 过滤、重建不校验产品名是否存在于库内文档（靠 UI 引导文案约束），另开任务。
- 新增产品图片识别 + 参数检索（问答场景，甲方需求「用户上传产品图片，识别是哪款产品并检索其参数」）：多信号融合识别。信号① 视觉模型直接读图——默认模型 `siliconflow-cn:Pro/MiniMaxAI/MiniMax-M2.5` 是视觉模型，多模态链路已存在（`image_content` → `build_chat_input_message` 构造多模态 HumanMessage，`message_type='multimodal_image'` → 正常 Agent 图，同一轮可直接 `query_kb`），读铭牌/标签/机身文字 + 外观 + 用户文字得型号候选；信号② 图片特征检索——新模块 `yuxi/knowledge/product_image_index.py` 用 SiliconFlow `Qwen/Qwen3-VL-Embedding-8B`（768 维视觉 embedding，`POST /v1/embeddings` VL 格式）把运营维护的产品参照图向量化，写入独立 Milvus `product_images` 集合（COSINE/IVF_FLAT，行带 kb_id/product/image_url，与文本检索集合隔离），内置 `search_product_image` 工具取当前对话最新一张用户图片做外观相似度 Top-K 候选；融合裁决写进系统提示词 `PRODUCT_RECOGNITION_PROMPT`（插在业务回答与图片展示之间、硬性护栏之前）：两信号一致且置信高直接确认型号，不确定/无标识/贴牌时列出候选并用 `ask_user_question` 反问确认、不得仅凭相似度直接断定型号，两信号都空时按统一拒答规则诚实说明，参数一律走文本 `query_kb` 检索并以 Markdown 表格 + 来源呈现。参照图约定：MinIO `public/{kb_id}/product-images/{产品名}.jpg`（文件名即产品名、每款一张），运维用 `python -m yuxi.knowledge.product_image_index <kb_id>` 建索引（幂等、可重复执行）。存储边界（已与业务确认）：用户上传的图片不落任何数据库、只随对话消息走（`Message.image_content`），参照图本体留在 MinIO 对象存储不复制，进向量库的只有特征向量（几十字节/张），用户图特征实时算一次用完即弃，Neo4j 图数据库完全不用。修复两个图片-only 拦路 bug：① `agent_router.py` 创建 run 的入口校验从 `payload.query` 改为 `payload.query or payload.image_content`，仅上传图片（无文字）不再 422；② `chat_service.py` 知识库预检加 `not image_content` 短路——有图时不再用空文字检索触发预检、把图片消息误拒答。实测信号②：同图相似度 0.9999/0.9906、异图 ~0.4，区分度清晰；建索引/重复实例化/检索/清理全链路端到端通过。补充单测：图片-only run 不 422 且构造多模态输入消息、`build_chat_input_message` 多模态 content 结构（text + base64 image_url data URI）、prompt 含识别指令且位于护栏前、图片消息跳过知识库预检（纯文本仍保留预检）、`search_product_image` 无图报错/命中返回候选/索引异常降级。**真机 e2e 修复对话图片的 OCR 兜底链路**：实际运行默认模型是 `deepseek:deepseek-v4-flash`（纯文本、拒图；Redis `yuxi:runtime_config` 覆盖了 base.toml 的 `siliconflow-cn:Pro/MiniMaxAI/MiniMax-M2.5`，而该 `Pro/` 规格不在模型缓存中无法加载，缓存里可用的视觉模型是 `siliconflow-cn:MiniMaxAI/MiniMax-M2.5`，可手动选配）——此前设想「默认模型自带视觉」不成立，图片进纯文本模型必走 OCR 兜底；而中间件 OCR 兜底此前只处理 `read_file` 落盘的沙盒文件，用户对话上传的 base64 图只随消息走、从未落盘，兜底便返回死胡同提示「当前模型无法读取图片，且没有可供 OCR 工具解析的文件路径」。现 `ImageInputCompatibilityMiddleware` 在模型拒图时把对话里的用户 base64 图按内容 sha256 确定性命名（`chat-image-{hash16}.{ext}`）落沙盒 `threads/{thread_id}/user-data/uploads/`（先 `ensure_thread_dirs`；拿不到 thread/uid 作用域则不落盘、保持诚实拒答），返回虚拟路径并直接发起 `ocr_parse_file` 工具调用；已 OCR 过的图片在后续轮次按「路径→最新 OCR 调用位置」从模型输入中自动剥除并替换为占位说明（路径可重算，不依赖中间件实例状态，跨轮/跨运行安全）。真机验证（真实 DeepSeek V4 Flash 拒图 + 真实 rapid_ocr）：落盘 `chat-image-13fc44ca9ae5cb79.png` → OCR 解析出「Mode: XK-5000 / Rated Power: 1500W」。补充单测 3 条：拒图时落盘并发起工具调用、已被 OCR 的图在后续轮剥除、无沙盒作用域时不落盘保持诚实死胡同提示。**部署注意**：运行时配置 `default_ocr_engine` 在本环境被置为 `paddleocr_vl_1_6`（需 `PADDLEOCR_API_TOKEN`，未配置即报错），本地可用的 OCR 引擎是 `rapid_ocr`（代码默认、本地 onnx 无需 token）与 `deepseek_ocr`，请按部署环境在运行时配置里显式指定。**已解决**：本环境已在 `.env` 配置 `PADDLEOCR_API_TOKEN`（docker-compose 服务 `env_file` 注入，`sed -i` 改 `.env` 后单文件 bind mount 哈希失效、重启报 OCI mount 错误，`--force-recreate` 可恢复）。**关键：agent run 及工具调用在 `worker` 容器执行、`api` 只对外服务 HTTP/SSE，改 `.env` 后 api 与 worker 两个容器都必须 `--force-recreate`**——只重建 api 时流式问答仍报 `Error streaming messages: [paddleocr_vl_1_6] PADDLEOCR_API_TOKEN 未配置`。真机验证 `paddleocr_vl_1_6` 可用：`test/data/测试图片.png` 提交 → 2.30s 返回 5758 字符表格 markdown。
- 验收修复：对话输出格式可选（甲方反馈 P1「支持以表格/列表/步骤等格式输出」此前只有提示词里的格式建议、无用户可控开关）。对话输入栏新增「输出格式」下拉（默认/表格/步骤/列表，按会话记忆，镜像模型选择的按线程记忆模式），选择结果随 `AgentRunCreate.output_format`（`Literal` 枚举校验，default/table/steps/list）下发；`build_chat_input_message` 将格式指令仅拼入模型输入 `langchain_message`（`<output_format>` 块 + 中文约束文案，含「内容确实不适合该格式时先说明原因再按合适方式回答」兜底，避免强套格式），`content` 保持原始问题不污染 UI 展示与问答对匹配，`raw_message` 持久化保证 resume 后格式约束不丢；`default` 不追加指令，行为不变。补充单测（指令注入/content 干净/枚举校验/与行业方案指令叠加）。修复：发送 payload 中 `output_format` 误传 computed 引用（`currentOutputFormat` 未解包 `.value`），导致 `JSON.stringify` 报「Converting circular structure to JSON」、所有消息发送失败——改为 `currentOutputFormat.value`。
- 验收修复：检索图片并以图片输出（甲方反馈 P1「输出格式 支持检索图片，并以图片输出」此前未实现）。文档内图片（DOCX/PPTX/XLSX 经 Docling、PDF 经 MinerU）解析时已提取到 MinIO `public` 桶（对象路径 `{kb_id}/kb-images/...`）并以 `![](http://localhost:9000/public/...)` 形式写在 chunk content，随检索原样进入模型输入；现补三块：① 系统提示词新增「图片展示」约束（`IMAGE_RESPONSE_PROMPT`，插在业务回答与可视化规范之间、硬性护栏之前）：当检索结果含与本问题直接相关的图片时，在回答正文以 Markdown 图片形式紧贴相关文字展示，只允许原样使用检索出现的原始 URL、不编造不改写、无关/无法核验的不输出、无相关图片时不虚构。② 图片 URL 统一代理：前端 `minioUrl.js` 的 `rewriteMinioImageUrls` 把 `https?://<host>[:9000]/public/` 重写为站内相对路径 `/minio/public/`（在 `markdown_preview.js` 渲染前统一应用），nginx 增 `/minio/` 反代到 `minio:9000`，dev 下 vite.config 增 `/minio` 代理，避免直连 MinIO 跨机器/端口裂图。③ 回答正文图片可点击全屏放大：`MarkdownPreview` 新增 `img-click` 事件（点击 `<img>` 时发射 src/alt），`AgentMessageComponent` 监听并复用既有全屏预览 overlay。运维注意：`public` 桶匿名读策略（`_ensure_public_read_access`）在本环境缺失导致直读 403，已用 `ensure_bucket_exists('public')` 重新下发；该策略每次向 `public` 桶上传对象时都会幂等重设，但若 MinIO 配置卷被重置后需先触发一次上传或手动重设。补充单测（提示词含图片指令且位于护栏前、URL 重写 localhost/局域网 IP/无端口/外链不受影响）。
- 验收修复：全库搜索融合 + 文件夹/文件分割展示（甲方反馈：全库搜索出现无关内容，文件夹和文件没有分割）：`global_knowledge_search_service.py` 原用纯互惠排名融合（`1/(60+rank)`）完全忽略实际相似度，各库默认 `similarity_threshold=0.2` 偏低，低相关片段也能挤进全局前列——现改为分类型换算全局分：vector（COSINE）与 hybrid 的 `score` 是有界相似度/加权分，可直接跨库比较，低于全局下限 `VECTOR_RELEVANCE_FLOOR=0.35` 的片段视为无关内容直接过滤，命中者按实际相似度排序；keyword（BM25）的 `bm25_score` 无上界、跨库不可比，退回互惠排名融合（`1/(60+rank)`）保留库内相对次序；无分数可用的检索结果（自定义后端）同样退回 RRF 不误杀。结果富化：批量 `list_by_file_ids` + `build_document_display_paths`（复用文件夹祖先链、visited-set + 64 跳防环路）给每条结果补 `file_dir`（目录路径）/`file_name`，前端 `GlobalKnowledgeSearchModal` 把文件夹路径与文件名分两行展示（目录灰色小字在上、文件名加粗在下），原来只能显示「知识库片段」占位。补充单测：低分过滤 + 按分排序 + BM25 不被下限误杀、富化路径拆分与无记录/无 metadata 不注入空字段。
- 验收修复：联网补答 `ascii` 报错根因（甲方反馈：知识缺口-点击-联网补答-出现 `'ascii' codec can't encode characters in position 9-15: ordinal not in range(128)`）：`.env` 中 `TAVILY_API_KEY` 是占位注释（`# 获取搜索服务的 api key 请访问 ...`，含中文），非空检查能通过，但会作为 `Authorization: Bearer <注释>` 头触发 httpx 对 header 的 ascii 编码错误，把 500 误报成联网失败。现 `_build_client()` 在非空基础上校验 key 必须是单个纯 ASCII token：以 `#` 开头（疑似占位注释）/含非 ASCII 字符/含空白（`isspace()`）任一命中即抛清晰 `ValueError("TAVILY_API_KEY 配置无效（疑似占位或含非 ASCII/空白）...")`→422，引导在 `.env` 配置真实 Key；首尾空白因先 `strip()` 不会误伤。补充参数化单测（占位/中文/内部空白拒绝，合法 ASCII 放行）。
- 验收修复：知识缺口补充与问答反馈的「语义相同问题不能回答」（甲方反馈：同一问题换个说法就答不了）。此前 `try_create_curated_qa_run` 只做规范化后完全一致的精确命中，改述问题直接落回普通 Agent 流程。现新增语义召回：`curated_qa_semantic_matcher.py` 的 `CuratedQASemanticMatcher` 对某 agent 的启用问答对逐条用 `question_embedding`（懒回填到 `curated_qa_pairs.question_embedding` JSONB 列，`ensure_knowledge_schema` 幂等补列，默认 bge-m3 向量）与新问题 `abatch_encode` 后的向量比余弦相似度，阈值 `CURATED_QA_SEMANTIC_THRESHOLD=0.70`（因大模型还会做参考判断，比直接顶答案可放宽），top-1 过阈值即语义命中。语义命中**不直接顶出原答案**，而是由 `_compose_answer_from_reference` 让大模型以人工问答对为参考、按用户问题原意组织回答（参考与问题不相关时如实说明未检索到依据，不编造），`answer_source='curated_qa_semantic'`/`curated_qa_id` 落库并随 SSE 事件 meta 下发；精确命中仍直接输出原答案。语义匹配失败只降级走正常 Agent 流程，不阻断。补充单测：余弦基础与阈值、最佳命中/不足阈值返回 None、无问答对跳过、缺失向量懒回填落库、语义命中引导生成、精确命中跳过语义。
- 验收修复：行业方案入口改为纯开关模式（甲方反馈：行业方案弹窗强制行业+需求+2-5 个产品才能提交，太受限；且不要按表单填写）。现点击「行业方案」按钮记为**开启**（按钮高亮并提示），再点一次记为**关闭**，不弹窗、不注入模板、不填表单；开启后用户在输入框直接输入需求发送即可，行业/产品留空。发送时前端把输入框内容整体作为 `requirement` 组装 `industry_solution` 结构化请求（`industry:''`、`products:[]`），开关在发送后保持开启，直到再次点击关闭。后端 `IndustrySolutionRequest` 放宽**入口**校验：行业可选（默认空）、产品可留空（去重后不足 2 个不再报错），仅需求必填；工具执行层 `ProductResearchInput`/`ExportIndustrySolutionInput` 仍强制 ≥2 个不同产品，避免无产品依据检索。`industry-solution` 技能规则同步放宽：`products` 为空时从需求描述中识别用户明确提到的产品名填入检索（只取明确提到的，不臆造补全），识别不足 2 个先向用户询问。补充单测：空行业/空产品入口放行、空需求拒绝、超 5 个拒绝、工具层 <2 拒绝。
- 验收修复：文档搜索不再命中目录路径前缀 + 全库搜索页文件夹/文件分离 + 三种搜索方式（甲方反馈：文档搜索搜到前面的路径、没有看到文件夹/文件分离）。`knowledge_file_repository.search_documents` 原用 `filename LIKE %kw%` 匹配，而 `filename` 存的是完整相对路径（如 `MCX资料/MCX V1.1.1 word/xxx.docx`），搜「MCX资料」会把该目录下全部 29 个文件带出（实证 29 个命中全是纯路径前缀、叶子名不含关键词）。现重构为 `search_type` 三模式（默认 `filename`）：① **filename** 只匹配路径最后一段（`regexp_replace(filename, '^.*/', '')` 取叶子名）+ `original_filename`，祖先目录名不再参与匹配；② **folder** 按文件夹名匹配并返回文件夹：虚拟目录从文件路径全部祖先段派生（`string_to_array` + `generate_series` 横向展开，`a/b/c.pdf` 隐含目录 `a`、`a/b`，目录用其名即末段匹配，路径去重），真实文件夹（`is_folder`）按自身名匹配，不再带出目录下文件；③ **content** 才 `outerjoin knowledge_chunks` 匹配 chunk 正文（无关键词时三模式都不 join chunk，避免破坏 COUNT 语义）。路由 `search_documents_across_knowledge_bases` 新增 `search_type` 查询参数（`Literal` 枚举校验，默认 filename）透传。前端 `KnowledgeBrowserView`（全库搜索页）新增「文件名/文件夹/内容」三段式切换（`a-segmented`）与动态占位符，搜索结果与热门文档把路径拆成「父目录（灰色小字）→ 名称（加粗）」两行展示，文件夹结果保留文件夹图标与「文件夹」标签，虚拟文件夹结果点击用 `?path=` 目录路径直达（真实文件夹仍 `?folder_id=` 深链）；搜索「boss」在文件名模式下不再命中 POCSTARS PPT（叶子名不含），切到「内容」模式才命中正文「运营商BOSS」；文件夹模式搜「MNO」返回文件夹 `poc资料/MNO`（不再带出目录下 7 个文件）、搜「证书」返回 17 个文件夹（含 `poc资料-证书/终端认证证书` 等无直属文件的祖先目录）。补充单测：`search_documents` 三模式 SQL 结构参数化断言（content 才 join chunk、folder 才匹配 `is_folder`）、router 透传断言、`search_documents` 不排除文件夹断言保留。
- 验收修复：ASR 语音转写模型可配置（甲方反馈：SiliconFlow - 模型配置里没有配置 ASR 模型的选项）。根因：内置 `siliconflow-cn`/`siliconflow` 模板 `capabilities` 只有 `["chat","embedding","rerank"]`，而手动添加模型的类型下拉由 provider capabilities 限定，故「手动添加」里没有 transcription 类型可选。现两个 SiliconFlow 模板 `capabilities` 增加 `transcription`；`ensure_builtin_model_providers_in_db` 对**存量内置 provider 做能力合并**（模板新增能力补进已有记录，只加不减，不覆盖管理员编辑），已存在且已启用 5 个模型的 SiliconFlow 在启动同步后自动获得 transcription 能力，无需手工改能力。运行时 `provider_type` 默认 openai，`transcription_service` 放行，`_get_base_url_for_type` 对 transcription 落到 `base_url`（`POST {base_url}/audio/transcriptions`），链路完整。补充单测：存量内置 provider 合并模板能力。

- 前端页面切换性能优化（web）：① 修复 `AppLayout` 中 `<keep-alive v-if="route.meta.keepAlive !== false">` 反模式——在缓存页（对话/工作区）与非缓存页（Dashboard/扩展/知识库浏览器等）间切换时 `v-if` 翻转会销毁整个 keep-alive 缓存，导致下次切回重量级页面全量重挂；改为稳定的 `<keep-alive :include="KEEP_ALIVE_VIEWS">`（`['AgentView', 'WorkspaceView']`），并给这两个页面加 `defineOptions({ name })` 保证按组件名匹配，同时移除路由中已失效的 `meta.keepAlive` 配置。② 知识库详情页图谱区懒加载：`KnowledgeGraphSection`（内联 g6/sigma/graphology）改用 `defineAsyncComponent`，仅在打开「图谱」Tab 时才拉取渲染，避免进入知识库详情页同步解析 ~1.4MB 图谱 chunk 造成切换卡顿；加载期间显示「图谱加载中…」占位。生产构建验证 g6 已拆为独立懒加载 chunk（`KnowledgeGraphSection-*.js`）。

- 聊天输入框新增语音输入：浏览器通过 MediaRecorder 录制 WebM/Opus，后端复用模型供应商配置调用 OpenAI-compatible transcription 接口，转写文本插入当前光标位置并保留已有草稿，由用户确认后手动发送；支持独立 ASR 模型类型与配置、25 MiB 限量上传、权限/兼容性/Provider 错误提示，录音不持久化。

- 答案调优 + 知识缺口联网补答（管理员侧）：① 答案调优——点踩反馈卡片新增「调优答案」入口，自动带出用户原问题与 AI 原回答，管理员编辑保存为「人工确认问答对」（`curated_qa_pairs` 新表：agent_slug + 规范化问题哈希唯一，问题先做空白折叠/大小写归一，问答对含来源反馈/来源消息、启用开关、命中计数）；同一 Agent 收到**完全相同**问题（规范化后）时 `create_agent_run` 优先直接命中返回人工确认答案（`try_create_curated_qa_run` 同步建 run、落 assistant 消息、标记 completed 并推送 SSE init/loading/finished 事件，不经过模型与 arq worker；带图片/附件/评估来源的请求不参与命中）。② 知识缺口联网补答——知识缺口列表与处理抽屉新增「联网补答」：管理员显式触发 Tavily 联网搜索（`TAVILY_API_KEY`，未配置 422、失败 502），返回候选答案与参考来源（URL 校验、上限 5 条），候选**不自动进入普通问答**；管理员审核、编辑后保存为人工问答对，知识缺口自动标记「已解决」并记录参考来源。两条路径的保存接口与调优上下文读取均 superadmin 权限。

- 答案反馈升级为结构化原因 + 反馈汇总统计：点踩弹窗由自由文本改为「预设原因单选（答案有误/信息过时/答非所问/其他）+ 补充说明」，提交时以 `label\n detail` 存储（无补充说明则仅存 label），后端 `parse_feedback_reason` 解析该结构并兼容历史自由文本原因（无法匹配的归为「历史反馈」）；新增 `GET /api/dashboard/feedback-summary`（superadmin 权限，可选 `agent_id` 过滤）返回总反馈/点赞/点踩/满意度及点踩原因分布，数据总览新增「答案反馈统计」面板展示总反馈、点赞、点踩、满意度与原因分布条形图。

- 知识库详情的“检索测试”新增回答预览：复用当前知识库真实检索、可选重排与模型配置，展示最终回答、引用来源和可折叠检索详情，并保留纯检索模式；回答来源卡片批量加载当前文件的版本链，对原始对象仍存在的历史版本提供受保护下载入口。历史版本仅作来源补充，不参与检索、重排、模型上下文或回答。
- 新增部门下的「团队分组」作为知识库授权粒度（甲方要求：部门之下再分团队，权限可精确到团队）：每部门自动持有且仅持有 1 个「默认团队」（不可删，创建部门时随事务自动建，存量部门启动迁移时补齐），每用户归属唯一团队（`users.team_id`，FK `ON DELETE SET NULL`）；默认团队 = 未分配到具体团队用户的兜底桶——把用户移到具体团队即离开默认团队，删除团队时其成员自动重挂本部门默认团队并清理该团队遗留的知识库授权。团队仅作为权限对象：只经知识库权限配置页的显式授权进入 `knowledge_base_permissions`（`subject_type='team'`，部门内子集；授权给团队不覆盖部门其他人，要给全部门仍授权部门，部门授权天然向下包含团队），创建流程与共享设置（share_config）、Agent/Skill 授权一律不动，无继承逻辑。后端：`teams` 新表 + 迁移（`ensure_knowledge_schema` 追加建表/`users.team_id`/默认团队种子/存量用户回填，种子与回填补 `created_at`、存量空值启动补 NOW()）、`TeamRepository`（部门级 CRUD、默认团队唯一、删除重挂用户 + 清理授权）、`GET/POST /api/departments/{department_id}/teams` 与 `PUT/DELETE .../teams/{team_id}`（默认团队删除 400、跨部门 403/404、同名 400）、`GET /api/teams` 全量含部门名；`UserCreate/UserUpdate` 增加 `team_id`（创建未选落默认团队、传了校验属本部门，改部门未选自动重置到新部门默认团队）、响应带出 `team_id/team_name`；权限判定 `_grant_matches_user` 增加 team 分支，用户上下文（`_user_permission_context`、`get_databases_by_user`）补齐 `team_id`。前端：权限面板授权类型新增「团队」，对象下拉按「部门 › 团队名」分组级联，行标签同格式渲染、编辑/删除/来源标记与既有授权一致；部门管理表格行内展开团队管理（列表 + 新建/重命名/删除，默认团队标「默认」且不可删）；用户管理创建/编辑按所选部门级联选团队、未选提示落到默认团队，用户卡片部门名后带出团队名；`department_api` 新增团队 CRUD 与 `GET /api/teams`。已端到端验证：建部门自动带默认团队、默认团队删除 400、团队授权后仅该团队成员可见（非成员同部门不可见，若 KB 无其他授权）、改团队即时生效、删除团队成员回默认团队且授权行清理。范围边界：团队不做 share_config 维度、不进 Agent/Skill、不做数据隔离/工作台过滤、不做多对多、不做历史授权迁移。
- 团队功能增补：部门管理团队行新增「管理成员」勾选弹窗（可增可减，需求方确认采用成员编辑模式）：弹窗列出本部门用户（多选、当前成员预选、标签带当前团队名），保存按勾选全集调 `PUT /api/departments/{department_id}/teams/{team_id}/members` 重设成员关系——勾选的进该团队、取消勾选的原成员迁回本部门默认团队；默认团队作为兜底桶，「取消勾选」不改变归属（弹窗有对应提示文案）。后端：`TeamRepository.set_members`（校验 user_ids 全部属于该部门否则 400，空列表 = 清空团队），`auth_router.read_users` 增加可选 `department_id` 查询参数供 superadmin 按部门拉用户（admin 仍强制本部门）。新增对应 repository 单测；端到端冒烟验证勾选加入/清空回默认/跨部门 400/不存在团队 404。
- 知识库权限设置与共享设置展示统一 + 兜底权限收窄修复（甲方反馈：权限设置面板与「共享设置」两处访问范围对不上）：后端 `KnowledgePermissionService.effective_permissions` 原先只要显式授权任一 flag 命中就直接返回合并结果、完全忽略 `share_config` 兜底，导致被「共享设置」勾选的对象反而因窄授权丢失兜底本有的查看/问答/下载权限（如只授查看却没了兜底的下载）。现改为显式授权与兜底逐 action 取宽集（OR），纯授权/纯兜底用户行为不变，仅「两处都命中」者权限变宽或持平；`get_database_info` 响应补充 `created_by`（列表端点本就暴露，view 端点已要求 can_view，无新增泄露）。前端权限设置面板改为单张统一表格展示「有效访问全集」并标注来源：创建者（gold「创建者」tag、全权限只读）、共享设置兜底（blue「共享设置」tag、仅查看/问答/下载，`global` 渲染为「全体用户」伪行）、显式授权（green「授权」tag、可编辑删除），同一对象多来源命中合并为单行并取 flag 并集（删除只删授权，兜底保留）；面板顶部静态提示平台管理员/超级管理员默认全权限。实现收敛到 `buildAccessRows` 纯函数（去重/来源标注/排序，空 `share_config` 不渲染虚假「全体用户」行）并复用 `formatSubjectLabel`，新增对应 helper 单测；service 单测把「窄授权禁用兜底下载」改写为「窄授权不再收窄兜底」，并新增 department 兜底用例。「共享设置」编辑入口（`ShareConfigForm`）保留原位不变。**后续微调**：权限面板部门行的标签去掉内部 ID（如「部门1（18）」→「部门1」）——部门名唯一、括号内数字易被误读为成员数；用户/角色行保留「名称（id）」格式不变。
- 全库搜索纳入文件夹结果与直达导航（甲方反馈：只能搜知识库、文件，不能搜文件夹）：`search_documents` 去掉 `KnowledgeFile.is_folder.is_(False)` 硬过滤，SELECT 与 GROUP BY 对称加入 `is_folder`（Postgres 非聚合列必须入 GROUP BY），文件夹按 `filename` LIKE 命中（chunk 外连接不命中文件夹，正文分支自然不匹配）。点击文件夹结果需完整面包屑直达真实目录（`?path=` 仅支持路径型虚拟目录），新增 `GET /api/knowledge/databases/{kb_id}/folders/{folder_id}/chain`（`can_view` 权限 + `_ensure_database_supports_documents`，沿 `parent_id` 上溯返回 top-down 祖先链，复用 `build_document_display_paths` 的 visited-set + 64 跳防环路模式，目标不存在 404）。前端 `documentApi.getFolderChain` + `stores/database.js` `navigateToFolderById`（完整面包屑 → `parentId` 加载子文件列表，失败回退根目录）；`FileTable.vue` kbId watcher 优先消费 `?folder_id=`（从搜索页进入必 fresh-mount，行为与既有 `?path=` 一致）；`KnowledgeBrowserView.vue` 文件夹结果以 lucide `Folder` 图标 +「文件夹」标签渲染、描述显示「知识库 · 文件夹」，点击 `browseDirectory` 按 `is_folder` 分支走 `?folder_id=` 深链。范围：全库搜索页；热门文档（`list_hot_documents`）仍仅文件、语义搜索面向内容块不纳入、知识库内文件浏览器按名搜索本次不加。补充 `search_documents` SQL 结构断言（WHERE 无 `is_folder` 排除 + SELECT 含 `is_folder`）、`get_folder_chain` 单测（top-down 顺序/目标缺失返回 None/环路守卫）与 chain 端点 router 测试（can_view 权限、404、参数透传）。
- 知识库内「移动文件到其它文件夹」：甲方反馈知识库内无法调整文件目录归属，需把文件从一个文件夹移到另一个文件夹。后端 `PUT /api/knowledge/databases/{kb_id}/documents/{doc_id}/move`（body `{new_parent_id}`，空值=根目录）此前已具备完整能力（含文件夹环路检测、父必须是文件夹否则 400），本次零后端改动。前端补齐整条链路：`documentApi.moveFile` API 封装；`stores/database.js` 新增 `moveFile` 动作（`state.lock` 包裹 → 移动 → 后台刷新文件列表 → 失败 `message.error`）；`FileTable.vue` 文件行操作菜单新增「移动到」（`canManage` 权限可见，图标 lucide `FolderInput`），弹出目标文件夹选择对话框——「根目录」选项（target=null 即移回根层）+ 懒加载 `a-tree`（展开时按 `parent_id` 复用 `list_documents` 拉取子文件夹，仅保留 `is_folder` 节点，避免大库预取全量树），确认后调用移动接口并刷新列表。范围：仅文件；文件夹移动、批量/拖拽移动未纳入（后端虽支持文件夹移动，前端本次不暴露）。**修复移动文件三处联调问题**：① 移动报 500 服务器内部错误——`move_file` 走 read→persist 往返，`_file_record_to_meta` 把 DB 时间戳序列化为 ISO 字符串，`_file_meta_to_record_data` 原样写回 TIMESTAMPTZ 列触发 asyncpg `DataError`（`activated_at` 等 4 个时间戳列全中招）；现 `_file_meta_to_record_data` 用 `coerce_any_to_utc_datetime` 把 4 个时间戳字段回转成 datetime 对象。② 移动报 400 `File __virtual_folder__:root:xxx/ not found`——移动目标树里出现了虚拟文件夹节点（`__virtual_folder__:` 路径前缀聚合、无真实记录），选中/展开它会被后端 `_load_file_meta` 判定为不存在的目标；现 `loadMoveFolderChildren` 只保留 `is_folder && !is_virtual_folder` 的真实文件夹。③ 移动弹窗目录树「点不开文件夹」——展开虚拟文件夹把 `parent_id` 发成 `__virtual_folder__:root:xxx/` 导致 400，与 ② 同根因，同一前端过滤一并修复。已端到端验证：移入真实文件夹（`parent_id` 落库）与移回根目录均成功、无 DataError。**虚拟目录移动支持**：实测发现多数知识库的文件以「完整路径当文件名」存在根目录（如 `poc资料/MNO/xxx.pptx`），浏览看到的多级目录全是 `_list_directory_documents` 按路径前缀聚合的**虚拟文件夹**（无 DB 记录），真实文件夹寥寥无几——仅支持真实文件夹的移动对这类库形同虚设。现移动目标树改为与主浏览器同一套导航语义（虚拟节点用 `path_prefix`、真实节点用 `parent_id` 懒加载），两者皆可作为移动目标；后端 `move_file` 识别 `__virtual_folder__:{parent_ref}:{prefix}/` 目标后**改写 filename 路径前缀**（`filename = prefix + basename`，`parent_id` 按内嵌父上下文置空或落真实文件夹，`normalized_name` 同步重算），移入真实文件夹/根目录则统一把文件名收敛为 basename 再写 `parent_id`（真实文件夹内文件以相对名存储，保留路径前缀会污染展示并派生多余虚拟子目录）；文件夹禁止移入虚拟目录（报 ValueError→400）。已端到端验证：`file_fb4fa6` 移入 `poc资料/MNO/`（filename 改写落库）、移回根目录、移入真实文件夹 `销售话术` 三条路径全部成功，移出恢复原状。补充 `MoveSpy` 单测 5 条（虚拟目录改写、虚拟路径移回根、虚拟路径移入真实文件夹剥前缀、真实文件夹下虚拟子目录落 parent、文件夹/非法 id 拒绝）。**修复移动弹窗二次打开失效**：`a-modal` 默认不销毁内容，`a-tree` 的展开/懒加载状态保存在 DOM 内部，第二次打开移动弹窗会残留上次展开的虚拟目录——树记住它已展开、但新树数据无 children 且不再触发 `load-data`，表现为目录"点不开/打不开"；现移动弹窗加 `destroy-on-close` 强制每次全新挂载，展开状态每次清零，同时保证根目录选项与树每次干净渲染。
- 删除知识库文案对齐：删除操作（按钮「删除数据库」、确认弹窗标题/正文）此前沿用内部「数据库」措辞，与页面其它「知识库」文案不一致，且删除即删除整个知识库（连带文件）。现将 `DataBaseInfoView.vue` 删除按钮、`stores/database.js` 删除确认弹窗及「数据库名称不能为空」「获取数据库信息失败」两个用户可见提示统一改为「知识库」措辞。纯前端文案改动，无行为变更。
- 创建知识库的类型选择器隐藏：当前可创建的知识库类型仅「AI知识库」（milvus）一种，dify/notion 为历史只读连接器且已在新建入口过滤，单选无意义。现 `DataBaseView.vue` 在可创建类型仅一种时隐藏整个「知识库类型」区块（`showKbTypeSelector = kbTypes.length > 1`），提交仍默认携带 `kb_type='milvus'`；后续新增可创建类型后选择器自动恢复出现。无后端改动。
- 共享设置取消「默认勾选/必选本人部门」：此前进入「部门共享」模式会自动把编辑者本人部门加入可访问部门列表，并在列表中标记「必选」且不可取消勾选，意图是防止编辑者把共享范围设置后自己失去访问权；但后端访问判定已独立保证编辑者永不锁死——`KnowledgePermissionService` 按角色（admin/superadmin）、创建者（`created_by`）、显式授权（user/department/role grant）三层判定，`share_config` 只是三者都不满足时的兜底，而能编辑共享设置的人必然属于前三类之一，因此该「必选」实为保护一个后端已不可能发生的场景，反而带来误导（「N 个部门可访问」把本人部门计入、无法取消、分享者本部门成员被动获得访问权）。现 `ShareConfigForm` 移除强制包含本人部门的逻辑：不再默认勾选、本人部门变为普通可勾选项、去掉「必选」标识、`validate()` 不再要求共享范围包含本人部门；`autoSelectUserDept` prop 及 5 处调用点（知识库详情/新建、Agent 编辑、Skill 详情/卡片）一并清理。安全性由「admin/创建者/有授权者天然可访问」后端保证，无需后端改动。注意：「指定人」模式仍保留「强制包含当前用户」原逻辑。**本轮补充后端残留清理**：共享 util `normalize_share_config` 原会在 `access_level='department'` 时无条件把操作者本人部门追加进 `department_ids`（KB 创建/更新、Agent、Skill 的分享配置均经此函数归一化并一路透传 `department_id`），导致创建知识库时未选任何部门、权限面板仍出现「部门 默认部门（1）」行（来源：共享设置）。现移除后端注入及 `department_id` 参数透传链路（KB manager/router、Agent repository、Skill service 共六处调用点），并顺带把原先被注入掩盖的「部门共享至少需要选择一个部门」校验变为真正生效；前端 `ShareConfigForm.validate()` 在部门模式未选部门时提示「请至少选择一个部门」并阻止提交（覆盖创建与编辑两处入口）。存量库中已注入的部门不在后端清理范围内，如需调整在编辑弹窗的共享设置里手动修改即可。
- 新增甲方客服知识库对比测试链路（内部，不交付甲方）：甲方新提供《【客服】POCSTARS知识库.xlsx》（15 表、约 410 有效问答对，问题→解决方法为标准答案）。新增 `scripts/eval_datasets/build_kefu_testset.py`（按表分层抽样 N 题写 `kefu{N}.jsonl`，过滤非问答表/线下操作/文件名资产/图片流程类，域写入 section）与 `scripts/eval_datasets/gen_kefu_report.py`（对比报告：主口径 = 答案正确性，三类分类「实质作答 / 知识库缺口拒答 / 需澄清未答」分开呈现，含全口径按 0 计）。首轮 `kefu20` 抽样 20 题（运营平台 3 / 终端-安卓 3 / 调度台 3 / MDM 3 / 终端-cat1 3 / miniserver 3 / 培训知识库 2）经 `run_agent_e2e.py` 真实 Agent 端到端作答，评分链路 `score_agent_results.py` 新增 `--metrics` 指标选择（默认仅 `answer_correctness`）。
- **业务决策：忠实度（Faithfulness）、贴合度（Answer Relevancy）不再计算**（`score_agent_results.py` 默认 `--metrics answer_correctness`，仅显式指定才计算；`gen_kefu_report.py` 报告只出答案正确性）。移除此前为忠实度判定加的检索上下文截断开关 `--faithfulness-context-cap` 及其 `_bound_contexts` 辅助函数与单测。95 题历史报告（`reports/accuracy_report_20260818_95.*` 等）已生成，保持原样不改动。
- 客服测试改为「单一口径」并限定 6 个业务 sheet：业务方指令——测试只针对 运营平台/调度台/终端-安卓/终端-cat1/MDM/miniserver（去掉培训知识库），生成的问题统一加 sheet 前缀（如「调度台-什么是下发消息」）携带模块上下文去问。`build_kefu_testset.py` 相应调整（SHEET_COLS 去掉培训知识库、query 加 `{sheet}-` 前缀），`gen_kefu_report.py` 报告补充测试条件说明。新增 `scripts/eval_datasets/score_kefu_facts.py`（单一口径评分 + 报告一体，替代 RAGAS 双口径）：**答案正确性 = 硬门槛 × (0.8×关键事实命中率 + 0.2×补充事实命中率)**——硬门槛 0/1（实质作答且切题为 1，缺口拒答/反问澄清/答非所问为 0），关键/补充事实由 judge LLM（deepseek-v4-flash）对照甲方标准答案拆解并逐条判命中（语义等价），补充事实为空时按该子项满分计（字面公式，业务方确认）。首轮带前缀 `kefu20`（6 sheet 各 3~4 题）真实 Agent 端到端：需澄清 3→0（前缀消除多义反问），实质作答 12 / 缺口拒答 8（均为知识库缺口：甲方客服库有答案、系统库没有），单口径答案正确性 **32.6%**（含拒答按 0 计），报告 `reports/kefu_facts_kefu20_prefixed_20260819.{md,json}`。
- 来源面板纳入 `find_kb_document`/`search_file` 的定位结果：此前来源提取只认 `query_kb`/`query_kbs`，对话若 `query_kbs` 召回为空但模型改用 `find_kb_document` 在文件内定位（或 `search_file` 搜文件）作答，来源面板即为空。现 `extractKnowledgeChunksFromConversation` 同时解析两个工具——`search_file` 命中的每个文件生成一张来源卡片（content 用完整路径文件名，便于面板内区分同名文件）；`find_kb_document` 每个命中窗口按行区间生成来源块，文件名由同一轮对话内 `search_file` 结果构建的 `file_id → filename` 映射解析，缺失时回退 `file_id` 不吞掉已定位内容；`hasKnowledgeRetrieval` 同步把这两个工具计入，纯定位会话也保留来源按钮。实测对话 a297b81d（`query_kbs` 空召回 + `find_kb_document` 命中 2 文件）：来源面板由空恢复为 2 个分组（Miniserver M200规格书20251125.xlsx / Miniserver M200 Datesheet 2025.xlsx），定位卡片与窗口块同组展示且通过回答引用过滤。补充 `messageProcessor.spec.js` 回归用例（search_file 文件卡片、find_kb_document 窗口来源名解析、无 search_file 时回退 file_id、纯定位会话按钮保留）。
- 修复来源面板同一文档跨库重复展示：同一份培训材料同时存在于多个知识库（如「营销包」与「定位资料」），`query_kbs` 并行检索后按文档名被 `filterKnowledgeChunksByAnswer` 各自命中，来源面板按完整 source 路径分组把 3 个文档拆成 6 张卡片（3 张重复），而「来源 N」计数的是 chunk 数（10）与卡片数（6）不一致。现 `MessageProcessor.groupKnowledgeChunksByDocument` 作为文档身份/分组的唯一口径——source 路径去掉前缀取文件名，同名文档跨库合并为一组、全部 chunk 保留；`KbResultGroupedList` 分组、`RefsComponent` 的「来源 N」、`KnowledgeSourceSection` 面板标题三处统一用该口径，数字与卡片数一致（3 个文档，chunk 数在各卡片「N chunks」标注）。实测验收对话「定位产品的销售话术」由 6 卡片/3 重复恢复为 3 卡片/10 chunk，与真实文档数一致。补充 `messageProcessor.spec.js` 跨库同名去重回归用例。
- 新增检索速度 + 首 token 两个一键可复现验收脚本（`backend/scripts/bench/`，挂载容器 `/app/scripts/bench/`，`docker exec` 单条命令即可跑），兑现验收报告「可随时复跑复现」承诺，产物 Markdown 报告 + jsonl 原始数据到 `reports/`（`{prefix}_{YYYYMMDD_HHMMSS}.md/.jsonl` + `_latest.md` 稳定指针）。① `retrieval_acceptance.py`：自动发现知识库并按 `query_params.options` 分类（vector_only/graph，不再硬编码 kb_id）；复制原链路打点但改**原始引用快照**模式（模块级存 7 个原始方法引用，`_patch` 始终基于快照打桩，可对每库/每场景安全重复调用，避免原脚本重复 patch 嵌套双计），STAGES=embedding/milvus_search/graph_vector/graph_neo4j_ppr/postgres/hydrate/rerank/other；三场景=单库全链路（每库预热 1 轮后轮换业务查询）/最大图库固定口径（p50+阶段 ASCII 条形图）/全库并发（标注特殊场景不纳入 <500ms 验收）；判定 vector_only 与 graph avg<500ms。② `first_token_acceptance.py`：系统侧口径，monkeypatch `BaseAgent._stream_input_with_state` 计时（first_model/first_tool），驱动生产 worker 路径 `stream_agent_chat`，自动发现默认 agent slug 与 superadmin/admin uid；首轮标 cold、其余 warm，统计含冷启动 vs 暖态两行，暖态 p50<1000ms 判定达标（对齐「首 token ≈ 0.5s」）。20260817 实测：检索 6 交付库全达标（本地嵌入档位，vector-only avg 39–94ms、graph avg 142–253ms、最大图库场景 B p50 200ms）；首 token 暖态 p50 379ms 达标（20260817 复测，`deepseek-v4-flash`，修复预暖阻塞事件循环回归后回到 20260814 报告 471ms 同一档位，详见下条「调用 skill 提速」修复说明）。
- 首 token 验收脚本内存优化：脚本每轮新建 thread 会触发 `stream_agent_chat` 预冷沙箱，10 轮瞬时拉起 10 个沙箱容器（每个约 600MiB，实测可直接打满 11GiB 内存 + swap 占满导致卡死）。现脚本 meta 固定 `file_thread_id`/`skills_thread_id` 为 `bench-sandbox`，`chat_service` 将该作用域透传进 `langgraph_config.configurable`（缺省退回 thread_id、行为不变），预冷与运行时命中同一沙箱缓存键 `uid::file_thread_id::skills_thread_id`，10 轮只起 1 个容器并热复用；每轮 thread_id 仍独立不串上下文。实测 3 轮仅新建 1 个 bench 沙箱（`file-thread-id=bench-sandbox`）。
- 调用 skill 变慢的根因修复：实测技能激活首个 `read_file SKILL.md` 要阻塞约 4.1s，来自沙箱容器冷启动（docker 容器启动 + `/v1/sandbox` 健康探测），由空闲回收器造成重复冷启动——回收器原生效值为 210s（配置 120s 因 ≤ exec timeout 180s 被自动抬到 210s），对话间隔稍长（阅读/思考/再次提问）容器即被回收，下次提问重新冷启动。双管齐下：① 闲置回收时长 `SANDBOX_IDLE_TIMEOUT_SECONDS` 120→300（5 分钟，20260818 因内存压力由 1800 收敛：闲置沙箱尽快回收释放内存，接受对话间隔超 5 分钟重新冷启动 ~4s），`.env` 与 docker-compose 默认值同步调整，provisioner 健康检查确认生效（当前生效值 300，300 > exec timeout 180 不触发自动钳制）；② 运行开始时后台预冷——`stream_agent_chat`/`stream_agent_resume` 在 run 启动即 `_schedule_sandbox_prewarm`（`provider.get(create_if_missing=True)`，作用域按 `meta.file_thread_id`/`skills_thread_id` 与子智能体独立），把 ~4s 冷启动藏到首轮 LLM 生成之后；热路径（缓存命中）亚毫秒，预冷失败只记 warning 不影响本次运行。补充 `test_chat_service_sandbox_prewarm.py` 3 个单测（作用域默认/子智能体独立/失败吞掉不传播）。**修复预暖阻塞事件循环的回归**：`_prewarm_sandbox` 在 asyncio task 里同步调用 `provider.get()`（内部走同步 `httpx.request` 等 sandbox-provisioner 建容器 + 健康探测，冷建约 4s，此前实测甚至直接 ReadTimeout），同步阻塞跑在事件循环上会冻结整个 loop，连带推迟首条模型 token——8/15 引入后 fresh thread 首条消息的系统首 token 由 ~0.5s 劣化到 ~4.6s（同会话追问因沙箱已缓存、`_connections` 命中不触发冷建，不受影响），与「藏到首轮 LLM 生成之后」的设计初衷相悖。现改为 `await asyncio.to_thread(provider.get, ...)` 把阻塞调用丢到线程池，事件循环不再被冻结；实测 `deepseek-v4-flash` 首条消息暖态 p50 由 4623ms 回到 **379ms**，预暖单测 3 个保持通过。
- 修复来源面板随回答折叠的问题：`filterKnowledgeChunksByAnswer` 此前只解析正文《》书名号与表格「来源文档」列首格，忽略回答末尾「来源说明/依据来源」段落——该段落是模型列出的实际使用文档清单（如“定位资料（POCSTARS 定位产品解决方案介绍、C10单页）”），被忽略后只要正文《》恰好命中 1 个文件，`matched.length>0` 便不触发全量回退，来源面板从全量检索块折叠到该 1 个文件。现 `extractCitationNames` 解析有界「来源说明」段落（起点为 来源说明/依据来源 等标记，终点为首个句号，跳过“以上内容依据…整理——”引导语，按顿号/逗号/括号/换行切分文档名候选）；匹配增加子串包含规则——模型改写文件名最常见形态是“逻辑名＝文件名±前后缀”（如「C10单页」⊆「C10单页-中文-1」「定位产品核心卖点与功能介绍」⊆「POCSTARS 定位产品核心卖点与功能介绍」），较短的串不足 4 字符不做包含匹配避免过宽。实测验收对话来源由 3 块/1 文件恢复为 13 块/5 文件，全部与「来源说明」逐一对应；匹配不到任何被引用文件时仍回退全量不误删。补充 `messageProcessor.spec.js` 回归用例（来源说明解析、句号后补充说明不入引用、子串匹配、无引用/无命中回退全量）。**后续补充**：模型还常以「──来源：」+ 完整路径列表引用文档（如 `poc资料/MNO/xxx.pptx（kb_id）` 换行排列），此前 `extractCitationNames` 的来源段落标记只认「来源说明/依据来源」等，裸「来源：」不识别 → 引用清单整段未被解析 → 来源面板回退全量（实测回答引 3 个文档却显示 22 个）。现标记正则增加 `来源[：:]`（精确匹配「来源：/──来源：」，不误伤散文「信息来源」）并取**最后一次出现**（引用清单通常在回答末尾）；完整路径经既有归一化（剥括号/标点/扩展名/版本尾）后子串包含规则可命中（路径包含文件名）。新增 `──来源：` 完整路径列表回归用例（3 引用命中、无关文件剔除）。**后续补充**：实测真实模型输出还常以 markdown 加粗的 `**来源**：` 引出清单，`来源[：:]` 要求「来源」与冒号紧邻而 `**` 恰好插在中间导致识别失败，来源面板再次回退全量（实测同一问答引 3 文档仍显示 22 个；用户复制的是渲染后文本，加粗被剥掉故看到的是 `来源：`）。现正则改为 `来源\s*[*_]{0,2}\s*[：:]`，容忍加粗/斜体标记分隔，同时兼容 `- ` 无序列表前缀；新增 `**来源**：` + `- ` 列表回归用例，并用实测对话 352 的真实 `content`（`**来源**：` 结尾）验证提取到 3 个路径、过滤出 3 个文档。
- 首 token 感知优化：问答加载态改为按模型真实阶段展示，思考与工具调用过程对用户可见。`reasoning_content` 数据链路此前已透传（`chat_service.py` 把 `reasoning_content`/`additional_reasoning_content` 塞进 `message_delta`，前端 `messageProcessor.js` 已解析），但思考面板默认折叠、header 的 `status=='reasoning'` 是死路径从未触发，且纯思考阶段加载文案一直显示"正在生成回复"。现：① `useAgentStreamHandler.js` loading 分支——思考增量（reasoning 非空、正文为空）绕过平滑层直接落地实时滚动，正文仍走平滑层保持原节奏；纯思考阶段置 `generationPhase='thinking'` 并保持 loading 显示"正在思考…"。② `AgentChatComponent.vue` `replyLoadingText` 新增 thinking 阶段，i18n 增加 `chat.reply.thinking`。③ `AgentMessageComponent.vue` 用 `isReasoning` computed（`isProcessing && 有 reasoning && 无 content`）替换死路径：思考中面板自动展开显示"正在思考…"，正文出现后自动收起（可手动展开查看完整推理链）。后端事件到达延迟同步收紧：`agent_run_service.py` `SSE_POLL_INTERVAL_SECONDS` 默认 1.0s→0.1s（事件从 Redis 到前端延迟由最高 1s 降至 ~0.1s，实测首次到达 ~28ms），`run_worker.py` `LOADING_FLUSH_INTERVAL_MS` 100→50ms、`LOADING_FLUSH_MAX_CHARS` 512→256。工具调用阶段展示（active 展开、参数/结果/耗时、按工具名切换文案）此前已具备，未改动。明确边界：正文流式节奏不动、reasoning 不落库（历史刷新不显示思考）、Anthropic thinking 不透传仍保持现状。
- 新增 100 题端到端问答验收脚本 `backend/scripts/eval_datasets/e2e_answer_100.py`（不交付甲方，仅开发者自用）：读取 `poc/mcx/loc.jsonl` 验收题集（100 题、九类），逐题驱动生产问答链路 `stream_agent_chat`（default-chatbot）完整走 ReAct 深度查证 + 知识库工具，从 `message_delta` 累积整段回答，记录 query/answer/工具序列/耗时/状态到 jsonl 并在末尾生成 Markdown 汇总。特性：断点续跑（跳过已完成 index）、失败重试（仅 `finished` 且非空回答视为成功）、多义词澄清自动处理——知识库对部分题目存在多义（如“关闭 SOS 报警”多产品含义不同）会触发 `ask_user_question` 澄清 interrupt，批量无人应答，脚本用 canned 引导回复经 `stream_agent_resume` 恢复同一线程继续作答，对齐 gold_answer“按最常见含义作答”风格。20260814 实测 100/100 全部 `finished`、无空回答：p50 约 57s/题（含澄清轮）、12 题触发过澄清、0 次重试；与 gold_answer 对照一致的“知识库未记载”诚实拒答（如调度台闪退/终端无法开机等故障排查题）按原样记录，不编造。产物 `backend/scripts/eval_datasets/reports/e2e_answers.{jsonl,md}`。
- 图谱检索降载（三处）：① 3 个图库 `knowledge_bases.query_params` 的 `graph_top_k` 20→10（减少注入答案生成的图谱片段量）；② 图谱 embedding 走查询级缓存——`MilvusGraphVectorStore._embed_query_text`（实例生命周期=单次查询，检索路径每次查询新建 store，缓存随查询自动回收），实体/三元组/审核断言三路子检索并发共享 1 次外部 embedding；③ 复用主检索 query embedding——`aquery` 的 vector/hybrid 分支已算好的 query embedding 通过 `MilvusGraphVectorStore.seed_query_embedding` 注入图谱缓存（`batch_encode` 与 `abatch_encode` 同源同向量），三路子检索直接命中已完成 Future，图谱路径不再调用外部 embedding API。实测图谱外部 embedding 调用由 2~3 次/查询降至 **1 次/查询**（即主检索那 1 次），最大图库 kb_3cm2gz6tyb 单库 10 轮 p50 由 **778ms → ~517ms**（avg 831→588ms，`graph_vector` 阶段由 ~648ms → ~34ms，纯本地向量检索）。补充单测：并发多路共享一次 embedding、不同 query/模型不共用、主检索注入后零外部调用。诚实边界：剩余墙为一次外部 query embedding（~300ms）与 Neo4j PPR（warm 65~190ms，波动大），p50 517ms 距 <500ms 验收仍差一口气，需接本地 GPU 推理或再压 embedding 环节。
- 新增多产品行业解决方案与 Word 交付：Chat 输入区提供轻量结构化入口，校验行业、需求及 2–5 个去重产品；内置 `industry-solution` Skill 对每个产品分别复用现有多知识库检索链路，保持 product → evidence → source 关联后再综合方案。最终方案正文和同一编号来源列表复用现有 `office_writer` 生成 DOCX，并通过线程 `outputs`、`present_artifacts` 与受认证 artifact 下载链路展示，普通对话不出现 Word 交付物；来源面板保留知识库/文件 identity 并继续走受保护下载接口，References 文件名不再被 Markdown 错误识别为外链；DOCX 支持 inline bold/emphasis、列表语义，并对本功能输出统一设置 Microsoft YaHei 中英文字体与标题层级。
- 文件行的“信息增强”与“QA 知识对”对普通上传的已入库文档不再不可用：此前两个功能的资格校验都强制要求 `confirmed_at`（仅“清洗→确认入库”流程会写入），普通上传文档（实测影响 269 个 active indexed 未确认文件）点按钮即报“尚未确认并完成入库”/“不是当前已确认且可检索的正式版本”；现 `document_enrichment_service._assert_eligible` 与 `document_qa_service._assert_eligible` 去掉 `confirmed_at` 要求，保留 `is_active`/`status`/`markdown_file` 检查，普通已入库文档即可生成摘要/关键词/标签与 QA 知识对；QA 列表的 `confirmable` 同步去掉 `confirmed_at`，普通文件的 QA 草稿可确认并投影到 Milvus 参与检索，不再只能停留在草稿。同时修复生成模型未配置时静默跳过的问题：`document_enrichment_model`/`document_qa_model` 运行期默认均为 `None` 且前端无配置入口，即使资格放开生成也会因“模型未配置”而 skipped；现在配置为空时回落系统全局默认对话模型 `config.default_model`（与 agent 的 `resolve_chat_model_spec` 兜底一致），保证点按钮能真正生成。补充 confirmed_at 为空时可生成的复现单测，并同步更新资格拒绝用例（改为 status 不可生成/无 markdown）。修复信息增强“生成成功但内容不落库”：`KnowledgeFileRepository._writable_fields` 漏收 7 个 enrichment 列（`enrichment_status`/`enrichment_version`/`enrichment_data`/`enrichment_content_hash`/`enrichment_generated_at`/`enrichment_error`/`enrichment_possibly_outdated`），`_sanitize_data` 据此把 `update_enrichment_fields_with_version` 的全部写入静默丢弃——任务报 success、version 靠 SQL 表达式正常递增，但状态/摘要/关键词/标签永远为空，前端“完全不显示”；现补上白名单，信息增强生成与人工编辑（update_summary/update_keywords/update_tags）均真正落库。新增 `test_knowledge_file_repository.py` 锁定白名单，防止再次漏加。
- 知识库来源面板只展示 AI 回答实际引用的文档：`query_kbs` 一次并行检索多个库会带出大量与最终回答无关的候选文件（实测一条问答返回 34 条、横跨 15 个文件），此前面板把全部检索内容都列出来；现依据回答正文的「依据来源」区段（《》包裹名 + 表格「来源文档」列首格）提取引用文档名，用「去括号注释/去扩展名/去尾部版本日期」归一化后与每个检索块文件名做 Levenshtein 编辑距离匹配（相似度 ≥0.8 保留，可容忍模型对文件名的轻微改写如 Datasheet/Datesheet），过滤后按引用在正文出现顺序 + 相关度排序，使面板与 AI 回答的来源一一对应。提取不到引用名或匹配不到文件时回退为全量展示，避免误删。实现于 `web/src/utils/messageProcessor.js` 的 `filterKnowledgeChunksByAnswer`，在 `AgentChatComponent.getConversationSources` 应用。
- 知识库未显式指定模型时跟随「设置-基本设置」的全局默认模型：新增 `resolve_embedding_model` / `resolve_reranker_model`（`yuxi/config/app.py`），在 `KnowledgeBase.load_metadata` / `_load_metadata` 归一化 `embedding_model_spec`，在 Milvus 检索时归一化 `reranker_model`，使 7 处向量检索读取点与图谱构建、冲突发布全链路在知识库 spec 为空时回退到全局 `embed_model` / `reranker` 配置，不再报「Embedding model spec not found」。因此知识库详情页不必再为每个库单独指定向量/重排模型，改在设置-基本设置统一配置（默认 siliconflow，可切到 local 本地推理）。原数据库中显式写入的 spec 会被清空以跟随全局默认。**全局 `embed_model` 切换热生效**：`load_metadata` / `_load_metadata` 改为存原始 spec（空值即跟随全局），7 处向量嵌入读取点与 `get_database_info` 展示改为使用点实时 `resolve_embedding_model`，设置页修改全局默认后无需重启即影响新嵌入与展示（`reranker` 检索时归一化本就每次读全局配置，无缓存问题）。注意：热生效只作用于新嵌入操作，存量集合中的向量由旧模型构建，如需换模型须重建/重新入库。
- 新增整栈停机冷备份脚本 `docker/backup/backup-all.sh`：按需手动执行（需要时 `bash docker/backup/backup-all.sh`），`docker compose stop` 停机后用 postgres:16 容器以 root 只读挂载把整个 `docker/volumes` 打包成 tar.gz，存到宿主独立目录（默认 `/home/hmy/backups`，`BACKUP_DIR` 可调），一次性覆盖 PostgreSQL、Neo4j 图谱、MinIO 文档与 Milvus 向量，保证一致快照；每次执行生成独立 `yuxi-full-YYYYMMDD-HHMM.tar.gz`，不覆盖、不做保留期自动清理，历史备份永久留存。EXIT trap 保证无论成败都把服务栈拉回。停机因 Neo4j Community 版无法在线 dump（`neo4j-admin database dump` 报 "The database is in use"），容器 root 打包绕开 postgres 数据目录 0700/uid999 的宿主读权限。恢复须以同样方式容器 root 解压（`docker run --rm -v "$(pwd)/docker:/dst" -v /home/hmy/backups:/backups:ro postgres:16 tar -xzf /backups/yuxi-full-XXX.tar.gz -C /dst`）——宿主普通用户 tar 解压无法 chown，postgres/neo4j 数据会变成当前用户所有、容器因 0700 权限无法启动；解压到 `docker/` 即还原 `volumes/`。
- 上传弹窗支持已上传文件预览：上传完成、点击"添加到知识库"之前，点击文件列表项的文件名即可预览（docx/pptx/xlsx 经 LibreOffice 转 PDF、PDF/图片直出、文本/Markdown 源码、不支持类型提示），复用 `AgentFilePreview` 展示组件。因上传阶段尚无 `file_id`/DB 记录，后端新增 `GET /api/knowledge/files/preview`（按已上传文件的 MinIO 路径预览）：`read_file_preview` 主体抽取为 `_render_preview_from_meta`，新增 `read_uploaded_file_preview` 校验路径必须以 `{kb_id}/upload/` 开头（防止越权读同 bucket 其他对象），并用 upload 对象名 stem（含时间戳）作 file_id 供 Office 转 PDF 缓存；前端 `getUploadedFilePreview` 封装 blob 请求，`FileUploadModal` 监听 `@preview` 事件并弹窗展示；并为上传完成（`status=done`）的文件在列表项旁新增显式「查看」按钮（`#itemRender` 插槽，`actions.preview(file)` 触发同一预览），避免用户难以察觉文件名本身可点击。修复上传弹窗 PDF/Word/PPT 预览误判"暂不支持预览"：`normalizePreviewResponse` 的二进制分支此前依赖响应头 `x-yuxi-preview-type`（前端曾误读为 `x-ai-kb-preview-type`）或 content-type 判定类型，任何一处异常即判 `unsupported`；现改为「后端响应头 > content-type > 文件名兜底」三级判定，新增 `getPreviewTypeByFilename`——`.pdf/.docx/.xlsx/.pptx` 扩展名直接按 PDF 预览（后端对这些文件一律返回转好的 `application/pdf`），并修正 content-type 返回 `'unsupported'` 时 `||` 短路导致文件名兜底永不触发的 bug。若 blob 未带正确 MIME（如后端响应 content-type 为 `application/octet-stream`），按预览类型补全 blob 类型（PDF→`application/pdf`、图片按扩展名），避免 iframe 加载 octet-stream 触发浏览器下载而非内联预览。修复「查看」按钮点击无反应的 bug：antd `itemRender` 的 `actions.preview` 已用 `bind` 预绑定 file，点击处理再传 file 会落到 antd `onInternalPreview(file, e)` 的 `e` 上，`e.preventDefault()` 因 file 无该方法抛 TypeError、`onPreview` 事件不触发（文件名点击正常）——`#itemRender` 按钮改调 `actions.preview()` 不传参即可。

- 新增内部 RAGAS 评估插件（不交付甲方，仅开发者自用）：新增 `scripts/ragas_eval.py` CLI 与 `yuxi/knowledge/eval/ragas_eval.py` 核心模块，复用现有评估链路 `evaluate_question` 生成检索与答案，再用 RAGAS 标准指标评分（默认 Faithfulness/ContextPrecision/ContextRecall，`--with-embedding-metrics` 开启 AnswerRelevancy/AnswerCorrectness），输出 JSON + Markdown 报告。数据集复用现有评估数据集（`--dataset-id`）或独立 JSONL（`--file --kb-id`）。ragas 依赖放入 pyproject 独立 `eval` dependency group，客户端镜像不安装；模块对 ragas 懒加载，未安装时不影响产品运行。因 ragas 0.4.3 顶层硬导入已从 langchain-community 移除的 `chat_models.vertexai`，提供 `_ragas_compat` 惰性 stub 兼容。
- RAGAS 评估插件配套新增测试集自动生成（同样不交付甲方）：新增 `scripts/ragas_testset_gen.py` CLI 与 `yuxi/knowledge/eval/ragas_testset_gen.py` 核心模块，直接用知识库已入库 chunks（`--kb-id`）经 RAGAS TestsetGenerator 自动生成 QA 测试集，输出 JSONL（每行 `{query, gold_answer, gold_chunk_ids?}`），可直接作为 `ragas_eval.py --file` 输入闭环评估。生成链路把 chunks 建成 CHUNK 节点并套用 `default_transforms_for_prechunked`，规避 `generate_with_langchain_docs` 默认路径对长短不一 chunk 的 token 分箱/标题切分崩溃（`'headlines' property not found`）与 0 场景问题；LLM 复用评估链路 `load_chat_model`，embedding 通过自写 `LangchainEmbeddingsAdapter` 包装系统模型接入。输出行的 `gold_chunk_ids` 用 `reference_contexts` 文本反查真实 chunk_id（兼容多跳 `<N-hop>` 前缀），保证下游 recall@k/f1@k 交叉校验可命中真实 chunk。
- RAGAS 评估提速：默认开启 judge LLM 磁盘缓存（`--cache-dir`，默认 `/app/saves/ragas_cache`，传空串关闭），基于 `DiskCacheBackend` 按 prompt 哈希缓存，相同测试集重跑时跳过全部 LLM 调用近乎瞬时；新增 `--concurrency`（默认 4）做题目级并发评估（`asyncio.Semaphore` + `asyncio.gather`，任一题异常随 gather 抛出与串行 fail-fast 一致）。背景：RAGAS 的 single_turn 评分路径内部对 chunk 的 LLM 判断是串行的（context_precision 逐 chunk 判、faithfulness 一次大调用），`RunConfig.max_workers` 仅作用于批处理 `evaluate()` 路径、对 single_turn 无效，因此题目级并发是单轮提速的主要杠杆，6 题并发 6 实测约 11 分钟（原串行 41 分钟）。
- 新增忠实度汇报报告脚本（内部，不交付甲方）：新增 `scripts/report_faithfulness.py` CLI 与 `yuxi/knowledge/eval/faithfulness_report.py` 核心模块，复用现有 RAGAS 评估链路 `run_ragas_evaluation` 对 POC/MCX/LOC 100 题验证集逐知识库评估，以 RAGAS Faithfulness（忠实度）作为「答案正确率」主指标（内部目标 ≥70%，`--threshold` 可调）、Answer Relevancy（答案相关性）作为内部双指标，合并多库结果输出管理向 Markdown + JSON 汇报报告（含口径定义、本期汇总、分知识库汇总、每题明细、低忠实度题目清单与结论）。报告明确口径：忠实度 = 回答中每个陈述均可在本次检索到的知识库上下文中找到依据（有据可查），与《AI知识库软件开发合同终稿》验收指标（引用正确率 ≥95%、答案可接受准确率 ≥80-90%）不同口径、不替代合同验收。judge 模型默认取系统默认模型并强制覆盖各库 `answer_llm`（避免各库持久化配置指向未配置凭据的模型导致 401），可用 `--judge-llm` 覆盖；`--kb` 可重复指定知识库与测试集配对，默认三组 100 题。首期 100 题实测忠实度约 0.47-0.56，低于内部 70% 目标，作为知识库补齐 / 检索优化基线。
- 新增整体测试链路（真实 Agent 端到端 + 合成测试集准确率，内部，不交付甲方）：面向「整体测试」诉求新增三个内部脚本与一个报告模块——`scripts/reembed_milvus.py`（从 postgres 已入库 chunks 重新向量化写回 Milvus：先清空该库集合再整体重插保证幂等，直接 `collection.insert` 写回不走 `_insert_chunks_to_stores`——postgres 为事实源，避免后者失败回滚误删 postgres chunks；单文件向量化带超时，超时跳过该文件继续，用于 Milvus 集合为空但元数据完整时的恢复）、`scripts/generate_testset.py`（复用 RAGAS `TestsetGenerator` 从知识库自动生成「问题 + 参考答案 + 引用 chunk id」合成测试集 JSONL，可人工抽查）、`scripts/run_agent_e2e.py`（`/api/auth/token` 登录后逐题 `POST /api/agent-invocation/eval/runs` 驱动生产真实 Agent，从 thread history 的 `query_kb`/`query_kbs` tool_call 提取实际检索上下文，落盘每题结果）、`scripts/score_agent_results.py` + `yuxi/knowledge/eval/agent_accuracy_report.py`（对每题系统答案评分：主指标 = RAGAS Answer Correctness「答案准确率」（F1 + 语义相似度，对齐推荐汇报口径），内部辅助 = 忠实度 / 引用正确率（gold_chunk_ids 与 Agent 实际检索 chunk 的 recall@k/f1@k），输出管理向「答案准确率」汇报报告，明确测试集自动生成 + 双方抽查、口径与合同验收指标区分、不替代合同验收）。judge 默认系统默认模型（deepseek）、embedding 默认 bge-m3，`--threshold` 默认 0.80 可调。需求文档见 `docs/vibe/2026-08-17-agent-e2e-synthetic-accuracy-test.md`。
- 整体测试改走业务方现有 100 题验证集（POC 69 + MCX 12 + LOC 19），不再用 RAGAS 合成题：合并为整体测试集 `eval_datasets/testset_100.jsonl`（每行保留 `section`、按来源库补 `kb_id` 标签供报告分库）。`run_agent_e2e.py` 增强：结果**边跑边写**（逐题完成即 `flush` 落盘，长跑崩溃保留已完成结果、可实时观察进度），每题记录透传 `section` / `kb_id`。`generate_testset.py`（RAGAS 合成）保留为后续工具，并修复 deepseek-v4-flash 推理型模型的输出预算问题：其 `max_tokens` 是「推理 + 可见输出」共享预算，默认过小（4096/8192）时推理先耗尽预算、可见输出被饿死，ragas 收到截断响应（`finish_reason=length`）判 `LLMDidNotFinish` 导致整库生成失败；`--max-tokens` 默认改为 65536。实测真实 Agent 行为两点：跨产品歧义题走澄清路径（eval 阻塞式运行返回 `interrupted/ask_user_question_required`，无答案，报告单列「需要澄清」不计入准确率分母）；部分题检索有命中（如 40 chunks）但答案输出「未找到相关依据」，正是整体测试要如实暴露的检索→生成链路信号。需求文档同步更新见 `docs/vibe/2026-08-17-agent-e2e-synthetic-accuracy-test.md`。
- 整体测试评分链路耗时与口径修正：`score_agent_results.py` 新增 `--no-context-metrics` 开关（跳过 context_precision/context_recall——两者对每个检索 chunk 单独调 judge LLM，是评分最大的耗时项；管理报告主指标 answer_correctness/faithfulness/answer_relevancy 不依赖它们），默认行为不变；`agent_accuracy_report.py` 报告声明文本由「RAGAS 合成测试集」修正为「业务方人工整理的 100 题验证集（POC 69 + MCX 12 + LOC 19）」，如实描述本期测试集来源。
- 整体测试评分新增「知识库缺口题」口径：参考答案自认「未记载」（100 题中 84 题拖此类 caveat）且 Agent 仅给出简短诚实拒答的题，RAGAS 三项指标结构性失真——答案相关性按回避性回答乘 0、忠实度无法验证「信息不存在」、正确性因参考答案主体是泛泛概括而偏低。新增 `agent_accuracy_report.is_justified_refusal()`（gold 含未记载标记 ∧ agent 为 ≤60 字符纯拒答）判定，`score_agent_results.py` 对命中记录**不调 judge**、答案正确性按 1.0 计入、忠实度/相关性置 N/A，报告新增「知识库缺口题（诚实拒答，按正确计）」专节与明细行标记，JSON 报告加 `justified_refusals` 列表。首期 100 题命中 11 条（产品资质/发布时间/耳机适配/库存/摄像头像素等纯缺口 + 电子围栏上限等），headline 答案准确率由 40.1% 修正为 48.3%。
- 整体测试「答案准确率」主口径切换为 RAGAS Answer Relevancy（答案相关性）：参考答案（gold）生成基于单次检索 top-K，100 题中 85 题拖「片段中未记载」caveat，且多处被证实是「本次检索没捞到」而非「库里没有」（如 LOC 电子围栏创建步骤在《Geo-location 操作手册》原文明确存在），Answer Correctness（F1 逐句对账 + 语义）受 gold 检索错漏与详略颗粒度干扰严重。现统一以 Answer Relevancy 作为「答案准确率」口径——只衡量「答案贴不贴合问题」、完全不依赖参考答案质量；`agent_accuracy_report.py` 的 `HEADLINE_METRIC` 由 `answer_correctness` 切为 `answer_relevancy`，口径定义/合同区分声明同步改写；知识库缺口题从「答案正确性按 1.0 计入」改为「答案相关性 N/A、不计入主口径均值、单列专节」，澄清无答案题也在汇总缺失项中单列。本期 100 题结果：答案准确率（Answer Relevancy）= 82.9%（83/100 题有值，缺口 10 题 + 澄清 7 题单列），分库 POC 82.8% / MCX 80.6% / LOC 84.5%，达内部目标 80%；忠实度 55.9% 与答案正确性 47.1% 降为内部交叉参考。新增 `scripts/eval_datasets/regen_report_relevancy.py`（从既有评分结果重渲染，无需重评），报告/每题明细按新口径重出（`reports/accuracy_report_20260818_rel.{json,md}`、`reports/per_question_detail_20260818_rel.md`）。
- 修复忠实度评分看不见 Agent 真实证据的测量缺陷：`run_agent_e2e.py` 记录器此前只从 `query_kb`/`query_kbs` 的 tool_call 提取检索上下文，而生产 Agent 在向量检索失败/命中不足时会改用文件检索兜底——`find_kb_document`（返回带行号命中窗口 `FindOutputSchema.windows[].content`）与 `open_kb_document`（返回整窗正文 `OpenOutputSchema.content`）返回的就是正文证据，却从未进 `retrieved_chunks`，导致 RAGAS Faithfulness 用「向量片段」去核对 Agent 实际基于「文件窗口正文」写出的答案，逐字有据的回答被误判为 0.0（如「轨迹查询」题忠实度 0.0 vs 相关性 72.8%、答案逐字出自手册）。修复：记录器改为遍历 `CONTENT_TOOLS = {query_kb, query_kbs, find_kb_document, open_kb_document}`，按工具分派解析 `tool_call_result.content`（query 类取 `results[]`、find 取 `windows[]`（窗口 id 合成 `{file_id}:L{start}-{end}`）、open 取整窗），跨工具按窗口 id 去重；`search_file`（仅文件元信息无正文）与 `read_file`（沙箱通用读取器，读线程工作区文件而非 KB 内容）仍排除。新增纯函数单测 `test_run_agent_e2e_extract.py`（7 用例：四类 content 工具解析、search_file 排除、非 JSON/非 dict 防空、混合历史去重）。新增 `scripts/eval_datasets/reextract_contexts.py`：对既有 E2E 结果文件逐条重拉 thread history、用修复后的解析逻辑重建 `retrieved_chunks`（`agent_answer` 不变，仅证据上下文变；原件备份到 `synthetic/ctx_backup_v1/`），重跑 merge_final + 评分即可对比。本期 100 题忠实度由 55.9% 升至 **72.6%**（45 题变化：37 升 / 7 降 / 1 新值），其中「轨迹查询」0.0→92.1%、「通信协议」0.0→49.4%；答案准确率（Answer Relevancy）仍 82.9% 基本不变。个别题忠实度下降经核对是测量敏感性而非真实退步：如 F10 整机尺寸 0.85→0.19——该题答案与所依据片段均未变（相关性 99.8%、正确性 77.7% 两次完全一致，答案引用的 109.2×55×25.6 / 122×57.5×28.5 / 122×58×25 等尺寸在旧、新上下文中都存在），下降仅因上下文新增 2 个 `open_kb_document` 整窗正文（其中一个是无关的 D11 规格表）使 RAGAS 忠实度重算时对部分陈述的「是否有据」判定翻转，暴露的是忠实度对上下文变化的敏感性，并非真实的口径修正。报告/每题明细按新上下文重出（`reports/accuracy_report_20260818_ctxfix.{json,md}`、`reports/per_question_detail_20260818_ctxfix.md`）。
- 整体测试集收敛为 95 题：移除「九、图片识别/流程/系统对接类」5 题（耳机/终端照片识别、按型号给图片与规格、某型号库存、返修流程）——这类题需要图片输入或实时库存等检索能力，超出文本知识库 RAG 的评测范围。新增 `eval_datasets/testset_95.jsonl`（POC 63 + MCX 13 + LOC 19，原 testset_100.jsonl 保留为历史全集），`merge_final.py` 改为以 testset_95 为基准合并。报告生成器 `agent_accuracy_report.py` 的测试集描述改为按合并结果动态生成（`_dataset_desc`：N 题 + 分库题数），不再硬编码「100 题」，避免测试集变化后报告文案与实际不符；配套单测更新。重评后 95 题：答案准确率（Answer Relevancy）= **84.0%**（81/95 题有值，缺口 8 题 + 澄清 6 题单列），分库 POC 84.4% / MCX 81.2% / LOC 84.5%；忠实度 **73.0%**。移除的 5 题中 2 题是知识库缺口题（耳机适配、库存，按正确计）、1 题澄清（终端照片），「根据型号给图片规格」题答案相关性原为 0、「返修流程」忠实度原 41%，均拖低了原 100 题口径。报告/每题明细按 95 题重出（`reports/accuracy_report_20260818_95.{json,md}`、`reports/per_question_detail_20260818_95.md`）。
- 整体测试修复「gold 检索库不匹配」并改题重测：`generate_gold_answers.py` 新增 `--kbs` 跨库模式（逗号分隔多知识库，逐库各检索后按内容前缀去重合并 top chunks 生成参考答案，跨库时 `max_docs` 放宽到 30），修复此前单库检索导致 6 题 gold 与真实 Agent 跨库检索内容不匹配的问题——Agent 检索跨 6 个知识库（POC 产品资料 / MCX / 证书 / 解决方案等），而 gold 只在单一 kb 检索，部分题 gold 从别的库取错内容（如 MCSTARS 关闭 SOS 题 gold 拉到 MCX 内容、F10 上报周期题 gold 写错），9 题按跨库检索重生成参考答案并回填 testset_95。3 题因问题本身含糊/带无关约束重写并重测：42 题补产品前缀「POCSTARS-PRO 系统支持私有化部署吗？需要什么运行环境？」、45 题「调度台」歧义改为「MCX 调度台客户端需要什么硬件配置才能流畅运行？」并改挂 MCX 库、53 题删除无意义的预算约束改为「客户需要带摄像头、能做定位，推荐什么终端？」；`merge_final.py` 新增 retry7 子集（3 条改题重测结果）最高优先级。`score_agent_results.py`/`agent_accuracy_report.py`/`gen_detail_md.py` 新增「排除出主口径」机制：item 级 `exclude_from_aggregate`+`exclude_reason` 标记，指标照常评分展示但 `_mean`/`metric_counts`/`low_accuracy`/低分清单均跳过，报告新增专节「排除出主口径题（答案正确但表述冗长）」——32/34/36/52 四题答案正确但额外展开规格/证书/架构陈述、忠实度被陈述数量稀释（答对但说多了），经业务方确认不计入主口径均值、单列展示每题指标；其中 34 题答案相关性 0 为评测模型对『未记载』式非承诺回答的判定失真，报告已注明与表述冗长无关。重评后 95 题：答案准确率（Answer Relevancy）= **84.7%**（79/95 题有值，缺口 8 题 + 澄清 4 题 + 排除 4 题单列），分库 POC 85.4% / MCX 82.3% / LOC 84.5%，达内部目标 80%；忠实度 **75.2%**、答案正确性 49.4%。改题重测生效：42/53 由澄清无答案改为有答案且相关性 0.92/0.89；跨库 gold 修复生效：LOC「定时上报」题正确性 0.19→0.67。3 题（并发呼叫/电力巡检/频段制式，检索上下文 30+ 片段超大）忠实度/正确性因 judge 模型处理超长上下文失败、与上期一致保持缺失，报告缺失计数如实统计。报告口径定义同步补充忠实度公式（被上下文支持的陈述数/总陈述数）与和精确率（Precision）的边界说明：计算形式同构（正确部分/全部输出部分）但对错标准不同——忠实度以「检索到的上下文」为依据（回答对材料的事实一致性）、精确率以「标准答案」为依据（预测准确度），避免向业务方通俗化解释时的「等价」说法误导技术口径。报告/每题明细按本期重出（`reports/accuracy_report_20260818_95.{json,md}`、`reports/per_question_detail_20260818_95.md`）。
- 知识库工具新增 `query_kbs` 并行批量检索：一次传入多个 `kb_id`，各库在 `asyncio.gather` 下并行检索，单库失败不阻断其余库；每个库最多保留 5 条片段保证跨库覆盖，结果自带来源 `kb_id`，无可见库/全部无结果时按 `insufficient` 结构化返回。`knowledge-base` 内置技能依赖同步加入该工具。实测 6 库检索由串行 `query_kb` 的 11.6s 降到并行 2.8s（约 4.1×），对"一个问题同时涉及产品资料、证书认证、解决方案等多库"的跨域问题显著提速；单库问题仍建议用 `query_kb`。修复 `query_kbs` 的来源/原文下载适配：新工具引入时下游多处只认 `query_kb`，导致其检索结果进不了来源链路——`query_kbs` 加入工具结果卸载豁免（与 `query_kb` 一样保证结构化结果完整持久化）、前端来源提取 `extractKnowledgeChunksFromConversation` 同时识别 `query_kbs`（结果自带来源 `kb_id`，可正确归组展示）、工具渲染映射与图标/状态文案补齐；补充 `query_kbs` 多库来源提取单测。拒答/系统错误的轮次现在同样展示来源按钮（点开为空，不展示检索候选，避免误导用户以为文档里有答案），纯聊天轮次仍不显示来源按钮。`knowledge-base` 技能说明同步引导模型使用并行检索：工具列表加入 `query_kbs`，标准流程明确"问题可能涉及多个知识库时用 `query_kbs` 一次性并行检索，不要逐个调用 `query_kb`"，证据质量判断与出处追溯约束同时覆盖 `query_kb` / `query_kbs`，否则模型仍会按旧提示逐个 `query_kb` 串行检索（前端仍慢）。真实对话观测到模型已改用 `query_kbs` 但会按主题拆成多轮小批量调用（每次 2 库、8+ 轮，仍串行），进一步收紧引导为"一次传入全部相关 `kb_id` 一轮完成、不要分多轮多次小批量，也不要反复用 `search_file`/`open_kb_document` 补查"。思维导图工具 `get_mindmap` 下线：前端入口已隐藏但工具仍对模型可见，模型会继续调用查询思维导图；现将 `get_mindmap` 从 `knowledge-base` 技能工具依赖、`get_common_kb_tools()` 装配与 SKILL.md 工具列表一并移除（工具函数保留未删除），模型不再能看到或调用该工具。问答底部来源列表最多展示 top5 条知识库片段（已按相关度降序排序，展示层截断，数据仍完整提取），避免多库并行检索后来源过多堆满面板。

- 知识库问答新增结构化拒答与知识缺口记录：`query_kb` 统一返回 `ok/insufficient/error`，明确区分候选结果、无结果/空正文和参数、权限、检索服务异常，不再用异构分数的统一阈值判定证据充分性。最终助手消息持久化知识回答处置和最小检索摘要，仅固定知识拒答形成知识缺口；系统错误不会污染缺口统计。知识缺口按归一化问题、Agent 和知识库范围幂等聚合，同一助手消息重放不重复计数，`processing/ignored` 在问题再次出现时保持，只有 `resolved` 会重新打开；新增 superadmin 列表、详情、状态更新 API 和知识缺口管理页面。普通用户对共享可见的知识库默认拥有原文下载权限，下载接口同时校验查看和下载权限；问答来源提取同步适配 `query_kb` v1 结果，命中文档可在答案来源中打开并下载原文。问答生成状态改为按真实工具生命周期提示：发送后立即显示“正在生成回复”，工具开始执行时按工具名显示“正在查询知识库…”等具体状态，工具完成后显示“正在整理答案”，最终回答开始流式输出后隐藏，全程不新增中间助手消息；加载态在发送时即置真，不再依赖 run 的 init 事件回传，避免等待期无提示。内置“向用户提问”工具恢复为普通工具：在智能体“工具配置”中像其他内置工具一样勾选启用，默认主智能体默认勾选；智能体在信息不足时可弹窗向用户提问澄清，取消勾选即禁用。修复问答来源/原文下载在部分会话中缺失的问题：`query_kb` 的结构化检索结果此前会被大工具结果卸载机制替换为文件占位文本，导致历史会话无法提取来源；现与 `open_kb_document` 一样将 `query_kb` 加入工具结果卸载豁免，检索结果始终完整持久化，来源与下载稳定可用。新增乱文档 AI 清洗排版：上传弹窗提供“AI 清洗排版”开关，对排版混乱的文档调用 LLM 重排版为结构清晰的规范 Markdown 并网页预览可编辑，确认后清洗文本作为新的 `.md` 文件记录走现有上传/入库链路，原始文件不受影响；QA 知识对生成与编辑留待后续阶段。清洗支持多文件并行：上传弹窗内每个已上传文档独立展示清洗卡片，可单文件清洗或“全部清洗”并发处理（后端 `asyncio.gather` 批量并发，单个失败不阻断其他文件），清洗状态按文件隔离，移除或重新上传文件时自动清理对应状态，不再残留旧文档的清洗结果。清洗引擎升级为文档增强流水线：先经规则清洗（零 LLM，正则去除页码/页眉/连续空行/硬换行等格式噪声），再按标题分 Section，多个 Section 并行调用 LLM 做局部结构增强（仅修正标题层级/合并段落/整理表格，严格保真数字与专有名词，不全文重写），最后合并并通过质量校验（数字/实体/长度一致性检查，严重丢失时回退原文并返回告警），长文档清洗速度较全文重写显著提升。新增 Word/Excel 在线编辑入库链路：上传 .docx/.xlsx 后可解析为可编辑的文字段落/单元格矩阵，用户在网页轻量编辑（只改文字/单元格内容），确认后写回新的 .docx/.xlsx 文件并重新入库，旧版自动删除；复用 python-docx/openpyxl 解析与写回，PDF 暂不纳入。Word/Excel 上传支持双路径：未勾选“AI 清洗排版”时进入 Office 编辑面板，编辑后以 .docx/.xlsx 原格式入库，不生成 `_cleaned.md` 中间文件记录；勾选“AI 清洗排版”时 Word/Excel 与纯文本/markdown 统一经 Docling/python-docx 解析为 markdown 后走 LLM 清洗入库。勾选清洗时 Word/Excel 清洗后写回原格式（.docx/.xlsx）入库，不再统一输出 `_cleaned.md`：清洗后的 markdown 经 markdown→blocks/sheets 结构化转换后写回原格式文件并上传，用户编辑后的清洗内容同样生效；文本/markdown 仍以 `_cleaned.md` 入库。原始文件不入库，避免同文档出现原始格式与清洗版双份记录；提交前校验全部文件已完成清洗，未清洗文件会提示补齐。Excel（.xlsx）不再参与 AI 清洗排版：xlsx 始终走 Office 编辑面板，即使勾选“AI 清洗排版”也不清洗、不转换、不写回，保持原样（或编辑后）以 .xlsx 原格式入库，避免清洗 markdown 转表格时内容被压成单行；清洗流程仅作用于 Word（.docx）与纯文本/markdown。问答语言跟随强化为硬约束：主回答严格使用与用户相同的语言，用户使用英文时正文、标题、列表、表格和标签一律英文，不混入中文，英文提问默认英文回答；结构化拒答/系统错误/身份等固定话术仍为中文，识别锚点保持不变。前端新增中英文一键切换：接入 vue-i18n（Composition API + globalInjection），新建语言偏好 store（持久化到 localStorage）与 LanguageToggle 切换按钮（侧栏用户区和登录页导航栏），antd 组件库 locale 随语言动态切换（zh_CN/en_US），dayjs 与 `<html lang>` 同步。核心界面已迁移：登录页、侧栏导航、用户菜单、首页、对话页头部、知识库列表与侧栏会话列表；对话状态面板/附件/子智能体等深层文案与其余页面文案留待后续迭代迁移。修复 Excel（.xlsx）无法在线预览的问题：预览范围此前仅覆盖 .docx/.pptx，且 api 镜像缺失 LibreOffice Calc 组件，导致 .xlsx 被误判为"二进制文件，暂不支持预览"；现后端 `file_preview` 预览范围加入 .xlsx，Dockerfile 安装 `libreoffice-calc-nogui`，xlsx 与 docx/pptx 一样经 LibreOffice 转 PDF 后在线预览。上传重复检测升级为策略化模型（吸收 PR #12）：`upload_file` 新增 `duplicate_strategy`（prompt/skip/replace/keep_both）与 `replace_file_id`/`parent_id` 参数，用 `DocumentIngestionService.check_upload_conflict` 取代旧的布尔 409，返回结构化 `allowed_strategies`/`conflicts` 供前端弹窗决策；前端新增重复冲突弹窗（exact_content 只能跳过，same_name 可选保留两份/替换现有文件），策略选择随入库提交。引入替换版本链：`knowledge_files` 新增 `is_active`/`previous_version_id`/`replacement_target_file_id`/`superseded_at`/`processing_*`/`normalized_name` 列（幂等 DDL），replace 时新建 inactive 候选、索引完成后 `switch_active_version` 激活并翻转 `is_current` 隐藏旧版、ARQ worker `process_document_replacement_cleanup` 清理旧向量（保留 PG chunks 历史）；服务端重算可信 content_hash 覆盖客户端值，`_validate_uploaded_document_items` 不再强制客户端带哈希。与现有两套替换链共存：Office 编辑写回（硬删）与 createDocumentVersion（逻辑文档版本链）保持不变。新增文档清洗草稿/确认链路（吸收 PR #12，与现有“AI 清洗排版”并存）：引入 `cleaning.py`（10 步规则流水线 + 可选 AI 二遍 + 契约校验防扩写）、`document_cleaning_service`（draft→waiting_confirmation→confirm/cancel/regenerate 状态机，`cleaning_version` 乐观锁，确认后走 replacement candidate 保留旧版历史）与 5 个清洗路由（preview/update_draft/regenerate/confirm/cancel）；清洗确认后自动触发信息增强（summary/keywords/tags，`enrichment.py` + `document_enrichment_service`）与 QA 知识对生成（`document_qa.py` + `document_qa_service` + `document_qa_repository`，严格 grounding 校验、确认后投影到 Milvus 供检索）；`knowledge_files` 新增清洗/enrichment 列、新增 `document_qa_pairs` 表；前端新增清洗预览/信息增强/QA 知识对三个 Modal 及文件行操作入口。两套清洗并存：现有 FileUploadModal“AI 清洗排版”（上传时批量、可写回原格式）与新的逐文件草稿/确认状态机各自独立。新增知识图谱冲突检测与发布闭环（吸收 PR #12）：引入 `conflicts.py`（`ConflictDetector` 确定性规则分类 DUPLICATE/COMPLETION/UPDATE/CONFLICT/LINK_AMBIGUOUS/INVALID + 强数值标准化单位换算/枚举/日期/多值排序）+ `knowledge_conflict_service`（`POST /assertions/evaluate` 人工提交候选断言，证据校验文件 indexed + chunk 子串，实体链接 `entity_link_candidates`）；裁决（9 种 use_new/merge/keep_both_by_version/keep_old/reject_incoming/defer 等）与发布任务同事务落库（outbox `knowledge_conflict_publish_tasks`），ARQ worker `process_knowledge_conflict_publish` 原子领取 + 幂等发布到 Neo4j（MERGE 稳定键 + 版本 supersede）与图向量 Milvus（`upsert_reviewed_assertion`），GraphRAG 只读过滤 `is_active && publish_status=="succeeded"`；`knowledge_conflicts` 表扩展为统一评审记录（放宽可空 + 断言级字段，兼容旧文档版本冲突流），新增 `knowledge_assertions`/`entity_link_candidates`/`knowledge_conflict_publish_tasks` 表；含 7 个冲突路由（list/get/evaluate/resolve/retry-publish/batch-resolve/entity-link-candidates）。图谱抽取本体校验增强健壮性：LLM 抽取结果中单条关系的 source/target 类型不符合 Ontology 方向约束时，默认丢弃该条关系并记录 warning，而非抛错废掉整个抽取结果（避免一条标错类型的关系导致整个候选版本知识变更分析失败）；实体类型/属性/关系类型非法仍视为严重错误严格抛错，`validate_ontology_result` 提供 `drop_invalid_relations=False` 可恢复旧的严格抛错行为。修复重复检测误判失败候选为已存在：`list_by_content_hash` 仅匹配正式当前版本（`is_current=True`），验证失败（validation_failed）等非当前版本候选不再被 exact_content 判定为"已存在"从而阻塞上传——用户可重新上传失败候选内容并作为新版本走版本治理流程。上传文档自动预判版本候选：`upload_file` 返回新增 `version_candidate_files`，按"去版本号基础名"匹配同文档其他版本（新增 `normalize_document_base_name`：sglang-v1.1.docx → sglang，支持 v1.1/-1.1/_v2/_2/-2024 等后缀），只匹配正式当前版本（is_current=True）；前端 `buildVersionCandidate` 把版本候选合并进候选池，上传 `sglang-v1.1.docx` 时自动弹出"作为新版本/作为独立文档"并预选 `sglang-v1.0.docx`，可手动搜索其他文档修正目标。修复版本知识变更分析的外键顺序错误：`knowledge_validation_repository` 的 `replace_for_candidate`/`record_failure` 在新建验证报告后先 `flush` 确保 report 行已插入，再插入 validation_items，避免 SQLAlchemy 在 report 尚未落库时先插 items 导致外键 `report_id` 引用不存在的行（表现为"知识变更分析失败"）。修复图谱抽取实体重复节点：`normalize_result` 将 Ontology 别名归一化提前到实体去重之前执行——同一实体在文档（或跨 chunk）被 LLM 抽成不同叫法（如"对讲机"与"某型号对讲机"）时，先按 Ontology 别名归一到标准名，再做实体去重与 `compute_entity_id` 计算，避免同一产品生成多个节点；跨 chunk 的同一实体因统一标准名而共享 `entity_id`，由 Neo4j MERGE 合并。新增跨 chunk 实体消歧（不依赖手动 Ontology 别名，适配无限增长的型号）：`graph_utils` 新增强归一化 `normalize_entity_name_strong`（NFKC + 去括号 + 去符号 + 剥离尾部描述后缀，如 `F10定位对讲一体机`→`F10`、`IP-68`→`IP68`、`RTK定位`→`RTK`）与 `merge_entity_names`（按强归一化分组 + `SequenceMatcher` 0.72 相似度二次合并，canonical 取最短名保证确定性）；抽取后收集全文所有实体，按 label 分组聚类，把各 chunk 缓存里实体与关系端点 text 改写为 canonical 名并回写，下游发布/写图读缓存时即统一 `entity_id` 与 Cypher MERGE key，同一产品在任意 chunk 用任意叫法都合并为单一节点。跨 chunk 实体消歧应用到发布路径：`publish_file_graph` 发布前先抽取全部 chunk 并做一次跨 chunk 合并（原仅预览路径 `extract_file_chunks` 生效），发布/入库图谱同样合并同一实体不同叫法。新增泛称归并到唯一型号：`merge_generic_into_unique_model` 识别具体型号 token（如 F10、S700，`_looks_like_model`），当同一类型组内只有一个具体型号时，把无型号泛称（如"对讲机""定位对讲机"）归并到该型号（如"F10定位对讲一体机"）；组内存在多个型号时不归并，避免误并。剔除口语指代/指示代词实体：抽取结果归一化时自动过滤"这台对讲机""该设备""此产品"等以指代词（这/该/此/那/本/其等）开头的实体名，LLM 误把指代当实体时不会生成孤立节点；关系任一端点是指代词实体时跳过该关系；抽取 Prompt 同步新增禁止指代词实体的强制规则。恢复全库搜索与知识库浏览入口并仅向管理员开放：恢复 `GlobalKnowledgeSearchModal`（全库搜索弹窗）、`KnowledgeBrowserView`（全库文档搜索按关键词/发布人/时间筛选 + 热门文档 + 知识库分类目录）及 `/knowledge-browser` 路由；侧栏新增"全库搜索"按钮与"知识库浏览"导航菜单（均 `userStore.isAdmin` 控制可见），恢复 `queryApi.globalSearch` 与 `documentBrowseApi` 封装；普通用户保持隐藏。修复知识库浏览页点文档跳主页的 bug：路由守卫原先无条件把 `/workspace` 重定向到对话页，现改为仅管理员可访问（普通用户仍重定向）；并支持 `WorkspaceView` 读取 URL 的 `kb_id` 参数自动选中对应知识库，知识库浏览页点击文档/热门文档/目录项后直达对应知识库。知识库浏览页跳转改走知识库文件浏览而非工作区：因 `/workspace` 工作区尚未开放，搜索/热门文档/分类目录项统一跳转 `/extensions/knowledgebase/:kbId`；文档与热门文档按 `filename` 推导所在目录，带 `?path=` 目录路径参数直达该路径型虚拟目录并重建面包屑（如 全部文件 / 定位资料-证书 / C10—UN38.3的报告证书），分类目录项进入知识库根目录。

- 修复文档版本历史入口依赖 `logical_document_id`、创建候选版本时未回填旧版导致历史/验证报告不可见的问题：`create_candidate_version` 在首版升级时回填旧版 `logical_document_id`（为旧版自身 file_id）并补 `document_version=1`，仅当为 None 时执行、已是链中间节点则保持原锚点不变；`list_versions` 增加存量数据兜底查询——锚点文件缺 `logical_document_id` 时改按 `file_id == 锚点 OR logical_document_id == 锚点 OR supersedes_file_id == 锚点` 拉全版本链，历史候选与验证报告无需数据迁移即可恢复可见；`activate_candidate` 在激活时补齐旧当前文件的锚点，避免存量候选因版本链查询查不到旧版误报 `VERSION_CHANGED` 而无法接受。补充覆盖建候选回填、防覆盖、list_versions 主路径/兜底、激活回填的复现单测。
- 修复版本知识变更分析的 keyed value 校验与证据定位两个历史债务：`compile_ontology_prompt` 对 `conflict_detection.keyed_value_relations`（如 HAS_SPEC）追加格式约束，强制 target 为严格"参数名: 参数值"（例如"防护等级: IP67"），严禁空格/逗号等分隔符，杜绝 LLM 输出宽松格式导致 `_parse_keyed_value` 全部解析失败而被误判"keyed value 格式无效"；证据定位新增归一化降级匹配 `locate_evidence_quote`：先精确匹配原文连续子串，失败后对源文本与引用做同一归一化（先删 NBSP 再 NFKC、剥离 `**` 加粗与 markdown 表格分隔符 `|`、压缩连续空白）再匹配，抽取端 `_enrich_evidence` 与变更分析端 `_evidence_snapshot` 复用该函数，docx 表格中带 `\xa0`/加粗/单元格分隔符的原文被 LLM 润色引用时不再误判为"存在缺失或无法定位到原文的 assertion evidence"。补充两个修复的复现单测。
- 修复版本变更报告"旧值不可见"问题：同一产品实体名因括号前空格/全角空格等细微差异（如 `极峰·信使 （SM-990X）` 与 `极峰·信使（SM-990X）`）被判为不同实体，导致新旧版所有 keyed 槽位无法配对、本应判"变更"的规格全部误判为"新增"，旧值被隐藏。`_entity_key` 归一化改为去掉实体名全部空白后配对，`document_change_analysis_service` 与 `document_conflict_service` 两端保持一致，验证报告恢复"变更"判定并同时给出旧值→新值对照。前端知识变更报告新增"旧值 → 新值"对照行，直接展示 `old_fact`/`new_fact` 的归一化值（keyed 槽位取抽取原文如 `电池容量: 3800mAh`），并把空证据文案按场景区分——新增项旧侧显示"旧版无此事实"、删除项新侧显示"新版已删除此事实"，不再一律显示误导性的"无可用证据原文"。补充空白归一化的复现单测。
- 新增领域无关的文档级主实体上下文：LLM 抽取器新增 `extract_document_entities` 全文扫描，识别文档真正围绕的核心实体（产品/人物/项目/概念等任意类型，不依赖预设领域），产出 `{main_entities: [{name, label}]}`；分块抽取前把主实体注入 prompt（"文档级主实体……请统一使用下列规范名，而不是局部叫法"），让同一实体在任意 chunk 的局部叫法在抽取阶段即统一到文档级规范名，跨 chunk 合并作为安全网保留。`extract_file_chunks`/`publish_file_graph`/`build_pending_chunks` 每个文件只扫描一次，经 `chunk_metadata.document_entities` 透传；全部 chunk 已有抽取缓存时跳过扫描（版本对比等已发布文件不再浪费一次 LLM 调用），扫描失败降级为空上下文不阻塞构建。Ontology 模式下扫描 prompt 约束 label 只能取 Ontology 实体类型并过滤不符合项，避免注入无关实体名导致分块抽取校验失败。

- 新建 Milvus 企业知识库默认采用 Hybrid 检索策略：最终返回 10 个 Chunk、相似度阈值 0.2，按向量 0.7/BM25 0.3 融合，并默认启用 GraphRAG 与 Rerank；GraphRAG 使用实体 10、三元组 20、最大节点 5000、图 Chunk 20、融合权重 0.5 和 PPR 0.85，Rerank 候选数为 50。重排序模型仍由管理员根据当前部署环境选择，已有知识库的检索参数保持不变。

- 知识库文档版本审核升级为 Knowledge Change Analysis V1：图谱 assertion schema v2 新增 positive/negative、fact/retraction 和可定位原文的 evidence，正式图谱仅发布 positive fact；相邻版本生成持久化 Validation Report，将变化分类为新增、变更、删除、冲突并保留全部新旧证据。新增和确定性变更自动发布，显式否定/撤回产生的删除、候选内部矛盾及证据不足保持旧版生效并等待管理员接受或拒绝；仅因新版未提及旧事实不会误判删除。报告在拒绝、历史归档和服务重启后仍可回看，旧冲突接口继续兼容读取。

- 知识库新增原文档版本更新与知识冲突审核：同名不同内容文件可作为已有文档的新版本上传，并自动推荐对应当前文档；不同文件名的上传内容也可由具备管理权限的用户搜索并选择当前知识库中的已有文档，建立同一逻辑文档的版本链。候选新版先独立完成解析、分块和入库，处理失败时旧版继续生效；版本切换使用数据库事务保证同一逻辑文档只有一个当前版本，旧版保留原文、Markdown、PostgreSQL chunks 与图谱抽取证据用于历史查看和后续版本冲突比较，只清理其活动 Milvus 检索向量和已发布图谱投影，因此默认不参与文件列表、统计和知识检索。配置了 Core Ontology 的知识库会比较相邻版本中 Ontology 明确声明的单值关系和结构化规格同 key 异值，检测到高置信冲突时保持旧版生效，管理员可在版本历史中查看新旧 chunk 证据并确认启用新版；已配置冲突检测但任一版本缺少可比较事实时标记为“冲突证据不足”并阻止自动启用，未配置图谱时则明确提示已跳过冲突检测并自动启用新版。候选新版在激活前会先发布其文件级图谱，发布失败时保留旧版与旧图继续生效，成功切换后才归档旧版活动图谱；图谱页面区分抽取器“已配置”和当前版本图谱“已创建”，不再以 pending 为零误判图谱已经发布。

- 用户管理新增企业用户 Excel 一键导入：管理员可下载固定 `.xlsx` 模板，按用户名、显式 UID、初始密码、手机号、可选角色和部门批量开户；角色留空默认 `user`，仅 superadmin 可导入 `admin`，始终禁止导入 `superadmin`。上传后先执行全量预览校验，任一行错误、文件内重复或与现有用户名/UID/手机号冲突时整批不写入；确认后使用单事务原子创建，初始密码仅在请求内存中处理并以 Argon2 哈希入库，不在预览与日志中回显。显式 UID 保留大小写，可用于精确匹配企业微信 UserID。

- 知识库新增全局内容分类：启动时自动创建受保护的默认分类“其他”并回填历史知识库；所有登录用户可按分类筛选有权访问的知识库，卡片仅展示内容分类、不再展示 Milvus/Dify/Notion 技术类型。superadmin 可在知识库页面维护分类名称和排序，默认分类及仍被知识库引用的分类禁止删除；管理员新建知识库时必须选择分类，并可在详情中修改。Dify/Notion 仅从新建入口隐藏，历史连接器、详情与后端能力保持兼容。修复普通用户进入知识库页面时误加载管理员专用类型和分块策略导致的错误提示，并在可访问知识库列表中补充创建时间和真实文件数，避免卡片因字段缺失统一显示“0 文件”。

- 管理员与 superadmin 的“智能体扩展”页面隐藏工具和 MCP 管理入口，并移除 MCP 详情直达路由；Agent 配置仍保留工具/MCP 的选择、刷新和运行时能力，后端 API 不变。管理员、superadmin 与普通用户的主页均隐藏工作区入口，直接访问 `/workspace` 会返回对话页。所有用户的设置弹窗同时隐藏 API Key 和环境变量页面；API Key 认证、CLI 登录/退出、外部系统调用及 Agent 沙盒环境变量注入能力继续保留。内置 Skill 的启动同步策略收敛为仅启用 `knowledge-base`，`image-gen`、`deep-research`、`mysql-reporter` 默认关闭，自定义 Skill 状态不受影响；内置深度研究 Agent 改为使用自包含编排 Prompt，不再依赖已关闭的 `deep-research` Skill，且启动时不再自动创建；系统启动时仅自动创建主智能体“智能助手”，并保留其默认依赖的“通用任务”子智能体；“网页检索”和“深度研究”不再自动创建。新建“智能助手”时仅选择“向用户提问”和“OCR 解析文件”两个内置工具、MCP 默认全不选、Skill 默认选择当前用户可用的全部项、子智能体默认为“通用任务”；管理员后续保存的资源选择保持不变。

- Core Ontology 内置预设升级为多版本不可变治理：已发布的 `shanli-preset@4.2` 保留原始文件和 digest 作为历史兼容版本，新发布 `shanli-preset@4.3` 并作为新知识库默认项。V4.3 按业务基线恢复为精确 22 个实体、22 个关系，补回 `Company`、`Brand` 与 `HAS_SPEC` 关系词典，将全部关系说明移入实际参与抽取 Prompt 的 `schema.json`；新证据关系统一使用 `SUPPORTED_BY`，`HAS_EVIDENCE` 仅保留为无别名的历史兼容关系。属性模型新增实体 owner、枚举值和业务说明，并贯通 Bundle 加载、Prompt、抽取校验、详情 API 与管理界面；已有 V4.2 知识库不会自动迁移，切换到 V4.3 前仍须显式重置图谱和抽取结果。

- Core Ontology 内置预设收敛为 `tongyong@1.0.0`“通用”和 `shanli-preset@4.2`“善理预设新版”两项：移除旧版善理 `1.0.0` 与空的 V4.1 scaffold，将现有通用业务本体固化为内置默认项；善理 4.2 保留稳定身份与 digest，仅更新展示名称，避免已有知识库配置和抽取缓存失效。

- 知识图谱抽取新增版本化 Ontology Registry：Core Ontology 结构、实体别名、关系映射和属性字典分别由 `schema.json`、`entity.yaml`、`relation.yaml`、`property.yaml` 管理；内置 `V4.1` 框架和可直接使用的“善理预设”，后者保留 `1.0.0`，并新增 `shanli-preset@4.2` 内置版本。V4.2 在产品知识层之外增加卖点、定位、路线图、案例、兼容目标和能力等业务知识实体及对应关系，两个版本可并存并按 `registry_id + version + digest` 精确解析。admin 可进入设置页查看所有 Ontology 的完整实体、关系、别名、属性和附加规则；superadmin 可通过业务表单创建，并在未被任何知识库配置或抽取结果引用时覆盖自定义 Ontology 的当前版本，内置预设始终只读。覆盖使用 expected digest 防止并发修改，生成新 digest 并原子切换当前内容，不自动清图或更新知识库引用。高级入口仍支持上传四文件 ZIP Bundle；所有入口共享严格校验并保存到 API/worker 共享的 `ONTOLOGY_REGISTRY_DIR`。知识库图谱配置按 `registry_id + version + digest` 精确选择，已有索引、真实抽取缓存或图谱数据时仍必须先显式重置才能切换 Core Ontology；修复 chunk 的 JSONB `null` 被误计为抽取结果，导致从未执行图谱抽取的知识库无法首次选择 Core Ontology 的问题。历史自由文本 Schema 保持兼容，文档解析、OCR、分块和入库接口不变。

- 新增企业微信自建应用消息回调：使用共享的权限感知检索服务，仅查询用户拥有 `can_search` 权限的知识库并保留知识库/文件来源；支持签名校验、AES 加解密、以企业微信 `UserID` 匹配本地 `UID`，未绑定用户不返回知识库内容；补充回调环境变量、单元测试和部署清单。

- 修复知识库列表与工作区文件浏览未统一操作级权限的问题，并将“智能体扩展 → 知识库”开放给普通用户：用户通过权限面板获得 `can_view` 后，可在扩展页和工作区看到知识库、浏览目录并预览文件；`can_upload` 允许编辑者上传文件并添加为“待审核”记录，但不会触发解析或入库。系统 `admin/superadmin` 对知识库拥有完整操作权限，可创建文件夹、处理待审核文件、执行解析和入库、下载文件并访问检索测试与知识图谱；普通成员下载仍需单独授予 `can_download`，且不能创建文件夹或处理文件。知识库详情下载与工作区统一复用原文件读取逻辑，兼容仅保存 `minio_url` 的文件记录。权限设置校验 `can_grant`，知识库详情页继续隐藏知识导图、RAG 评估和评估基准入口。

- 修复线程文件接口的同步文件 I/O 阻塞：交付物预览仅异步读取媒体类型识别所需的 512 字节文件头，不再同步加载完整文件；线程文件全文读取和目录扫描下沉到工作线程，避免大文件或大目录并发访问时阻塞 API 事件循环。
- 修复应用 lifespan 关闭时未释放共享 Neo4j driver 的问题，避免同进程重载或重复启动后残留图数据库连接。
- 修复删除 Milvus 知识库阻塞事件循环：`MilvusKB.delete_database` 恢复异步基类契约，并将同步的主集合与图集合清理下沉到工作线程，避免删除期间阻塞其他对话和 SSE 推送。
- 修复 Agent 对话流式输出时的前端性能问题：自动滚动改为监听 `conversations` computed 的顶层引用变化，不再对完整对话与消息树执行深度 watch，避免每个 token 到达时递归遍历全部历史消息。
- 修复删除知识库文件图谱时清理范围过宽：Neo4j 仅删除本次文件 `MENTIONS` 边触及且已无任何 `MENTIONS` 引用的实体，不再顺带删除同知识库内其他文件遗留的孤儿实体。
- 对照当前解析器、知识库工具和 Agent 运行链路重整正式文档：补充默认 OCR、文件级处理参数、工作区 `AGENTS.md` / `USER.md` / `MEMORY.md`、知识库 `knowledge-base` Skill、`search_file`、`ocr_parse_file`、子智能体进度和图片 OCR 回退语义；更正知识库工具使用 `kb_id`、MCP 配置按数据库实时读取等过时描述；移除正式文档中的问答式栏目。
- 统一用户菜单的设置入口：管理员与普通用户均显示“设置”，打开后默认进入账户设置；管理员专属的基本设置、用户管理等标签继续按原权限展示。
- 工作区 `agents` 目录新增 `USER.md` 与 `MEMORY.md` 上下文文件，并与 `AGENTS.md` 一起在 Agent 运行开始时加载；三个默认文件首次创建时均写入对应标题和说明，不再生成空文件，已有内容保持不变。
- 新增 Summary 上下文压缩实时状态流式同步：`AI知识库SummarizationMiddleware` 触发压缩时通过 `langgraph.config.get_stream_writer()` 推送 `yuxi.context_compression` 自定义事件（started/completed/failed），复用 DeepAgents 已有 `_summarization_event` 作为完成数据源；`base.py` 通过 `astream_events(version="v3")` 的 `CustomTransformer` 透传 custom 流，`chat_service`/`agent_run_service` 将事件映射为 `context_compression` chunk 并透传到前端；前端收到 `started` 时将"正在生成回复"加载态文案切换为"正在压缩上下文"，压缩结束（`completed`/`finished`）即切回，不额外渲染分隔符、不保留压缩完成态。为避免摘要 LLM 调用的 token 流被 LangGraph messages stream 捕获并广播成 phantom 摘要消息，重写 `_create_summary`/`_acreate_summary` 在摘要模型 invoke 的 config 上挂 `TAG_NOSTREAM`，让流式层在源头跳过该调用，主 messages 流天然只含用户可见回复，无需 `chat_service` 下游过滤（参考 DeerFlow 实现）。异步 L2 压缩路径的 `_aoffload_to_backend` 与 `_acreate_summary` 改回 `asyncio.gather` 并发执行，与 DeepAgents 父类一致，避免串行等待一次文件 I/O 与一次摘要 LLM 调用；两路复用 `_SUMMARY_SANITIZED_MESSAGES` 的 id 缓存。L1-only 调用若仍触发 provider context overflow，会回落到 L2 summary 后重试；`summary_tool_result_token_limit` 默认改为 300，并同时作为 L1 工具结果 offload 阈值和预览上限，L2 只消费 L1 视图，不再对工具结果做第二轮 offload；L2 摘要模型的待摘要历史输入上限改为与 `summary_threshold` 对齐，避免固定 4000 token 裁剪丢失早期历史；新增 `summary_l2_trigger_ratio` 管理 L1 后进入 L2 的比例阈值，默认 `0.4`。

- 知识库详情页新增整页内容加载态：切换或首次进入详情时，在知识库信息返回前仅展示居中 loading，避免标题、标签页和文件区域先渲染旧数据或空状态。
- 修复知识库文件处理中频繁刷新时，旧目录请求覆盖当前子目录列表并造成列表抖动的问题。
- `InfoCard` 新增统一的 `card-more-action-corner` 菜单插槽，并在组件内部固定渲染横向三点按钮；更多操作从卡片绝对定位改为进入 header 的正常 flex 布局，与图标、标题和 `status` 共享同一垂直中心线，业务页面只能提供菜单内容；智能体、知识库和用户管理卡片均改为复用该组件与菜单能力，用户部门/角色标签使用现有 `status` 插槽展示在标题区右侧，菜单图标与文字使用统一行高居中，知识库菜单支持复制 ID、直接打开编辑弹窗，以及确认后删除并刷新列表。
- 智能体管理页的普通智能体卡片新增“去对话”入口，点击后进入新建对话并预选对应智能体；子智能体卡片不展示该入口。
- 修复 API/Worker Docker 镜像构建失败：后端项目要求 Python `>=3.12,<3.14`，Dockerfile 基础镜像与 `.python-version` 同步到 `python:3.13-slim`，并将 `docker/api.Dockerfile` 的 `COPY` 源路径改为相对仓库根目录的 `backend/...`，与 `docker-compose` 中 `build.context: .` 保持一致；同时移除 `uv sync` 对 BuildKit `--mount` 的依赖并启用 `--no-cache`，避免分别因 Python 版本不兼容、`../backend/...` 越出 build context、未启用 BuildKit 或 uv 缓存残留导致镜像构建失败或体积膨胀。
- 新增用户级配置：保留现有全局配置链路不变，新增 `user_config` 表、`UserConfigSchema` 与无缓存的 `UserConfig` PostgreSQL 读取/保存入口；新增 `/api/user/config`，所有登录用户可读写自己的配置。首个字段为 `enable_memory`（是否启用 Memory），作为预留开关仅持久化与展示，不接入运行逻辑；设置弹窗新增“用户配置” Tab 展示并保存该开关。
- 优化 Skills 管理页展示文案：补充推荐 Skills 与内置 `mysql-reporter` 的卡片描述，避免短描述在两行卡片布局下显得过空。
- 新增 PaddleOCR 云端 API OCR 解析器：支持 `paddleocr_vl_1_6` 调用 `PaddleOCR-VL-1.6` 输出版面 Markdown，支持 `paddleocr_pp_ocrv6` 调用 `PP-OCRv6` 输出纯 OCR 文本；解析器复用 PaddleOCR jobs 提交、轮询与 JSONL 下载逻辑，健康检查仅校验 `PADDLEOCR_API_TOKEN` 配置状态，不创建真实 OCR 任务；知识库上传与临时附件解析弹窗同步增加两个 OCR 选项。
- 优化对话消息代码块交互：助手消息中的 Markdown 代码块右上角新增简约复制按钮，支持点击快速复制代码内容并显示短暂“已复制”反馈。
- 新增 Markdown `html:preview` 辅助可视化预览：仅显式标记的围栏会渲染为 sandboxed iframe，普通 `html` 继续展示源码；预览使用清洗后的静态 HTML/CSS `srcdoc`，按内容自适应高度并最高限制为 700px，超高时保留 iframe 内滚动，流式输出期间复用预览节点避免闪烁；内置 Agent Prompt 同步约束 Markdown 仍为回答主体，HTML 只补齐指标、对比、时间线、关系结构等可视化短板，不承载大段叙事、完整报告或正文解释。
- 新增历史对话搜索：侧边栏增加“搜索对话”入口，打开命令面板式弹窗，支持默认最近对话、新对话入口、搜索中骨架屏、结果列表、方向键选择与 Enter 跳转；后端新增 `/api/chat/threads/search`，按当前用户 active 对话中的非工具消息 `content` 检索并按对话聚合返回命中片段，同时将侧边栏导航项高度统一调整为 32px。
- 模型供应商管理前端开放 Anthropic provider type：Provider Type 下拉仅保留 OpenAI Completions API 与 Anthropic Messages API 两种可选项，保存值继续使用后端枚举，并在供应商卡片中展示友好类型名称。
- 优化 Agent 状态面板子智能体弹窗：弹窗消息列表复用对话消息渲染路径，打开运行中的子智能体时会展示主 run SSE 已路由到 child thread 的流式消息，并在生成中保持与主对话一致的处理态；修复当前 run 的历史半成品消息与 ongoing 流式片段叠加导致同一个子智能体在主对话中重复展示的问题，子智能体状态查询工具不再渲染成独立 Agent 卡片，弹窗会随子智能体条目补齐 run_id 后订阅对应 SSE，并复用主对话的流式平滑输出与底部跟随滚动控制；已完成的子智能体改为直接读取持久化 Message 历史，不再从 Redis run event 重放渲染。
- 增强异步子智能体 `subagent_status`：状态查询会从子 run 的 Redis 事件流反向提取最近 3 条可读进度摘要，并在工具卡中优先展示，终态结果读取语义保持不变；同时移除模型侧 `subagent_events` 工具，Redis 原始事件流继续仅供运行基础设施与前端 SSE 使用，避免包含重复 metadata、query 与嵌套 payload 的事件信封进入模型上下文并被写入 `large_tool_results`。
- 优化任务中心（Tasker）定位为「后台作业实体 + 只读进度面板」。前端修正失效的任务类型标签、状态判断收敛、任务详情补充参数/结果，并把轮询收敛到 store 修复抽屉关闭后角标不更新；后端 `TaskContext` 暴露 `payload` 消除私有穿透，进度更新按增量节流降低写放大，新增终态任务保留上限自动裁剪内存与数据库，`_load_state` 恢复历史任务使任务中心重启后仍可见。图谱构建任务迁移到现有 ARQ worker，API 仅持久化任务并投递 `task_id`，worker 从 PostgreSQL 恢复参数、更新进度与检查取消请求，因此 API 热重载不再中断构建；同一知识库的活跃任务创建通过数据库事务锁防止重复投递。图谱构建存在失败或剩余 Chunk 时不再误标为成功，会保留成功/失败/剩余及失败 Chunk 摘要并返回真实错误；图谱状态稳定选取最新持久化任务，区分失败与取消，前端可查看结果统计并对仍有待处理 Chunk 的失败或取消任务重新索引。Tasker 关闭时不再持锁等待 worker 取消，服务重启恢复出的内存任务会补齐错误和完成时间，同时不会误判由 ARQ 管理的图谱任务。
- 知识库访问能力迁移为内置 Skill：新增 `knowledge-base` Skill，绑定 `list_kbs`、`query_kb`、`find_kb_document`、`open_kb_document`、`get_mindmap` 等知识库工具；内置 Agent 不再默认挂载知识库工具，改为读取并激活 Skill 后按需加载，同时保留 `knowledges` 作为知识库资源范围与权限边界。Agent 配置页在启用知识库但显式未选择 `knowledge-base` Skill 时实时展示提示，保存时不阻断。修复 Skill 依赖工具的可执行性：`create_agent` 中「模型可见工具」与「ToolNode 可执行工具」是两套，仅靠 `awrap_model_call` 动态追加工具只会绑定给模型、不进 ToolNode，导致激活 Skill 后调用 `list_kbs`/`query_kb` 报 `not a valid tool`；现由 `resolve_configured_runtime_tools` 统一把所有可见 Skill 依赖的本地工具随基础工具一起注册进 ToolNode（可执行），`SkillsMiddleware` 运行期再按 Skill 激活状态门控模型可见性（保持按需加载）。新增 `search_file` 工具支持按文件名关键词跨/指定知识库搜索文件，并已加入 `knowledge-base` Skill 的依赖工具；其分页统计基于全量扫描结果计算 `total`/`has_more`，避免按 `limit+offset` 截断导致计数失真。
- 增强知识库工具结果豁免：`open_kb_document` 工具结果加入 Summary 卸载豁免名单，避免大文档窗口被摘要后丢失上下文。
- 新增 AI知识库 Python CLI 首版底座：新增独立 `packages/ai-kb-cli` 包，提供 `remote add/use/list/ping`、`login --browser`、`login --api-key`、`whoami`、`status`、`logout`；配置统一写入 `~/.ai-kb/config.toml`，remote URL 只保留实例入口并派生 `/api` 请求路径。后端新增 `/api/auth/cli/sessions` device flow 授权接口与 `cli_auth_sessions` 持久表，浏览器确认后为当前用户创建一次性返回的 API Key；新增公开 `/api/system/discovery` 声明服务端版本、API 前缀、CLI 能力和关键端点，CLI 登录前校验服务端版本至少为 `0.7.1`（`0.7.1.dev*` 按 release tuple 兼容）及对应能力；前端新增 `/auth/cli/authorize` 授权确认页。补充 CLI 本地单测与后端服务/路由单测。
- 安全与健壮性加固：token 兑换接口改为 `POST /api/auth/cli/sessions/token`，`device_code` 改走请求体，避免凭据出现在访问日志的 URL 路径中；兑换与批准会话时对会话行加 `with_for_update` 行锁，防止并发/重试导致重复签发 API Key；CLI 浏览器登录轮询区分瞬时错误（网络层错误、5xx）与终止错误，瞬时错误继续重试而非中断整个登录；`config.toml` 以 `0600` 原子创建并对名称等写入值做引号/反斜杠转义，避免明文凭据短暂可读及特殊字符破坏配置；API Key 认证在绑定用户失效时改为直接拒绝，不再 fallback 到部门管理员或 superadmin，创建 API Key 时校验部门与关联用户一致，用户软删除会同步禁用其 API Key；进一步要求 API Key 必须绑定具体用户，启动 schema 演进会清理历史未绑定用户的 API Key 并将 `api_keys.user_id` 收紧为非空；Dashboard 管理接口与前端入口改为仅 superadmin 可访问；用户软删除脱敏名改用用户主键生成，避免短哈希碰撞触发唯一索引冲突；前端授权页新增确认提示与对结构化错误 `detail` 的兼容渲染。
- 收敛 API Key 生成逻辑：移除独立 API Key 生成服务，统一通过 `AuthUtils.generate_api_key()` 生成 CLI 授权与用户管理中的 API Key。
- 收敛认证模块命名：CLI 浏览器授权路由合并到 `auth_router.py`，授权会话服务迁移到 `auth_service.py`。
- 为 CLI 知识库上传补齐后端接口边界：discovery 新增 `cli.kb_upload` 能力声明；普通文件上传接口在传入 `kb_id` 时先校验知识库存在且支持文档，校验通过后才读取文件或写 MinIO；新增同步 `POST /api/knowledge/databases/{kb_id}/documents/add`，用于把已上传的 MinIO 文件添加为知识库文档记录但不解析、不入库、不进入 Tasker；新增 `GET /api/knowledge/databases/{kb_id}/documents/exists?filename=...`，用于上传前按文件名或相对路径检查知识库内是否已有同名文件；旧 `/documents` ingest 入口保留兼容，但在 enqueue 前补充空 items、非 MinIO URL 与缺失 content hash 的请求级校验。
- 新增 `ai-kb kb upload` 上传命令：默认仅包含 `.md/.txt/.docx/.html/.htm`，省略 `--kb-id` 时会从 remote 拉取并只展示支持文档上传的知识库，支持非全屏的方向键单选知识库与多选文件类型；支持 `--include-ext/--exclude-ext` 与 `--concurrency` 控制本地并发队列，并发默认 10、上限 300；交互终端上传阶段显示进度条，非交互输出保留文本进度；每个并发单元默认会先按相对路径调用 `/documents/exists` 检查知识库中是否已有文件，存在则直接跳过，传入 `--force-upload-file` 时跳过该预检并完全依赖上传接口的重复文件校验；单文件上传成功后立即调用 `/documents/add` 添加该文件记录，不触发解析/OCR/入库；目录上传通过 `source_paths` 保留相对路径，后端创建文件记录时使用该路径作为展示文件名以保持前端目录层级；上传接口返回“同内容文件已存在”时按已上传过跳过，不再作为错误展示；大批量上传调度改为有界提交，避免数十万文件时一次性创建全部 future 导致资源峰值过高。
- 发布 `ai-kb-cli` 到 PyPI，并新增 GitHub Release 触发的 PyPI Trusted Publishing 工作流；文档新增命令行工具使用说明；CLI 运行访问 remote 的命令前会先输出当前 CLI 版本、remote 名称和 URL。
- 修复知识库文件入库/解析成功却被统计为失败（#793）：成功的文件元数据会固定携带 `error: None`，而后台任务此前以「结果中是否存在 `error` 键」判定失败，导致成功项也被计入失败数并在全部成功时仍抛出「处理完成，失败 N 个」。改为统一通过 `_is_failed_item` 按「显式 `status == failed` 或非空 `error`」判定，覆盖入库、解析、单独解析/入库三处统计。
- 修复 Windows 初始化脚本自动生成 JWT 配置失败（#804）：`init.ps1` 改用 Windows PowerShell 兼容的 `RandomNumberGenerator.Create().GetBytes(...)` 生成随机字节，避免旧 .NET 环境缺少 `RandomNumberGenerator.Fill()` 导致按 Enter 自动生成时报错。
- 优化知识库文件列表状态流转与文件预览边界：`uploaded/parsed/error_parsing/error_indexing` 状态分别展示解析、入库或重试操作；源文件预览与解析后的 Markdown 查看分离，txt/图片/Markdown/HTML/PDF/代码类按源文件类型预览；Office 源文件仅支持 `.docx/.pptx`，点击预览时按需生成并缓存 PDF 预览内容，由同一个预览接口直接返回，不再把解析 Markdown 产物当作源文件预览。
- 收敛知识库分块策略选项来源：后端以单一 `CHUNK_PRESETS` 配置派生 preset id、描述和选项列表，并新增 `/api/knowledge/chunk-presets`；前端分块策略选择器改为通过接口读取选项，避免前后端重复维护同一份文案。
- 优化大规模知识库文件列表加载：知识库详情接口默认不再返回全量 `files`，新增按 `parent_id/path_prefix/page/page_size/status` 查询的轻量文件列表接口；前端文件管理页改为目录懒加载与服务端分页，后端按 `source_path`/路径型文件名聚合虚拟目录，列表项只保留交互所需字段，顶部统计改用后端聚合结果，避免数十万文件场景下前端全量建树和传输压力。工作区知识库文件浏览统一改用同一套分页懒加载查询，支持真实目录和虚拟目录页码分页，非文档型知识库不再出现在工作区文件源中；文件浏览组件和后端列表接口均不再承载文件名搜索，后续搜索能力由独立后端接口和组件实现；文件列表展示抽出共享 `FileBrowserTable`，知识库详情和工作区共用展示层，并移除原知识库文件列表拖拽移动入口。
- 优化知识库启动元数据加载：服务启动时不再把全部 `knowledge_files` 记录加载进 `self.files_meta`，文件解析、入库、预览、下载、打开内容等单文件操作改为按 `file_id` 从数据库懒加载；文件状态流转改为通过数据库窄字段更新和状态条件更新完成，移除进程内处理队列修复逻辑，避免 api/worker 多进程下出现虚假的状态修复；文件统计刷新改用数据库聚合，文件大小补全从启动阶段移入显式统计修复任务，并收敛处理参数合并日志，避免大规模文档场景下启动内存和日志压力随文件数线性放大。
- 调整知识库待处理统计卡行为：文件管理顶部“待解析/待入库”统计卡从状态筛选改为提交对应后台处理任务；新增按待处理状态批量解析/入库接口，任务内按 500 条游标分页读取文件 ID，避免前端一次拉取和提交海量 ID；显式选中文件解析/入库接口增加 1000 个 ID 的单次上限。
- 修复大规模知识库统计修复失败：`repair_missing_file_stats` 不再对未入库文件查询 chunk 表，未入库文件残留的 chunk/token 统计会归零；chunk repository 的批量 `IN` 查询统一分批执行，避免 asyncpg 单条 SQL 参数超过 32767。
- 优化思维导图构建接口设计，支持增量构建和更新：新增 GET /mindmap/diff 接口检测文件变更，POST /mindmap/generate 新增 incremental 参数支持增量更新；纯删除场景无需 AI 调用（递归树手术），新增文件时 AI 整合进现有分类结构；思维导图文件加载改为显式 repository 查询，增量 diff 会按已追踪 file_id 补查分页外文件，避免把分页文件列表误当全量文件集；前端导图 Tab 新增"增量更新"按钮和变更数量 badge。修复删除文件后知识导图仍展示旧内容：单文件删除接口成功后调用 `remove_file_from_mindmap`、批量删除接口成功后调用 `batch_remove_files_from_mindmap`，同步移除导图快照中对应叶子节点，无需用户再手动增量更新。
- 优化文档结构与智能体运行说明：项目简介去除对 LangGraph 具体版本的强调；中间件文档按当前内置 Agent 链路重写，补充知识库工具、Skills 激活、附件/文件系统、子智能体 task、Summary 上下文压缩与工具结果卸载机制；知识库文档补充知识导图与示例问题生成机制；Langfuse 集成文档从“智能体开发”移动到“高级配置”分组。
- 移除知识库普通上传接口遗留的 `allow_jsonl` 参数，上传类型判断统一依赖 `SUPPORTED_FILE_EXTENSIONS`；评估数据集 JSONL 继续通过独立评估接口上传。
- 修复 Dependabot esbuild 告警：web 与 docs 统一锁定 `esbuild@0.28.1`，docs 同步升级 Vite/Vue 插件 override 并固定 pnpm 版本，避免旧锁文件继续解析到存在漏洞的 esbuild 版本。
- 修复 CORS 与依赖安全告警：后端 CORS 改为通过 `YUXI_CORS_ORIGINS` 配置允许来源，开发环境默认仅允许本机前端端口，生产环境未配置时不开放跨域，显式使用 `*` 时会关闭 credentials；同步刷新前后端锁文件，将 `aiohttp`、`cryptography`、`langchain`、`langchain-anthropic`、`pypdf`、`python-multipart`、`starlette`、`pyjwt`、`torch`、`torchvision`、`dompurify`、`js-yaml`、`markdown-it`、`vite` 升级到安全版本。
- 修复添加/编辑 MCP 弹窗中环境变量无法新增的问题：环境变量编辑器存在 rows -> object -> rows 的双向同步回环，`modelValue` 变化时会完全根据已有 key 重建行，导致只填了 key 的行（含刚点击「添加变量」生成的空行）被过滤掉而无法新增；现在仅当传入值与组件自身 emit 的内容不一致时才重建行，避免回声覆盖未填 key 的行。
- 修复模型与知识库后端导入循环：`yuxi.models` 改为惰性导出模型选择函数，知识库可见范围和知识库工具延迟读取全局 `knowledge_base` 实例，避免单测、热重载或轻量导入知识库包时因模块尚未完成初始化而失败。
- 修复知识库创建权限持久化一致性：创建知识库时由 Manager 归一化 `share_config/created_by` 后作为受控记录字段随首次知识库元数据插入写入数据库，避免先插入基础记录再二次更新权限字段产生短暂不一致。
- 修复 HTML 预览 iframe 高度问题：侧边预览模式改为 `height: 100%` 适应父容器，避免底部内容裁切；全屏预览模式移除 `min-height: calc(80vh - 40px)`，避免短内容下方白边；iframe 设为 `display: block` 消除行内基线间隙导致的底部白边；全屏渲染改用独立 `srcdoc`（不注入 `zoom`）按 100% 显示，侧边预览仍保持 0.75 缩放。
- 对话消息图片支持点击全屏预览：对话中用户上传的图片支持点击放大查看，复用文件预览的全屏蒙层交互（Teleport 蒙层，点击图片/空白处或按 Esc 关闭），不引入额外依赖。
- 新增 Agent token usage 状态快照，在状态面板中作为普通可折叠分组展示完整 `messages`、当前传给 LLM 的 `messages`、system/tools 构成、输入构成堆叠条和上下文窗口占用估算。
- 优化 Agent token usage 状态面板展示：后端补充 LLM 内容消息与工具消息的 token/count 拆分字段，前端将内容消息、工具消息、系统消息与工具定义分开展示，并修正上下文窗口/剩余信息换行与对话流式输出期间的底部跟随滚动。
- 收敛 Agent `read_file` 多模态边界：仅 UTF-8 文本和图片可读，PDF/Office 文档会引导使用 `ocr_parse_file` 转为 Markdown，音视频及未知二进制不再注入模型消息；OpenAI 兼容链路的 tool-role 图片桥接从私有 payload 覆盖迁移到公开模型中间件，Provider 明确拒绝图片输入时会自动调用 `ocr_parse_file` 提取文字，并在后续请求中移除同一张历史图片，避免文本模型重复报错。
- 新增默认 OCR 解析引擎配置 `default_ocr_engine`，普通登录用户可读取系统配置；知识库上传弹窗与临时附件解析弹窗默认选中系统默认 OCR，解析入口仅在未显式传入 `ocr_engine` 时使用该默认值。修复读取该配置时因反向导入知识库模块导致配置初始化循环、并中断后续配置加载的问题；OCR 注册表改为轻量模块，知识库单例迁移到显式 runtime 入口，解析器调用方直接导入真实定义模块，包初始化不再加载运行对象。
- 新增 Agent 内置 `ocr_parse_file` 工具：只允许解析 `/home/gem/user-data/{workspace,uploads,outputs}` 下的沙盒虚拟路径文件，使用指定或系统默认 OCR 引擎生成 Markdown，并把结果写入 `outputs/ocr/*.md`；工具返回结果文件路径、字符数和短预览，不写入知识库 MinIO，也不创建知识库文件记录。
- 收敛 Agent Invocation 服务边界：新增 `agent_invocation_service.py` 承接 agent-call/eval 的外部调用语义、同步等待、异步响应与 OpenAI-compatible 响应装配；`agent_invocation_router.py` 收敛为 HTTP 适配层，`agent_run_service.py` 只保留通用 AgentRun 生命周期能力，`subagent_run_service.py` 改为调用公开 AgentRun 创建 API，不再穿透私有函数。
- 修复 Agent 状态读取与消息落库在重新读取 LangGraph checkpoint 时未传入运行时 context 的问题，避免主智能体或子智能体线程因系统默认模型已不可用而查询状态/保存历史失败；模型供应商管理页新增默认模型保护，阻止删除、停用默认模型所属供应商或移除当前默认模型。
- 优化 Agent 上下文压缩：AI知识库 的 DeepAgents summary adapter 在生成 summary 与写入 conversation history 时，会先对本次模型调用的临时消息视图执行 L1 结构精简，截断旧 `write_file`/`edit_file` 大参数，并把超过阈值的大 `ToolMessage.content` 写入 `outputs/large_tool_results` 后替换为路径和有限预览；L1 不修改 LangGraph state 原始消息，L1 后若上下文低于入口阈值的 40% 则直接调用模型，不生成 summary event，仍超过时才进入 L2 summary。L2 继续使用 DeepAgents `_summarization_event.cutoff_index` 重建 effective messages；Summary 阈值判断改为使用 AI知识库 自己的近似 token 计算结果，不再根据 provider `usage_metadata.total_tokens` 或 usage scaling 提前触发；首次写入 `conversation_history` 前读取旧文件的 sandbox 404 会按 `file_not_found` 处理，不再产生误导性 warning；`present_artifacts` 会拒绝展示 `large_tool_results` 与 `conversation_history` 等工具调用阶段文件。新增管理员可配置项 `summary_keep_messages`、`summary_prompt`、`summary_tool_result_token_limit` 与 `max_execution_steps`，分别控制摘要后保留消息数、摘要提示词、summary 阶段工具结果预览上限和 LangGraph `recursion_limit`。
- 收敛普通聊天模型加载链路：`select_model` 保留旧 `.call()` 调用契约，内部改为通过 LangChain chat model adapter 复用 Agent 侧模型加载器，统一 OpenAI-compatible、Anthropic 与 Gemini 等 provider 的运行时适配；移除旧 `OpenAIBase` wrapper，默认重试策略迁移为 LangChain provider 参数。
- 统一 Redis 客户端管理：新增 `yuxi.storage.redis` 作为 Redis 配置、短生命周期同步客户端、共享异步客户端与 ARQ RedisSettings 的唯一基础设施入口；运行队列、系统配置快照同步、模型缓存和 worker 不再各自散落读取 `REDIS_URL` 或直接创建 Redis 客户端，Redis 连接失败日志统一使用脱敏 URL。
- 新增系统配置 Redis 快照同步：管理员保存配置时仍以 `saves/config/base.toml` 作为唯一持久化来源，成功写入后将可运行时同步的公开配置字段写入 `yuxi:runtime_config`；API 与 worker 进程在启动时各拉起一个后台同步线程，按 5 秒间隔从快照刷新内存值，读取端按普通属性访问、无需感知，Redis 不可用时继续使用当前内存值。`save_dir` 是启动期内部路径配置，不在管理员配置中展示、不从 `base.toml` 读取、不写入 Redis 快照且不支持通过管理员配置接口修改；sandbox 相关配置仍属于启动期敏感配置，运行中的已初始化组件不承诺完整热更新，修改后仍需重启保证生效；移除已无运行时调用点的 `enable_reranker` 与 `default_agent_id` 配置字段。
- 优化 FastAPI 请求链路并发能力：Milvus 知识库检索中的同步 embedding、向量/BM25/混合检索调用，以及图谱查询中的同步 Milvus/Neo4j 读操作（含连接建立）统一通过有界 `asyncio.to_thread` 在线程中执行，避免阻塞 API 事件循环；并发上限按事件循环懒加载信号量控制，不改变检索默认行为与参数上限。
- 修复 AgentRun worker 在 LLM 流式响应期间长期占用 PostgreSQL 连接：chat 与 resume 在完成运行时解析、会话和附件等预处理后，进入流式执行前显式提交事务并归还业务连接，最终消息保存时再按需获取连接。
- 修复异步文档解析阻塞 API 事件循环：DOCX、PPTX、XLS/XLSX、DOC、CSV 与 HTML 的同步转换统一下沉到工作线程，文本读取改用异步文件 I/O；Docling 单例转换增加线程互斥，避免并发解析共享转换器，并补充事件循环可继续调度的回归测试。
- 改进 OpenAI 兼容提供商流式工具调用兼容（替代 v0.7.0 的按 provider 禁流式处理）：根因是 LangGraph v3 流式累积对 tool_call 字段“后值覆盖”，SiliconFlow、阿里云百炼等在参数续片里把 `name`/`id` 下发为空字符串覆盖首片真实值。改为 `_ToolCallChunkFixChatOpenAI` 把续片空串 `name`/`id` 归一化为 `None`，对所有 OpenAI 兼容 provider 通用生效且保留流式，移除原 `_NON_STREAMING_TOOL_CALL_PROVIDERS` 名单。
- 新增 Agent 评估运行入口：`POST /api/agent-invocation/eval/runs` 会创建正常对话与 AgentRun，复用 worker 执行链路，并以 `source=agent_evaluation` 与 `agent_invocation_meta.evaluation` 标记写入 conversation、AgentRun 输入消息与 Langfuse trace；接口阻塞至运行结束后直接返回最终结果（状态、最终 assistant 输出、Langfuse trace id），并支持通过 `include_trajectory_summary` 按需返回轻量工具调用轨迹摘要。`ai-kb-cli` 新增 `ai-kb agent eval` 命令，用于从 Langfuse 数据集读取输入并回传实验输出
- 对话消息点赞/点踩反馈接入 Langfuse score：本地 `MessageFeedback` 保存成功后，如助手消息已关联 Langfuse trace，则同步写入 `user-feedback` score，点赞为 `1`、点踩为 `0`，点踩原因写入 comment，便于在 Langfuse 中按用户反馈筛选 trace。
- 新增外部系统 Agent 调用入口：独立 `agent-invocation` router 提供 `POST /api/agent-invocation/agent-call/runs` 与 `POST /api/agent-invocation/agent-call/runs/result`，字段沿用 AI知识库 命名（`agent_slug/thread_id/request_id/model_spec`），复用 AgentRun 队列和结果读取能力；支持非流式同步等待或 `async_mode=true` 立即返回 `run_id`，Agent Call 不允许通过 `agent_call_meta.context` 覆盖 Agent context，运行时模型覆盖只允许走独立 `model_spec`；修复无 `thread_id` 且模型校验失败时提前提交空对话，导致孤儿对话和 `request_id` 失败重试非幂等的问题；Agent Call 的 `messages[].content` 兼容 OpenAI 风格的 `text`/`image_url` 多模态数组，纯文本数组不再误报 422，图片输入会保留原始 LangChain 多模态消息供 AgentRun worker 恢复；Agent Eval 与 Agent Call 统一通过 conversation-backed invocation helper 创建 run，后续定时任务等入口只需做请求解析和结果出口适配。
- 修复 Agent Invocation 创建的 eval/call 对话进入用户对话导航的问题：侧边栏最近对话与对话搜索会按 conversation metadata `source` 排除 `agent_evaluation` 与 `agent_call`，保留 run/conversation 持久化与结果追踪能力。
- 下沉 AgentRun 基础能力：将「读取某个 run 的最终结果」（`get_agent_run_result`/`load_agent_run_result`，含状态、最终 assistant 输出、Langfuse trace id 与错误）与「阻塞至 run 终结再取结果」（`await_agent_run_result`，复用有限事件流、无额外轮询）提升进 `agent_run_service`，供 chat/eval 及未来定时任务统一复用；eval 运行入口改为非流式复用该能力（不再做 SSE 封装），移除其私有结果构建逻辑（结果不变）。
- 重构 AgentRun 接口底座：`agent_run_service` 拆出内部 `create_agent_run`、`enqueue_agent_run` 与 `request_cancel_agent_run`，保留现有 `/api/agent/runs` 行为并新增 `/api/agent/runs/{run_id}/result` 结果读取接口；`AgentRunRepository` 增加按 `parent_agent_run_id` 查询 child run 的能力，为后续异步 subagent 生命周期控制预留统一入口。
- 修复子智能体流式事件兼容：AI知识库 task middleware 的 DeepAgents 子智能体 transformer 改用专用 `yuxi_subagents` projection，避免与 LangChain `create_agent` 默认注册的 `subagents` projection 冲突导致运行流式消息时报错；子线程路由收集优先读取 AI知识库 projection，并保留原 `subagents` fallback。
- 重构 AgentRun 与子智能体运行链路：保留现有 `/api/agent/runs` 行为并新增 `/api/agent/runs/{run_id}/result` 结果读取接口；子智能体新增 `subagent_start/status/cancel/await` 工具，支持后台启动、轻量进度查询、等待结果、取消运行和已完成 child thread 续跑；同一用户、同一子智能体、同一 conversation thread 存在运行中 run 时返回 busy，不做隐藏排队。
- 修复子智能体同步等待超时语义：`await_agent_run_result` 在有限 SSE 等待结束后会校验 run 终态，非终态时抛出明确等待超时；`task` 与 `subagent_await` 不再把仍在运行的子智能体误报为“已完成但无文本结果”，同步 Agent Call / Eval 入口遇到等待超时返回 504 和当前 run 快照。
- 收紧子智能体运行创建边界：`SubagentRunService` 显式拒绝以子智能体 run 作为父 run 创建新的子智能体，固化“不支持孙子智能体”的架构约束。
- 修复 AgentRun busy 检查的并发窗口：为同一用户、智能体和 conversation thread 的非终态 run 增加数据库部分唯一索引，并在插入冲突时返回现有 `run_busy` 结构，避免不同 `request_id` 并发启动绕过忙碌检查；AgentRun 创建冲突改用局部 savepoint 处理，避免 `_create_agent_run` 在共享 session 上 rollback 撤销调用方刚创建的子智能体线程关系或输入消息。
- 收敛 AgentRun 数据模型与输入语义：运行记录统一使用 `agent_slug`、`conversation_thread_id`、`created_by_run_id`、`input_message_id` 等字段，子智能体通过 `subagent_threads` 关系表维护 parent/child conversation 归属；补齐旧库升级时 `agent_runs` 旧字段到新字段、`subagent_threads.subagent_slug/created_by_run_id` 的静默回填与约束收敛，并在创建部分唯一索引前终结重复活跃 run，避免早期分支库保留 nullable schema 或历史重复活跃数据阻塞升级；Agent 状态中的 `subagent_runs` 改为以 `run_id` 作为执行身份，`resume` 请求字段明确为 `Command(resume=...)` 输入载荷。
- 精简旧链路与失败语义：恢复审批统一走 `POST /api/agent/runs` 的 `resume` 载荷，移除旧 `POST /api/chat/thread/{id}/resume` 流式接口和已废弃的 `chat_service.agent_chat`；子智能体运行缺少必要线程上下文时直接报错，状态查询只在真实缺失或无权访问时返回 404，内部运行记录格式异常返回 500。
- 统一流式事件线程 ID 提取契约：新增共享 `extract_thread_id` 工具，`BaseAgent`、聊天服务和 run worker 统一只读取规范化事件的一层稳定路径，并通过显式 fallback 处理父线程归属，避免递归扫描嵌套 metadata 导致父/子线程事件路由分歧。
- 检索模型运行时切换：`KnowledgeBase.update_database` 新增 `embedding_model_spec` 参数，可运行期切换库的嵌入模型（内外同款 bge-m3 向量兼容，无需重建索引），`models/providers/builtin.py` 新增 `local` 供应商（`http://local-inference:8000/v1`，bge-m3 1024 维 + bge-reranker-v2-m3），并补齐 `test_model_provider_service` / `test_knowledge_base_update` / `test_knowledge_router` 相关测试。
- 知识图谱检索性能优化：`MilvusGraphService.query_seed_subgraph` 的 Cypher 子图投影只返回 PPR 需要的标量字段（`element_id/name/chunk_id/entity_id/label` 与边 `source_id/target_id`），不再回传整棵子图的节点/关系全属性，图谱链路 Neo4j PPR 465ms→206ms（约 2.3×，语义与排序结果一致）；`storage/neo4j/manager` 启动时确保 `Entity.entity_id` 建索引。
- 拒答转人工（企业微信）：新增 `wecom_handoff_service` 与 `knowledge_handoffs` 表（`uid/query/query_hash/status/notification_error`，按 uid+query 索引）；聊天不做预检索，最终助手消息被 `knowledge_disposition` 判定为 `knowledge_refusal` 时持久化 `handoff_available/handoff_query` 元数据，前端据此在拒答消息下展示"转人工"入口，提交后写入转人工记录并按 `WECOM_CUSTOMER_SERVICE_URL` 通知企微。
- 全局知识库搜索：`GlobalKnowledgeSearchService.search_with_status` 跨用户全部可见库检索并按库汇总；前端新增 `GlobalKnowledgeSearchModal` 全局搜索弹窗（`AppLayout` 顶栏入口），`DataBaseInfoView` / `AgentMessageComponent` / `BaseToolCall` / `useAgentStreamHandler` 同步支持全局搜索结果的检索状态与转人工展示。
- 禁止 AI 调用思维导图：`kbs.tools` 工具列表从 6 个收敛为 5 个，`get_mindmap` 对模型不可见（仅前端人工查看），buildin 知识库 skill 描述由"查看思维导图"改为"搜索文件"，`test_skills_middleware` / `test_skill_service` 同步对齐。
- RAGAS 评估与压测脚本：新增 `backend/scripts/eval_datasets`（loc/mcx/poc 测试集、gold answers 生成、图谱消融 `ablate_graph`、RAGAS 评估报告）与 `scripts/bench`（`agent_query_kb_bench` 代理侧检索、`e2e_first_token` 端到端首字、`retrieval_latency_bench`/`latency_stats`/`milvus_only_probe` 链路压测、`local_inference` 本地推理服务与配置切换），附 `docs/检索性能验收测试报告-20260810.md` 链路压测报告。
- 修复沙箱地址在 Docker Desktop/WSL2 下不可达导致 Skill 读取超时：provisioner docker 后端此前用 `host.docker.internal:<随机宿主端口>` 作为沙箱访问地址，而该地址在容器内（api/worker/provisioner 与沙箱同处 `yuxi-know_app-network` 桥接网络）不可达，导致每次沙箱 readiness 检查（provisioner 侧等满 300s）与 api 侧 `read_file`（客户端 20s `ReadTimeout: timed out`）全部失败，Skill 加载每次 30s+ 超时。现 `LocalContainerProvisionerBackend._to_record` 在配置了 `DOCKER_NETWORK`（沙箱加入共享网络）时改用容器名 + 内部端口 `http://yuxi-sandbox-<id>:8080` 直达，未配网络时保留原宿主地址方案；实测修复后冷启动建沙箱 ~4s、热读 ~0ms。同步重启 api/worker 刷新存量连接缓存。
- 修复「流式处理失败」偶发报错：`search_product_image` 工具此前未声明 `args_schema`，langchain 从函数签名推断 schema 时把注入参数 `runtime: ToolRuntime` 也推理了进去，而 `ToolRuntime` 是含 `stream_writer: StreamWriter`（Callable）的 TypedDict，模型绑定该工具时对推断 schema 调 `model_json_schema()` 抛 `PydanticInvalidForJsonSchema: Cannot generate a JsonSchema for core_schema.CallableSchema`，run 在 0.4s 内失败、前端显示「流式处理失败」；该工具仅在启用产品识别的线程配置下被绑定，故偶发。修复为给它显式 `args_schema=ProductImageSearchInput`（仅 `query_text`，排除 `runtime`），与 `ocr_parse_file` 同一模式；新增回归单测断言其 JSON Schema 可序列化，`test_search_product_image` + 中间件测试 16 条全绿。
- 修复知识库来源面板显示错误文件（甲方验收实测：E600 型号参数回答正确，但底部「知识库来源」只显示 2 个无关的 RoHS 证书 PDF）。根因：① 模型通过 `open_kb_document`（按行窗口读取文档原文，而非 `query_kb` 检索）作答，而前端 `extractKnowledgeChunksFromConversation` 此前只收集 `query_kb(s)`/`search_file`/`find_kb_document`，真正依据的规格书/彩页从未进入来源面板；② 同一轮 `search_file "对讲机"` 探索返回了 11 个证书文件（被收集），③ 回答末尾的提问句「需要我进一步检索该机型的认证证书信息（如 CE、RoHS、IEC62133 等）吗？」被 `extractCitationNames` 切成引用候选，归一化核心「rohs」子串命中 RoHS 证书文件名（≥0.8），过滤后只剩 2 个证书。修复四连：① 后端 `OpenOutputSchema` 新增 `source`（文件显示名，`filename`/`original_filename`/`file_id` 依次回退），`open_file_content` 随窗口返回；② 前端 `extractKnowledgeChunksFromConversation` 收集 `open_kb_document` 窗口（来源名优先取后端 `source`，缺失时回退同轮 `search_file` 解析）；③ `extractCitationNames` 在「来源：」清单后遇空行再接散文/提问句时截断，提问句不再误当文档名；④ `hasKnowledgeRetrieval` 纳入 `open_kb_document`，纯读取文档的轮次也保留来源入口；另把文档名切分时的 markdown 代码标记反引号（真实输出路径用 `` ` `` 包裹）与 `*`/`•`/`-` 一并剥除。新增前端回归测试（`open_kb_document` 收集 + 来源名优先级、清单后提问句截断、端到端过滤只保留 E600 规格书与彩页、`hasKnowledgeRetrieval` 判定）与后端 `open_kb_document` 测试；后端 kbs_tools + run_agent 33 条、前端 messageProcessor 及全部相关测试全绿。
- 验收 UI 微调三项（甲方反馈）：① 知识缺口「处理/联网补答」流程改**人工补答优先**——此前只有「联网补答」一条路径能保存答案，且弹窗一打开就自动触发 Tavily 联网搜索，但并非所有缺口都需要联网（人工直接编辑即可）；现弹窗打开不再自动搜索，可直接输入答案保存为人工问答对，「联网搜索生成草稿」降为可选的候选生成按钮，行内/抽屉按钮改「补答 / 处理」文案。纯前端改动（`save-qa` 接口本就只要求 `answer`，`sources` 可选）。② 上传（及文件入库参数弹窗）分块参数折叠：`ChunkParamsConfig` 把「最大 Token 数 / 重叠比例 (%) / 分隔符」三项收进「高级」展开/收起按钮（默认收起），「分块策略」保留在首屏。③ 「行业方案」按钮前端改名「word产品方案」（按钮文案、tooltip、开启/关闭 toast 同步）。
- 历史会话来源面板兜底显示 14 个文档的历史数据回填：`source` 字段引入前的 `open_kb_document` 工具调用落库 `tool_output` 无 `source`，前端拿不到来源名退化为 `file_id`（如 `file_5d52bf`），与答案文件名无法归因匹配，触发"匹配为空 → 兜底展示全部检索文档"。用 `backend/scripts/backfill_open_kb_document_source.py` 按 `knowledge_files.filename` 一次性回填 459 条历史 `tool_output`（457 条补齐，2 条 `tool_output` 为错误文本、非法 JSON 跳过）；已用 conv 714 真实数据端到端验证，来源面板从 14 个恢复为仅 2 个 E600 文档。新会话运行即自动带 `source`，无需处理。
- 隐藏知识缺口「处理」入口（甲方反馈后简化）：此前「处理」可把缺口直接标记「已解决」，但接口只改 `status + resolution_note`、不保存问答对，智能体下次遇到相同问题照样拒答，且 `record_occurrence` 会在同一问题再次出现时把已解决缺口自动重置回「新发现」，属假解决。现前端隐藏「处理」按钮与弹窗（行操作、抽屉入口一并移除），只保留「补答」路径（保存问答对并自动置已解决）；后端 `PATCH /knowledge-gaps/{id}` 接口保留，列表/详情响应继续带 `has_answer` 派生字段（同一 `assistant_message_id` 下存在 `source_type=knowledge_gap` 的启用问答对即视为已补答，列表批量查询一次避免 N+1），供后续需要时恢复入口或其它调用方使用。

## v0.7.0 (2026-06-13)

### 破坏性变更

- Provider 与模型配置收敛：移除旧版 v1 模型配置与 Ollama 支持，运行时模型统一使用 `provider_id:model_id` 与独立 provider 模块；自定义 provider 实现逻辑从文件移动到数据库，并从 config 文件迁移到 provider 模块。
- 智能体运行时语义收敛：用户可见的 `AgentConfig` 收敛为数据库持久化的一级 `Agent`，内置 Python Agent 改为智能体后端；聊天、运行任务、恢复审批和文件预览均从线程绑定的 Agent 解析运行时上下文，前端只提交 `agent_id`。
- 知识库能力边界收敛：移除 Upload 与 LightRAG 知识库/图谱能力，知识库类型收敛为 Milvus 与只读连接器；知识库 API 统一使用 `/databases/{kb_id}/xxx` 形式，并整合 mindmap / eval 等子接口。
- Agent 资源默认选择与权限过滤：未显式配置工具、知识库、MCP、Skills、子智能体时默认启用当前用户可访问/可用的全部资源，显式选择后按允许列表过滤；Agent 创建前统一完成最终资源权限过滤、知识库 `kb_id` 可见范围派生和 Skill prompt/readable 依赖闭包派生。
- Skill 安装与权限模型收敛：Skill 元数据使用 `source_type/share_config/enabled` 表达来源、生效范围与启用状态；内置 Skill 启动或同步时自动写入数据库并默认全局启用，上传和远程添加统一改为解析草稿后确认安装，不保留旧直接安装兼容路径。
- 历史兼容层精简：移除 sandbox provisioner `local` 后端别名、ask_user_question 单问题旧协议、JWT 历史默认密钥特殊判断、内置 Skill `SKILLS.md` 文件名回退、运行事件数字 seq 兼容和前端旧字段回退。
- 用户身份命名收敛：原业务登录标识统一改为 `uid`，Agent/LangGraph runtime、conversation、agent_run、sandbox 路径和前端用户态均使用字符串 `uid`；`user_id` 仅保留给外部响应中的数值 `users.id` 或真实外键场景。

### 开发记录

- 发布版本号更新至 `0.7.0`，同步 package、Docker 镜像标签与快速开始分支引用。
- 新增内置「深度研究」多智能体：编排器 Agent（`deep-research`，ChatbotAgent 后端）负责澄清、拆解、并行调度子智能体与综合成稿，配套两个子智能体 `research-explorer`（围绕单个子问题多轮检索网页/知识库并返回带引用发现）和 `fact-verifier`（对抗式核验关键论断、标注冲突与置信度）；完整研究方法论沉淀为新增内置 Skill `deep-research`（依赖 `tavily_search`），编排器运行时读取并据此调度。三者随 `lifespan` 启动通过 `AgentRepository.ensure_deep_research_agents` 幂等落库（已存在不覆盖管理员修改）。
- 新增内置 `general-purpose` 通用任务子智能体：使用 `SubAgentBackend` 与空运行配置，作为 `task` 工具的通用委派目标，由启动初始化自动写入数据库。
- 收敛 MCP 创建与编辑入口：前端移除整段配置文本入口和模式切换器，仅保留表单字段提交；后端 MCP 创建/更新请求拒绝额外配置字段，避免绕过表单约束。
- 调整内置 MCP 默认项：移除 `sequentialthinking` 的系统内置同步，启动同步时清理历史系统内置记录，保留用户手动创建的同名 MCP。
- 图片生成能力迁移为 Skill：Qwen-Image 从内置 Python 生成工具迁移到内置 Skill `image-gen`，模型调用与图片下载在 Agent 沙盒中完成，生成结果保存到 outputs 并通过 `present_artifacts` 展示，为多图片生成模型接入复用同一产物展示链路。
- 优化前端头像加载兜底：用户与智能体头像优先展示已配置图片，加载失败后回退到基于 ID 的 DiceBear 默认头像；离线或默认头像不可达时显示名称前两个字和稳定背景色。
- 降低知识库路由与工具模块复杂度：示例问题生成迁移到知识库 utils，文件上传统一 100 MB 限制，URL 预处理入库路径与旧 `content_type=url` 行为收敛，并修复 uid、导出 MIME 与异常透传等路由问题。
- 重构智能体配置语义：用户可见的 `AgentConfig` 收敛为数据库持久化的一级 `Agent`，内置 Python Agent 改为智能体后端；新增 `/api/agent` 管理与运行接口，聊天、运行任务、恢复审批和文件预览均从线程绑定的 Agent 解析运行时上下文，前端只提交 `agent_id`，并在模型配置页新增“智能体”管理页签。
- 删除 Upload 与 LightRAG 图谱/知识库能力：知识库类型收敛为 Milvus 与 Dify，只保留 Milvus 知识库内图谱构建/展示/检索，移除独立 `/graph` 页面和默认上传图谱工具。
- 收敛只读知识源连接器：新增 `ReadOnlyConnectors` 基类，Dify 改为声明自身创建参数与校验规则，新增 Notion Data Source 只读知识库并支持 Search/Find/Open；知识库类型接口返回创建参数 schema，前端新建表单按类型动态渲染非 Milvus 配置并统一保存到 `additional_params`。
- 新增知识库 Chunk 持久化：Milvus 知识库索引/更新流程会将 chunks 双写到 PostgreSQL `knowledge_chunks` 表与 Milvus，文件内容查看优先查询 PostgreSQL，并为位置信息、图谱实体关联、标签和抽取结果预留结构化字段；chunk 入库改为分批 embedding 与分批写入，避免大文件一次性写入触发 gRPC 消息大小限制；入库成功后将单文件 chunk 数与 token 数写入文件元数据，并将知识库级总 chunk 与总 token 汇总保存到 metadata，前端文件管理页展示该统计并支持一键修复历史文件缺失的统计值。
- 完善 Milvus 知识库图谱构建：修复 Chunk 图谱写入返回值、Neo4j 同步写入阻塞事件循环、重复构建任务竞态、图谱查询提前终止、Neo4j 连接复用、LLM 抽取超时重试和前端错误详情展示等问题；图谱构建会将 entity/triple 本体与 chunk 引用写入 PostgreSQL，并为唯一 entity/triple 建立 Milvus 语义索引，单文件删除时同步清理图谱引用和孤儿向量。
- 优化图谱抽取器配置：未配置时在图谱中心展示配置入口，抽取方案收敛为 LLM，前端仅保留“更多拓展中”占位；LLM 抽取器使用固定 Prompt + 自定义 Schema，并支持模型参数与并发队列数；已配置后允许修改参数并提示重置重抽风险。修复上传并入库新文件时旧内存 metadata 覆盖数据库图谱配置的问题。
- 新增 Milvus 图谱检索链路：Query 可召回图谱实体和三元组，结合 Chunk 命中实体构造 seed entity，读取 Neo4j 2-hop 子图后用 igraph 执行 PPR，最终以 Chunk 为产物并通过 RRF 与原 Chunk 召回融合；检索配置改为 dataclass 元数据生成，支持 `depend_on` 控制重排序和图检索参数展示。
- 收紧用户管理部门隔离：普通管理员创建用户时固定归属本部门，用户列表、访问选项、详情、更新和删除接口均限制在本部门范围内。
- 修复用户管理列表超过 100 人时被默认分页截断的问题：前端按 `skip/limit` 分批加载用户，并在用户卡片列表中补充分页渲染。
- 调整 Agent 资源默认选择与运行时上下文：未显式配置工具、知识库、MCP、Skills、子智能体时默认启用当前用户可访问/可用的全部资源，显式选择后按允许列表过滤；Agent 创建前统一完成最终资源权限过滤、知识库 `kb_id` 可见范围派生和 Skill prompt/readable 依赖闭包派生，聊天运行时与文件系统预览复用同一结果。
- 重构 Skills 权限与安装流程：Skill 增加 `source_type/share_config/enabled`，内置 Skill 作为启动同步入库的全局资源，不再保留前端安装/更新状态，支持启停但不允许删除；上传和远程添加统一为解析草稿后确认生效范围，安装 slug 优先读取 `SKILL.md` 的 `slug` 字段并保留 `name` 展示名，压缩包名称不参与 slug 校验；管理端支持编辑生效范围与启停；Agent 运行时按当前用户可访问 Skills 派生 prompt/readable 依赖闭包并限制挂载/激活，Skills prompt 改为模型请求级注入以避免污染 runtime context；主智能体恢复 `install_skill` 工具，允许当前用户安装私有 Skill 并激活当前会话，子智能体配置和运行态均禁用该工具。
- 精简历史兼容层：移除 sandbox provisioner `local` 后端别名、ask_user_question 单问题旧协议、JWT 历史默认密钥特殊判断、内置 Skill `SKILLS.md` 文件名回退、运行事件数字 seq 兼容和前端若干旧字段回退。
- 重构知识库共享权限：`share_config` 改为全局共享、部门共享、指定人可访问三档，部门共享必须包含当前用户部门，指定人可访问必须包含当前用户，并补充权限过滤测试。
- 移除知识库沙盒文件系统映射：不再通过 `/home/gem/kbs` 暴露知识库文件树，Agent 继续使用 `query_kb` 与 `open_kb_document` 访问知识库内容。
- 修复 MinerU 文档解析配置说明：文档处理指南原先指引启动 `openai-server`（30000 端口，仅提供 `/v1/chat/completions`），与解析器实际调用的 `/file_parse` 接口不匹配导致 `mineru_ocr` 不可用；更正为使用项目内置的 `mineru-api` 服务（30001 端口），并补充镜像构建与显存调优说明。
- 规范 Agent 知识库 Search/Find/Open 工具协议：`resource_id` 统一表示知识库 `kb_id`，Search 返回结构化 `resource_id/file_id/chunk` 结果，新增 `find_kb_document` 在已知文件内做关键词或正则定位，Open 默认窗口扩大到 1800 行。
- 收敛知识库分块配置：分块预设仅表达策略选择，通用分块参数统一通过 `chunk_parser_config` 传递；移除 `chunk_size`、`chunk_overlap`、`qa_separator` 等旧 root 字段兼容。
- 收敛知识库文件解析参数：文件级 `processing_params` 统一保存 `ocr_engine` 与 `ocr_engine_config`，解析阶段直接使用该结构并保留分块参数快照。
- 修复知识库文件大小显示为 0 的问题：文件上传时 `file_sizes` 参数未正确传播或历史数据缺失导致 DB 中 `file_size` 为 `None`；新增 `MinIOClient.stat_file/astat_file` 获取文件大小方法，`add_file_record` 在 `size` 缺失时从 MinIO 回补，`_load_metadata` 加载元数据后自动为缺少 `size` 的文件从 MinIO 补全并持久化。
- 优化评估基准自动生成：生成任务支持配置队列并发数，默认 10，范围 1-20。
- 完善模型供应商类型：普通聊天模型运行时新增 Anthropic provider type 适配，并清理不再支持的旧 provider type 入口。
- 重梳理知识库评估存储：评估数据集、题目、评估运行和逐题结果统一入库，JSONL 仅作为导入/导出格式；后端和前端 API 统一使用 dataset/run 语义；评估运行支持用户命名，历史记录按名称展示，综合评分只聚合检索指标。
- 扩展知识库上传来源：添加“从工作区上传”模式，后端将当前用户工作区文件预处理上传到 MinIO，前端沿用现有 `addDocuments` 入库链路提交 MinIO URL、内容哈希和文件大小。
- 重构知识库详情页布局：`DatabaseInfo` 改为顶部详情 header + 左侧功能 tab 侧边栏 + 右侧内容区，Milvus 默认进入文件管理，并将检索测试、知识图谱、知识导图、检索配置、RAG 评估和评估基准统一纳入侧边栏导航；只读连接器保留检索测试与检索配置。
- 整合知识导图接口：移除独立 mindmap router 与前端 API 模块，思维导图生成、查询和文件列表接口统一收敛到知识库 API 下。
- 收敛独立模型配置模块运行时：运行时 chat / embedding / rerank 均统一从 provider 模块与模型缓存读取 `provider_id:model_id`；旧版静态模型配置、v1 slash spec、旧模型列表接口和 Ollama 适配已移除；内置 provider 模板补充 XiaomiMiMo、XiaomiMiMo Token Plan CN 与 Kimi Code（`kimi-for-coding`）。
- 调整智能体模型配置默认值：`BaseContext.model` 默认保持为空，运行时按“请求模型 > 智能体配置模型 > 系统默认模型”解析；子智能体未配置模型时继承主智能体当前运行模型，避免把系统默认模型固化进每个智能体配置。
- 调整智能体配置归属与字段权限：`AgentConfig` 从部门共享改为按 `uid` 隔离，所有登录用户可管理自己的配置；`BaseContext` 支持字段级 `auth` 元数据，后端按用户角色过滤可见与可保存的配置项。
- 新增用户级沙盒环境变量：增加 `agent_envs` 表与 `/api/user/agent-env` 接口，设置面板支持当前用户维护 Agent 沙盒环境变量；创建新沙盒时与全局 `sandbox.env` 合并注入，用户变量优先。
- 收敛用户身份命名：原业务登录标识统一改为 `uid`，Agent/LangGraph runtime、conversation、agent_run、sandbox 路径和前端用户态均使用字符串 `uid`；`user_id` 仅保留给外部响应中的数值 `users.id` 或真实外键场景。
- 工作区知识库分类显示：知识库侧边栏按创建者分组为“我的知识库”和“共享知识库”，自己创建的知识库显示在“我的知识库”下，非自己创建的显示在“共享知识库”下；`knowledge_bases` 表新增 `created_by` 字段记录创建者 uid。
- 工作区文件上传支持多选：`/workspace/upload` 与 Viewer 工作区上传统一使用 `files` 多文件字段，一次最多上传 50 个文件，批量上传失败时清理本次已写入文件。
- 聊天附件新增 MinIO tmp 临时上传、可选 PDF/图片解析、确认后加入线程附件的流程；前端改为弹窗内上传、解析与确认。
- 修复智能体对话上传透明 PNG 后图片失真的问题：多模态图片处理在导出 RGB 前会先按白底合成 alpha 通道，避免透明像素中的隐藏颜色被直接转为可见像素；交付物预览优先按文件头识别 MIME，避免 `.jpg` 文件名包裹 PNG 内容时前端按错误格式加载；Agent run 输入消息会持久化为 `multimodal_image`，刷新历史后仍能显示用户上传图片。
- 优化智能体对话页细节：状态面板隐藏空 section，待办名称限制为 20 个中文汉字以内，模型选择器展示供应商名称，并收紧附件状态标签与文件编辑浮动操作样式；
- 标准化 Agent run/SSE 执行链路：run 创建时持久化输入消息并提交后入队，worker 统一写入 Redis Stream envelope，SSE 输出 `event/data/id`、心跳注释、`Last-Event-ID` 回放和终止 `end` 事件；前端强制使用 run API 并支持 ask_user_question 中断后以 resume run 恢复；事件 envelope 构造收敛到统一 helper，前端优先使用 envelope 一级 `thread_id` 路由。
- 修复 AgentRun 恢复与取消边界：无显式 `request_id` 的 resume run 改为按父 run 与恢复载荷派生稳定幂等 key，避免恢复重试创建重复 run；取消请求先提交数据库状态再发布 Redis 取消信号，避免 worker 在提交窗口内读到旧状态。
- Agent run SSE 新增 `verbose=false` 精简模式：默认仍返回完整事件载荷；精简模式仅在 SSE 输出前重建最小 payload，跳过 `metadata` 和空 `yuxi.agent_state`，将同一 data 内的 `request_id` 外提为单个字段，移除 chunk 中重复的 `meta`、`metadata`、`thread_id`、`response`、空 `namespace` 和图片 base64 等调试字段，保留消息增量、工具调用、工具结果、非空 Agent state、终止状态和 SSE 游标，前端订阅默认使用精简模式。
- 修复 SiliconFlow MiniMax 与阿里云百炼工具调用流式兼容：二者的 OpenAI 兼容流经 LangGraph v3 event stream 累积工具调用时会丢失关键字段（MiniMax 在参数增量 chunk 返回空 `function.name`，百炼丢失 `tool_call.id`），空值被写入 checkpoint 后会导致工具执行失败或工具结果无法按 `tool_call_id` 关联、工具状态永远停留在“进行中”；这两类提供商默认对工具调用禁用流式模型响应（正文回答仍流式），保留 LangGraph v3 运行事件并拿到完整 tool_call。该缺陷属 LangChain v3 流式协议上游问题（参见 langchain#37420、langchainjs#10937、langgraphjs#2496），截至 langchain-core 1.4.4 仍未修复，待上游修复后可移除对应提供商的禁流式处理。
- 收敛后端模块边界：文档解析从 `plugins.parser` 移动到 `knowledge.parser`，内容审查从 `plugins.guard` 移动到 `services.guard`。
- 收敛文件服务边界：文件预览判断抽为独立服务，Viewer 文件系统的 workspace 分支复用用户 workspace 服务，线程运行时上下文解析从泛化 `filesystem_service` 拆出为 agent runtime helper。
- 升级 DeepAgents 到 0.6.7 并适配新版文件系统协议：SubAgentMiddleware 改为显式 subagent spec，Skills prompt 补齐新版占位符；sandbox/skills backend 复用新版 `ReadResult`、`GlobResult`、`GrepResult` 等协议类型，文件权限在 backend 层明确区分 skills、uploads、outputs 与 workspace，保留最小 `CustomCompositeBackend` 以避免非 route glob 误扫其他 route；Agent 上下文压缩改为复用 DeepAgents SummarizationMiddleware，历史摘要与大工具结果统一 offload 到 outputs。
- 优化聊天输入 @ 文件提及：未创建 Thread 时可搜索用户 workspace，创建 Thread 后按当前对话文件优先、workspace 兜底的来源顺序搜索，并拆分 workspace/thread 缓存避免假 thread 与跨用户缓存污染；输入框与用户消息支持将 raw mention 渲染为带类型图标的引用单元，文件仅显示文件名且保留原始沙盒路径文本。
- 重构子智能体为 Agent-backed 形态：移除旧 `subagents` 表与 `/api/system/subagents` 管理链路，子智能体改为 `agents.is_subagent=true` 且使用 `SubAgentBackend`，创建/编辑统一走 Agent 管理入口；内置后端收敛为 `ChatbotAgent` 与 `SubAgentBackend`，Context 分为 `BaseContext`、`ChatBotContext` 与 `SubAgentContext`；主 Agent 通过 AI知识库 task middleware 启动真实子 Agent graph，子智能体不再嵌套调用子智能体。沙盒挂载同步拆分为 child checkpoint thread、父对话 uploads/outputs、用户级 workspace 与子 Agent skills scope；主线程状态记录 `subagent_runs` 并在前端 task 工具中展示子智能体名称、执行状态、child thread 和产物，task 工具结果会暴露 child thread ID 且支持传回 `thread_id` 继续既有子智能体线程；子智能体执行复用 `agent_runs(run_type=subagent)` 记录父 run、child thread 与状态，child thread state 查询以 `agent_runs` 关系为准，不再解析 thread ID 反推父线程；真实流式 E2E 覆盖子智能体输出文件可由父线程文件/Viewer API 读取。流式链路参考 DeepAgents event streaming，后端将 LangGraph v3 raw event 归一化为 AI知识库 semantic stream event，按父/子线程归属隔离 run SSE chunk，并支持通过 child thread state 拉取子智能体中间过程。
- 修正评估综合得分计算：`overall_score` 改为有答案准确率时取各题准确率平均，否则取各题 `recall@10` 平均，不再把 recall/f1/各 k 检索指标混合平均；历史已存运行不回填。
- 清理无效鉴权中间件：移除启动时未实际校验令牌的 `AuthMiddleware` 和公开路径残留判断，后端认证边界明确收敛到路由依赖；`/api/auth/me` 改为强制登录并补充未登录访问返回 401 的集成测试。

## v0.6.2 (2026-05-22)

### 新增

- 新增个人工作区预览与管理：提供独立于对话 thread 的用户级 workspace API，并增加“工作区”页面，用于浏览、预览、编辑、上传、下载、删除个人 workspace 文件；默认创建 `agents/AGENTS.md`，并在 Agent 执行时将其内容追加到系统提示词。
- 新增独立模型配置模块：增加 `model_providers` 表、独立管理接口和“模型配置”页面，支持 provider 基础信息、远端候选模型、enabled models 配置和手动添加模型能力。
- 新增远程 Skill 批量安装能力：后端新增 `install_remote_skills_batch()` 与 `POST /remote/install-batch`，前端补充批处理安装 API 和 UI 逻辑。

### 优化

- 下放扩展管理权限：普通管理员现在可进入扩展管理并完整管理 Tools、MCP、SubAgent、Skills；同步放开 Skill 管理接口权限并补充权限测试。
- 调整 Agent 知识库默认选择：未显式配置知识库时默认启用当前用户可访问的全部知识库，显式保存空列表仍表示不启用知识库。
- 优化评估基准自动生成：仅支持 commonrag/Milvus 知识库，默认参考 chunks 数量改为 1；多 chunk 场景复用知识库向量检索选择相似 chunks，不再对全量 chunks 重新计算 embedding。
- 优化 Agent 输入框文件 mention：用户级 workspace 文件候选改为从独立 workspace API 递归加载，不再依赖 active thread；插入时仍转换为 `/home/gem/user-data/workspace/` 沙盒虚拟路径。
- 调整知识库思维导图后端结构：将思维导图路由文件重命名为知识库语义更明确的 router，并把文件列表整理、提示词构建、AI JSON 解析等纯逻辑下沉到知识库 utils。
- 收敛知识库评估后端结构：将评估指标、单题评估、答案生成提示词和自动基准生成算法下沉到 `knowledge/eval`，`EvaluationService` 保留任务、文件和持久化编排职责。
- 扩展管理界面交互逻辑重构：MCP / Subagents / Skills 从“左侧边栏 + 右侧详情面板”调整为“卡片式网格布局 + 路由跳转二级页面”，工具标签页改为卡片网格布局 + 弹窗详情。
- 统一卡片样式：`ExtensionCard` 新增 `tags` prop 并复用于知识库列表页，知识库列表改用 `ExtensionCard` + `ExtensionCardGrid` 替代原有自定义卡片。
- 调整应用主导航：`AppLayout` 升级为默认展开的侧边栏，保留折叠态图标导航，并统一导航项、任务中心、GitHub、用户信息的图标与文字对齐。
- 合并智能体对话导航：移除 `AgentChatComponent` 内部聊天侧边栏，将新建对话入口和对话历史移动到 `AppLayout` 主侧边栏，并通过共享线程 store 统一管理。
- 统一前端 Markdown 预览渲染：新增共享 `MarkdownPreview` 组件与 `markdown_preview` 渲染工具，替换 Agent 消息、文件预览、知识库 chunk、任务工具结果、聊天导出等场景中的旧预览实现。

### 修复

- 修复聊天中普通用户 `@` 提及出不来技能和 MCP 列表的问题：放宽技能列表与 MCP 服务器列表读取接口至已登录用户，并对普通用户请求的 MCP 列表进行敏感连接参数脱敏。
- 修复知识库文档入库状态回退：当已解析文件缺失 `markdown_file` 解析产物时，索引流程会将文件状态恢复为未解析，便于重新解析。
- 修复附件上传后未立即刷新 mention 候选的问题。
- 加固 JWT 鉴权安全：移除历史默认密钥回退，初始化脚本支持生成并持久化 `JWT_SECRET_KEY` 与 `YUXI_INSTANCE_ID`，签发和验证令牌时校验 `iss/aud`，并拒绝已删除或登录锁定用户继续使用旧令牌访问系统。
- 修复模型配置路由请求模型未接收 `embedding_base_url` / `rerank_base_url` 导致前端已填写仍被后端校验拦截的问题。
- 修复知识库文档处理任务状态不一致问题：文件解析失败时任务中心正确显示"失败"而非"已完成"。

## v0.6.1 (2026-04-24)

### 新增

- 合并知识库导航入口：左侧导航仅保留"知识库"，文档知识库与图知识库在页面 header 中通过同一组轻量切换入口切换
- 抽象页面轻量切换 header：知识库与扩展管理页直接共用 `ViewSwitchHeader`，收敛文档知识库、知识图谱、Tools、MCP、Subagents、Skills 等入口的信息层级
- 调整任务中心交互：入口移动到 GitHub 按钮下方，并将右侧抽屉展示改为居中弹窗
- 将 `yuxi` 从 uv workspace 成员调整为 `backend/package` 下可独立构建的本地 Python 包，backend 通过 path dependency 以已安装包形式发现依赖
- 新增 Skills 远程安装能力：Skills 管理页支持填写 `owner/repo` 或 GitHub URL，后端通过隔离的临时 `HOME` 调用 `npx skills add` 下载指定 skill
- 调整部门删除语义：删除部门时不再要求用户数为 0，而是将部门下用户迁移到默认部门
- 扩展 viewer 工作区文件操作：`/home/gem/user-data/workspace` 支持从文件系统面板新建文件夹和上传文件
- 为历史线程补充前端本地配置变更提示：当已有历史消息的对话中切换 Agent、切换配置或编辑配置项时，插入非持久化的信息提示
- 调整 Worker run 模式下的消息首屏反馈：前端发送消息时先乐观渲染用户消息，再将前端生成的 `request_id` 透传给 `/api/chat/runs` 与服务端 `init` 对账
- 调整聊天首页的智能体切换入口：当智能体数量 `>= 4` 或内容区宽度小于 `380px` 时自动收敛为"当前智能体 + 下拉按钮"形式
- 调整智能体对话中的工具调用展示：连续工具调用默认折叠为"调用了 N 个工具"的轻量摘要
- 调整输入框配置入口与侧边栏头尾交互：输入区配置按钮改为轻量 dropdown 触发器

### 修复

- 修复沙盒 `workspace` 隔离粒度：宿主机目录从共享 `saves/threads/shared/workspace` 收敛为用户级 `saves/threads/shared/<user_id>/workspace`
- 收紧文件系统安全边界：viewer/chat 下载与删除路径统一基于解析后的真实路径做允许目录校验，阻止通过软链接逃逸工作区/线程目录
- 修复 OIDC 原始用户名绑定中的占位用户解析：解析目标用户 ID 时改为从右侧拆分，避免 `sub` 中包含冒号时把已绑定账号误判成冲突账号
- 修复 DOCX 解析中的图片回插顺序：Docling 导出的多个 `<!-- image -->` 占位符现在按文档图片顺序替换
- 修复前端依赖安全告警：通过 `pnpm.overrides` 将传递依赖 `flatted` 锁定到 `3.4.2`、`lodash-es` 锁定到 `4.18.1`
- 修复对话摘要中间件的工具结果卸载链路：摘要触发时改为将大体积 `ToolMessage` 写入当前 agent 可见的 sandbox outputs 路径
- 修复 agents 页对话侧边栏在 `keep-alive` 路由切换后的误关闭问题
- 调整 Milvus 混合检索实现：集合 schema 增加 BM25 稀疏向量字段、BM25 函数和中文 analyzer 配置
- 重构 MCP 运行时配置加载模型：移除 `MCP_SERVERS` 作为运行正确性前提的设计，改为每次直接从数据库读取最新 MCP 配置
- 为知识库检索工具补充 `metadata.filepath` 注入：在 `query_kb` 统一出口基于会话可见知识库构建 `file_id -> /home/gem/kbs/...` 映射并回填 Milvus 检索结果
- 移除知识库沙盒文件系统映射：Agent 不再通过 `/home/gem/kbs` 遍历知识库文件，继续通过 `query_kb` 和 `open_kb_document` 检索与打开文档。

## v0.6.0 (2026-04-01)


### 新增
- 重构后端代码 src -> backend/package/ai-kb
- 重构文档解析，统一文档解析体验，并新增 Parser 类
- 新增 LITE 模式启动，启动时不加载知识库、知识图谱相关模块，可以使用 make up-lite 快捷启动
- 新增沙盒环境，详见后续文档更新，统一沙盒虚拟路径前缀默认值为 `/home/gem/user-data`
- 新增基于沙盒的文件系统，前端工作台可以查看文件系统，支持预览（文本、图片、PDF、HTML）、下载文件
- 新增 `present_artifacts` 内置工具：Agent 可将 `/home/gem/user-data/outputs/` 下的结果文件显式写入 LangGraph state 的 `artifacts` 字段，前端支持在输入框顶部以默认折叠的堆叠卡片展示本轮交付物文件，并保持可下载、可预览能力
- 交付物卡片新增“保存到工作区”能力：支持将单个交付物复制到共享目录 `workspace/saved_artifacts/`，并复用现有文件树/预览/mention 体系立即可见
- 新增基于沙盒的知识库只读映射，按“用户可访问知识库 ∩ 当前 Agent 已启用知识库”暴露原始文件与解析后的 Markdown
- 重构附件系统，直接集成在了沙盒文件系统中，附件上传后直接落盘到沙盒挂载目录
- 优化前端流式消息体验：新增通用 `useStreamSmoother` 调度层，统一平滑 Agent runs SSE、普通聊天流与审批恢复流中的 `loading` chunk
- 优化项目文档说明，并添加贡献指南
- 重构前端 Agent 路由结构，体验更加顺畅，切换更加自然（类 chatgpt 体验）
- 新增 API Key 认证功能，支持外部系统通过 API Key 调用系统服务
- 新增 subagents 的支持，支持在 web 中添加 subagents，以及两个内置的子智能体
- 新增内置Skills reporter，并移除内置 Agent reporter，数据库报表将由 Skills 完成
- 新增内置 Skills `deep-reporter`，用于指导生成科研报告、行业调研和其他深度分析类长报告
- 重构内置 Skills/MCP/Subagents 安装/添加/移除机制：内置 skill 支持按需安装、基于 `version + content_hash` 的更新提示与覆盖确认，不再使用服务器级开关切换
- 新增知识库 PDF、图片的预览功能
- 重构后端测试目录结构：按 `unit / integration / e2e` 分层迁移现有测试，拆分全局 `conftest.py`，统一测试入口为 `uv run --group test pytest`，并新增独立测试规范文档 `docs/develop-guides/testing-guidelines.md`
- 新增工具元数据 `config_guide` 字段：后端工具列表接口现在可返回“给人看的配置说明”，前端工具详情页会展示该说明，用于提示工具使用前需要配置的环境变量或入口；首批为 MySQL 工具和 `Qwen-Image` 补充了配置指引
- 补充 Langfuse 集成方案文档：明确采用“云端优先、先 tracing 后 feedback”的接入路径，并约定 AI知识库 的 `user/thread` 到 Langfuse `user_id/session_id` 的映射关系
- 新增面向用户的 Langfuse 集成文档：在“高级配置”分组中说明 Langfuse 的定位、能力、配置方式与查看路径，并与当前 `LANGFUSE_BASE_URL` 配置保持一致

<!-- 添加到这里 -->

### 修复

- 调整聊天首页的智能体切换入口：在无历史对话时，智能体数量 `<= 3` 且 `chat-main` 宽度不小于 `380px` 时继续使用横向 segmented；当智能体数量 `>= 4` 或内容区宽度小于 `380px` 时自动收敛为“当前智能体 + 下拉按钮”形式，避免多智能体或窄屏场景下入口被截断
- 发布前一致性修复：统一 0.6.0 版本号（backend/package/web）、更新 dev/prod 镜像标签语义（`0.6.0.dev` / `0.6.0`），并为 `/api/system/health` 补充 `version` 字段，提升部署可观测性与发版追溯能力
- 收敛“状态工作台”自动弹出规则：前端不再因为共享 `workspace` 或文件系统天然存在内容而默认展开，改为仅在 `/home/gem/user-data/uploads` 或 `/home/gem/user-data/outputs` 下检测到实际文件时自动弹出；手动打开、关闭、刷新和伸缩交互保持不变
- 调整智能体 todo 展示语义：待办状态不再作为 `capabilities` 前端开关，而是直接根据运行态 `agent_state.todos` 渲染；同时将 todo 入口从 Agent Panel 移到输入框内的轻量浮层，并让右侧“状态工作台”收敛为文件系统视图，输入框按钮文案同步由“状态”调整为“文件”
- 优化 Agent 输入框 mention 行为：在保留附件 mention 的同时，将共享 `workspace` 文件纳入候选范围；并将 `@` 空查询时的候选列表改为空，仅在继续输入后再执行筛选，避免工作区文件过多时直接铺满下拉面板
- 为前端工作台文件树补齐文件删除能力：`/api/viewer/filesystem/file` 新增删除接口，`AgentPanel` 文件节点新增删除按钮与确认交互，删除后会同步刷新树与预览状态
- 扩展 Agent Panel 状态工作台删除能力：继续复用 `DELETE /api/viewer/filesystem/file`，在保持接口不变的前提下支持删除文件夹；空目录与非空目录现在都会递归删除，`workspace` 下目录也可直接清理，前端目录节点同步新增删除入口与对应确认文案
- 调整前端工作台文件预览交互：恢复默认侧边/弹窗预览，并新增显式“全屏预览”入口；全屏模式下由预览内容直接覆盖整页，仅保留右上角悬浮关闭按钮；同时修复 HTML 文件首次在弹窗中预览偶现白屏的问题，改为在内容更新后强制重建 `iframe`
- 统一 Agent Panel 文件预览与消息区交付物预览组件：两处改为复用同一套 `AgentFilePreview` 预览实现，并为交付物预览补齐与工作台一致的“全屏预览”入口
- 修复交付物卡片展开后的长列表展示：当单轮交付物文件超过面板可见高度时，卡片内容区改为显示纵向滚动条，避免超过约 10 项后底部文件与操作按钮被裁切
- 兼容旧版已安装的内置 `reporter` 技能记录：`update_builtin_skill` 现在会识别由 `system` 或 `builtin-system` 管理的历史记录，避免更新时误报“技能 `reporter` 不是内置 skill”
- 调整沙盒 user-data 目录隔离策略：`workspace` 改为共享目录 `saves/threads/shared/workspace`，`uploads/outputs` 继续保持 thread 级隔离；同时更新 thread artifact 权限校验、viewer 文件系统列举逻辑，以及对应的 router/E2E 测试
- 重构聊天接口请求模型：流式与非流式聊天统一使用 `query + agent_config_id` 请求体，并移除路径中的 `agent_id`；同时修复非流式接口实际误走流式执行链路的问题，改为调用 `invoke_messages` 一次性执行，并补充对应测试
- 修复对话线程与 Agent 配置错位的问题：发送消息时将当前 `agent_config_id` 绑定到 thread 的 `extra_metadata`，线程列表接口返回该绑定值，前端切换历史 thread 时会自动恢复对应配置
- 为沙盒与 viewer 文件系统补齐知识库只读映射：新增 `/home/gem/kbs` 命名空间，按“用户可访问知识库 ∩ 当前 Agent 已启用知识库”暴露原始文件与解析后的 Markdown，并补充对应后端与 viewer 路由测试
- 优化 viewer 文件系统目录树加载：根目录与 `/home/gem/user-data` 改为直接读取本地线程挂载目录，不再为只读树视图触发 sandbox 冷启动，并补充对应后端测试
- 修复 `/home/gem/user-data` 根目录文件不可见的问题：根目录现在会同时展示 thread 目录下的真实文件和 `workspace` 入口，不再只保留固定命名空间目录
- 修复前端工具图标与渲染匹配不准确的问题：工具管理列表与工具调用结果统一改为基于工具 `id` 的精确映射，避免模糊匹配导致的误渲染，未命中的工具不再显示默认扳手图标
- 修复 GitHub Pages 文档部署工作流失败：移除 `actions/setup-node@v4` 对不存在 `docs/package-lock.json` 的缓存依赖，并将 `docs` 目录安装命令从 `npm ci` 调整为 `npm install`，避免因未提交锁文件导致 CI 在依赖缓存和安装阶段直接失败
- 修正沙盒 provisioner backend 命名与配置说明：统一对外使用 `docker` / `kubernetes`，保留 `local` 作为兼容别名；同步清理 compose 中未生效的 provisioner 环境变量、补齐 K8s 相关变量注释，并更新沙盒架构文档中的默认模式与 backend 描述
- 修复智能体配置列表接口在“无配置自动创建默认配置”路径下的参数缺失：补齐 `get_or_create_default` 的 `agent_id` 入参，避免 `/api/chat/agent/{agent_id}/configs` 返回 500
- 修复 LightRAG 同库写入并发导致的入库失败：为 `index_file` / `update_content` 增加按知识库维度的串行锁，并补齐 `documents` 接口 `auto_index` 阶段对最新解析状态的回写与回归测试，避免长时间入库任务进行中再次选择同库文件时直接并发写入报错

<!-- 添加到这里 -->


---


## v0.5

### 新增

- 优化 OCR 体验并新增对 Deepseek OCR 的支持
- 优化 RAG 检索，支持根据文件 pattern 来检索（Agentic Mode）
- 重构智能体对于“工具变更/模型变更”的处理逻辑，无需导入更复杂的中间件
- 重构知识库的 Agentic 配置逻辑，与 Tools 解耦
- 将工具与知识库解耦，在 context 中就完成解耦，虽然最终都是在 Agent 中的 get_tools 中获取
- 优化chunk逻辑，移除 QA 分割，集成到普通分块中，并优化可视化逻辑
- 重构知识库处理逻辑，分为 上传—解析—入库 三个阶段
- 重构 MCP 相关配置，使用数据库来控制 [#469](git@github.com:96980754/shanli/pull/469)
- 使用 docling 解析 office 文件（docx/xlsx/pptx）
- 优化后端的依赖，减少镜像体积 [#428](git@github.com:96980754/shanli/issues/428)
- 优化 liaghtrag 的知识库调用结果，提供 content/graph/both 多个选项
- 优化数据库查询工具，可通过设计环境变量添加描述，让模型更好的调用
- 优化任务组件，改用 postgresql 存储，并新增删除任务的接口
- 支持更多类型的文档源的导入功能（支持后端配置的白名单的 URL 导入）

### 修复

- 修复文件上传弹窗中 OCR 下拉选项展开时不会自动检查服务状态的问题
- 修复知识图谱上传的向量配置错误，并新增模型选择以及 batch size 选择
- 修复部分场景下获取工具列表报错 [#470](git@github.com:96980754/shanli/pull/470)
- 修改方法备注信息 [#478](git@github.com:96980754/shanli/pull/478)
- 修复多次 human-in-the-loop 的渲染解析问题 [#453](git@github.com:96980754/shanli/issues/453) [#475](git@github.com:96980754/shanli/pull/475)
- 修复沙盒后端接入回归：补齐 composite backend 的 `sandbox_backend` 参数、限制 `/api/sandbox/prepare` 仅允许访问当前用户线程、确保 `release()` 之后的 `destroy()` 会真正停止热池容器，并恢复 docker-compose 的完整模式默认值
- 重构沙盒为 deer-flow 风格的 AIO provider：切换为 thread-local sandbox、统一 `/home/gem/user-data/{workspace,uploads,outputs}` 固定路径、移除公开 `/api/sandbox/*` 生命周期接口，并补充 lite 模式下的 provider 生命周期、filesystem API 与 sandbox 复用/隔离 E2E 验证
- 调整聊天附件存储链路：线程附件改为直接落盘到 `saves/threads/<thread_id>/user-data/uploads`，解析成功后额外生成 `uploads/attachments/*.md`，不再依赖 MinIO 或显式上传到 sandbox
- 修复知识库文件列表包体异常膨胀：上传阶段不再把批次级 `content_hashes` 写入每个文件的 `processing_params`，并从数据库详情列表接口中移除该字段，改为按需读取单文件详情

## v0.4

### 新增
- 新增对于上传附件的智能体中间件，详见[文档](/advanced/agents-config.html#%E6%96%87%E4%BB%B6%E4%B8%8A%E4%BC%A0%E4%B8%AD%E9%97%B4%E4%BB%B6)
- 新增多模态模型支持（当前仅支持图片），详见[文档](/advanced/agents-config.html#%E5%A4%9A%E6%A8%A1%E6%80%81%E5%9B%BE%E7%89%87%E6%94%AF%E6%8C%81)
- 新建 DeepAgents 智能体（深度分析智能体），支持 todo，files 等渲染，支持文件的下载。
- 新增基于知识库文件生成思维导图功能（[#335](git@github.com:96980754/shanli/pull/335#issuecomment-3530976425)）
- 新增基于知识库文件生成示例问题功能（[#335](git@github.com:96980754/shanli/pull/335#issuecomment-3530976425)）
- 新增知识库支持文件夹/压缩包上传的功能（[#335](git@github.com:96980754/shanli/pull/335#issuecomment-3530976425)）
- 新增自定义模型支持、新增 dashscope rerank/embeddings 模型的支持
- 新增文档解析的图片支持，已支持 MinerU Officical、Docs、Markdown Zip格式
- 新增暗色模式支持并调整整体 UI（[#343](git@github.com:96980754/shanli/pull/343)）
- 新增知识库评估功能，支持导入评估基准或者自动构建评估基准（目前仅支持Milvus类型知识库）详见[文档](/intro/evaluation.html)
- 新增同名文件处理逻辑：遇到同名文件则在上传区域提示，是否删除旧文件
- 新增生产环境部署脚本，固定 python 依赖版本，提升部署稳定性
- 优化图谱可视化方式，统一图谱数据结构，统一使用基于 G6 的可视化方式，同时支持上传带属性的图谱文件，详见[文档](/intro/knowledge-base.html#_1-%E4%BB%A5%E4%B8%89%E5%85%83%E7%BB%84%E5%BD%A2%E5%BC%8F%E5%AF%BC%E5%85%A5)
- 优化 DBManager / ConversationManager，支持异步操作
- 优化 知识库详情页面，更加简洁清晰，增强文件下载功能

### 修复
- 修复 GitHub Actions 的 Ruff CI 在仓库根目录执行 `uv sync` 导致找不到 `backend/pyproject.toml` 的问题，同时统一检查路径为 `backend/package`
- 修复重排序模型实际未生效的问题
- 修复消息中断后消息消失的问题，并改善异常效果
- 修复当前版本如果调用结果为空的时候，工具调用状态会一直处于调用状态，尽管调用是成功的
- 修复检索配置实际未生效的问题
- 修复 sandbox 文件系统 `ls` 在异常输出下触发 `KeyError: 'path'` 的问题，并将工具调用异常降级为错误消息，避免直接中断聊天 stream
- 修复智能体状态面板中文件树仍依赖 `agent_state.files` 的问题，改为通过真实 `/api/filesystem/*` 接口按层懒加载后端可见文件系统，并让输入框下方状态按钮常态化打开工作区视图
- 为工作台新增 viewer-oriented filesystem service 与 `/api/viewer/filesystem/*` 接口，解耦 agent backend 语义，支持真实目录浏览、原始文件读取与下载
- 重写沙盒技术文档，明确 thread-local sandbox、viewer-oriented filesystem service、`/mnt` 命名空间、skills 可见性与当前实现边界，替换过时的 `/api/sandbox/*` 与 user-level 设计描述
- 收紧沙盒遗留代码：修复未注册 `sandbox_router` 中残留的 user/thread 参数错位，改进宿主机挂载路径映射逻辑，并为 remote sandbox provisioner 增加基础 URL 校验与销毁失败日志
- 修复 builtin skill 内容哈希计算对单文件使用 `read_bytes()` 的无上限内存读取问题，改为分块计算并补充回归测试

### 破坏性更新

- 移除 Chroma 的支持，当前版本标记为移除
- 移除模型配置预设的 TogetherAI


## v0.3
### Added
- 添加测试脚本，覆盖最常见的功能（已覆盖API）
- 新建 tasker 模块，用来管理所有的后台任务，UI 上使用侧边栏管理。Tasker 中获取历史任务的时候，仅获取 top100 个 task。
- 优化对文档信息的检索展示（检索结果页、详情页）
- 优化全局配置的管理模型，优化配置管理
- 支持 MinerU 2.5 的解析方法 <Badge type="info" text="0.3.5" />
- 修改现有的智能体Demo，并尽量将默认助手的特性兼容到 LangGraph 的 [`create_agent`](https://docs.langchain.com/oss/python/langchain/agents) 中
- 基于 create_agent 创建 SQL Viewer 智能体 <Badge type="info" text="0.3.5" />
- 优化 MCP 逻辑，支持 common + special 创建方式 <Badge type="info" text="0.3.5" />
- LightRAG 知识库应该可以支持修改 LLM

### Fixed
- 修复本地知识库的 metadata 和 向量数据库中不一致的情况。
- v1 版本的 LangGraph 的工具渲染有问题
- upload 接口会阻塞主进程
- LightRAG 知识库查看不了解析后的文本，偶然出现，未复现
- 智能体的加载状态有问题：（1）智能体加载没有动画；（2）切换对话和加载中，使用同一个loading状态。
- 前端工具调用渲染出现问题
- 当前 ReAct 智能体有消息顺序错乱的 bug，且不会默认调用工具
- 修复文件管理：（1）文件选择的时候会跨数据库；（2）文件校验会算上失败的文件；
